<h1>Практика (Блок 3) – Урок 2</h1>

<br>
<h2>🟢 УРОВЕНЬ 1 — Самое простое (войти в тему)</h2>

<br>
<h3>1️⃣ Сохранение одного слова в БД</h3>

> Задание: Сделай приложение, где пользователь вводит слово и нажимает кнопку «Сохранить».

<p>Подсказка:</p>
<ul>
    <li>Создай модель <b>WordItem</b> с полем <b>Text</b></li>
    <li>Создай таблицу <b>WordItems</b></li>
    <li>Сохраняй введённое слово в таблицу</li>
    <li>При запуске приложения загружай слово из таблицы и показывай на экране</li>
    <li><b>Entry</b> — поле ввода слова</li>
    <li><b>Label</b> — для отображения слова</li>
</ul>
<hr>

<br>
<h3>2️⃣ Кнопка «Показать данные»</h3>

> Задание: Сделай кнопку, которая при нажатии загружает и показывает все слова из таблицы.

<p>Подсказка:</p>
<ul>
    <li>Используй <b>List&lt;WordItem&gt;</b> для получения всех записей из таблицы</li>
    <li>Выводи слова в <b>Label</b>, разделяя запятыми</li>
    <li>Используй <b>VerticalStackLayout</b></li>
    <li>Используй <b>foreach</b></li>
</ul>
<hr>

<br>
<h3>3️⃣ Очистка поля ввода</h3>

> Задание: После сохранения — очищай Entry

<p>Подсказка:</p>

```c#
myEntry.Text = string.Empty;
```

<hr>

<br>
<h3>4️⃣ Проверка на пустой ввод</h3>

> Задание: Если пользователь вводит пустое слово — не сохраняй его

<p>Подсказка:</p>

```c#
if (string.IsNullOrWhiteSpace(myEntry.Text))
{
    // Не сохранять
}
else
{
    // Сохранять
}
```

<hr>
<br>
<h2>🟠 УРОВЕНЬ 2 — Среднее (усложнённые задачи)</h2>
<br>

<h3>5️⃣ Хранение списка любимых предметов</h3>
> Задание: Пользователь вводит любимый предмет и нажимает кнопку «Добавить». Сохраняй предметы в БД и показывай их списком.

<p>Подсказка:</p>
<ul>
    <li>Создай модель <b>SubjectItem</b> с полем <b>Name</b></li>
    <li>Создай таблицу <b>SubjectItems</b></li>
    <li>Используй <b>List&lt;SubjectItem&gt;</b> для получения всех записей из таблицы</li>
    <li>Выводи предметы в <b>Label</b>, разделяя запятыми</li>
    <li>Используй <b>VerticalStackLayout</b></li>
    <li>Используй <b>foreach</b></li>
    <li>Используй это в модели:

```c#
public string Name { get; set; }
```

</li>
</ul>
<hr>

<br>
<h3>6️⃣ Кнопка «Удалить последний»</h3>
> Задание: Сделай кнопку, которая удаляет последний добавленный предмет из таблицы.
<p>Подсказка:</p>
<ul>
    <li>Используй <b>database.Delete(item);</b></li>
    <li>Для получения последнего элемента используй:

```c#
var lastItem = database.Table<SubjectItem>().OrderByDescending(i => i.Id).FirstOrDefault();
```

</li>
</ul>

> OrderByDescending — сортирует по убыванию
> FirstOrDefault — получает первый элемент или null, если таблица пустая
<hr>

<br>
<h3>7️⃣ Подсчёт записей</h3>
> Задание: Показывай количество сохранённых предметов.
<p>Подсказка:</p>

```c#  
var count = database.Table<SubjectItem>().Count();
myLabel.Text = $"Всего предметов: {count}";
```

<hr>
<br>

<h3>8️⃣ Автозагрузка при старте</h3>
> Задание: При запуске приложения автоматически загружай и показывай все предметы из таблицы.
<p>Подсказка:</p>
<ul>
    <li>В методе конструктора страницы добавь код для загрузки и отображения предметов</li>
    <li>Используй уже созданный метод для отображения предметов</li>
</ul>

<hr>
<br> 

<h2>🟣 ТВОРЧЕСКИЕ ЗАДАНИЯ (на выбор)</h2>
<br>
<ul>
<li>Список желаний

> Задание: Пользователь вводит желание и нажимает кнопку «Добавить». Сохраняй желания в БД и показывай их списком.

</li>
<li>Счётчик нажатий
    
> Задание: При каждом нажатии на кнопку сохраняй количество нажатий и показывай его.

</li>
<li>Список покупок

> Задание: Пользователь вводит товар и нажимает кнопку «Добавить». Сохраняй товары в БД и показывай их списком.

</li>
<li>Избранные фильмы

> Задание: Пользователь вводит название фильма и нажимает кнопку «Добавить». Сохраняй фильмы в БД и показывай их списком.

</li>
</ul>