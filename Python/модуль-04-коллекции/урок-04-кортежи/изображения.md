# Что показать на проекторе — урок 4 «Кортежи» 🖼

> Три SVG-схемы. Открываются в браузере (`Ctrl+O` или перетащи файл). Показывай в момент из [преподавателю.md](преподавателю.md).

---

## 1. `изображения/кортеж-заморожен.svg` 🔒
**Когда:** Часть 1 (кортеж и неизменяемость), 5–19 мин. **Ключевая схема урока.**

Слева ✏️ список (`nums = [10, 20, 30]`): `nums[1] = 99` работает (элемент стал зелёным). Справа 🔒 кортеж (`point = (10, 20, 30)`): `point[1] = 99` даёт `TypeError`. Внизу — «кортеж берут для координат, цвета, даты».

**Как обсудить:**
- Список — **квадратные** скобки, меняется; кортеж — **круглые**, заморожен.
- Читать кортеж можно (индекс, срез, `in`), менять — нет.
- Зачем: чтобы важные данные (координата, цвет) не испортились случайно.

---

## 2. `изображения/распаковка.svg` 📦
**Когда:** Часть 2 (распаковка), 19–33 мин.

Слева: `point = (3, 5)` → стрелки вниз → `x = 3`, `y = 5` (`x, y = point`). Справа: обмен `a, b = b, a` (было 10,20 → стало 20,10). Внизу — распаковка в цикле `for x, y in points`.

**Как обсудить:**
- Одна строка `x, y = point` раскладывает кортеж по переменным.
- Обмен `a, b = b, a` — справа собирается кортеж и распаковывается.
- В цикле удобно: `for x, y in points` — сразу две координаты.

---

## 3. `изображения/художник-точки.svg` 🖌
**Когда:** Часть 4 (практикум), 45–63 мин.

Координатное поле с нарисованным домиком; вершины — синие точки с подписями-кортежами `(-2,-2)`, `(2,-2)`, `(2,1)`, `(0,3)`, `(-2,1)`. Панель кода `move_to(house[0])` + `for point in house: line_to(point)`. Внизу — команды и приём сдвига.

**Как обсудить:**
- `house` — список кортежей-вершин; соединяем их линиями в фигуру.
- Каждая вершина — кортеж `(x, y)`.
- Чтобы двигать фигуру, кортеж не меняем, а собираем новый: `(x + 4, y)`.

---

## 💡 Общие советы
- Схемы светлые, читаются с задних парт; масштаб `Ctrl` + `+`.
- `кортеж-заморожен.svg` держи открытой Часть 1 — правило «список меняется / кортеж нет» — стержень урока.
- Приём «предскажи»: спроси, что выведет `x, y = (7, 2); print(y, x)`.
