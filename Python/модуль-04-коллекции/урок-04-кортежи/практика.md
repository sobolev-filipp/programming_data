# Практика урока 4 «Кортежи» ✍️

> **20 задач** в 3 уровнях + 🐢 turtle-практикум (10 заданий). Сначала — короткая разминка на синтаксис, потом **настоящие мини-программы** с координатами, оценками и температурами.
> 🟢 разминка · 🟡 мини-программы · 🔴 со звёздочкой.
>
> 📌 Забыл тему — [итого.md](итого.md). Нужен разбор с нуля — [самоучитель.md](самоучитель.md).

---

## 🟢 Уровень 1 — разминка (создать, прочитать, распаковать)

**1.1 — Точка.** Создай кортеж `point = (4, 7)` — координаты точки. Выведи отдельно x (первое число) и y (второе) по индексу, а затем длину кортежа.

**1.2 — Кортеж заморожен.** Возьми тот же `point` и попробуй записать `point[0] = 0`. Запусти и прочитай, какую ошибку выдаёт Python — так ты убедишься, что кортеж менять нельзя.

**1.3 — Распаковка.** Разложи кортеж `(4, 7)` в две отдельные переменные `x` и `y` одной строкой, потом выведи `x` и `y`.
> 💡 Слева два имени, справа кортеж.

**1.4 — Обмен.** Даны `a = 5` и `b = 9`. Поменяй их значения местами в одну строку через кортеж и выведи `a` и `b` после обмена.

**1.5 — Кортеж из одного.** Создай кортеж, в котором ровно ОДИН элемент — число 7. Выведи его `type`, чтобы проверить, что это кортеж, а не число.
> 💡 Одна деталь превращает `(7)` в кортеж.

**1.6 — Список точек.** Заведи список из трёх точек-кортежей `[(0, 0), (2, 5), (4, 1)]`. Выведи каждую точку с новой строки, пройдя список циклом.

---

## 🟡 Уровень 2 — мини-программы (применяем на практике)

**2.1 — Анкета.** Дан кортеж `("Аня", 12, "Москва")`. Распакуй его в три переменные `name`, `age`, `city` и выведи фразу: `Аня, 12 лет, из города Москва`.

**2.2 — Точка в четверти.** Спроси у пользователя координаты точки x и y (два `input`, числа). Сложи их в кортеж и определи, в какой она четверти: если `x > 0 и y > 0` — «первая четверть», иначе выведи «не в первой четверти».
> 💡 Условие с `and` — точка в первой четверти, когда ОБА числа положительны.

**2.3 — Список учеников.** Дан список пар `[("Аня", 5), ("Боря", 3), ("Вера", 4), ("Глеб", 2)]` (имя, оценка). Выведи всех в виде `Аня — 5`, а затем отдельно — имена только тех, у кого оценка 4 и выше.
> 💡 Распаковывай пару прямо в цикле: `for name, mark in students`.

**2.4 — Погода по дням.** Дан список `[("Пн", 12), ("Вт", 15), ("Ср", 9), ("Чт", 11), ("Пт", 14)]` (день, температура). Найди и выведи самый тёплый и самый холодный день недели (с названием дня).
> 💡 Идёшь по списку, запоминаешь день с наибольшей и наименьшей температурой (как поиск максимума/минимума с запоминанием).

**2.5 — Победитель матча.** Дан список результатов игры `[("Иван", 5), ("Мария", 7), ("Кирилл", 4)]` (имя, очки). Найди победителя — у кого больше всех очков — и выведи его имя и счёт.

**2.6 — Расстояние между точками.** Даны две точки `p1 = (0, 0)` и `p2 = (3, 4)`. Распакуй обе и посчитай расстояние между ними по формуле `√((x2−x1)² + (y2−y1)²)`. Должно получиться 5.0.
> 💡 Квадратный корень — это возведение в степень `** 0.5`.

**2.7 — Сколько это времени.** Спроси у пользователя число секунд (например, 200). С помощью готовой функции `divmod` разложи его на минуты и секунды и выведи: `200 сек = 3 мин 20 сек`.
> 💡 `divmod(секунды, 60)` вернёт пару `(минуты, остаток_секунд)` — распакуй её.

**2.8 — Яркий или тёмный цвет.** Дан цвет как кортеж RGB, например `color = (30, 40, 50)`. Распакуй его в `r, g, b` и определи: если сумма трёх чисел меньше 300 — «тёмный цвет», иначе — «яркий цвет».

---

## 🔴 Уровень 3 — со звёздочкой

**3.1 — Центр точек.** Дан список точек. Найди «среднюю» точку: средний x и средний y (сумма всех x делить на количество, так же для y). Выведи центр как кортеж.

**3.2 — Угадай координату.** Компьютер загадывает случайную точку `(x, y)`, где x и y — числа от 1 до 3 (`random.randint`). Пользователь пытается угадать обе координаты (два `input`). Если угадал обе — «Победа!», иначе подскажи правильный ответ.

**3.3 — Самая дальняя точка.** Дан список точек. Найди ту, что дальше всех от начала координат `(0, 0)`, и выведи её.
> 💡 Сравнивать удобно по `x*x + y*y` — чем больше, тем дальше.

**3.4 — Тёмные цвета.** Дан список цветов-кортежей `[(30,30,30), (250,200,10), (10,20,30)]`. Собери в новый список только «тёмные» — те, где сумма трёх чисел меньше 300.
> 💡 В цикле удобно распаковать сразу `for r, g, b in colors`.

**3.5 — Сдвинуть все точки.** Дан список точек. Собери НОВЫЙ список, в котором каждая точка сдвинута на +2 по x и +1 по y (старый список не меняем — кортеж пересобираем).

**3.6 — Найди отличие.** Даны два кортежа одинаковой длины, например `(1, 2, 3, 4)` и `(1, 9, 3, 8)`. Пройди их и выведи индексы, на которых значения различаются (здесь — 1 и 3).
> 💡 Иди по позициям через `range(len(...))` и сравнивай элементы на одинаковом индексе.

---

## 🐢 Практикум «Художник по точкам» (10 заданий, на компьютере)

Открой [`материалы/художник.py`](материалы/художник.py). На координатном поле уже готова фигура `house` — список её вершин, каждая вершина это кортеж `(x, y)`. Меняй номер задания в переменной `TASK` и пиши алгоритм в отмеченном блоке.

**Команды каркаса:**
- `move_to(point)` — перенести перо в точку `point` **без линии** (чтобы начать фигуру).
- `line_to(point)` — провести **линию** в точку `point` от текущего положения.
- `dot(point)` — поставить точку-маркер в вершине.

Здесь `point` — это всегда кортеж `(x, y)`. Чтобы двигать фигуру, кортеж менять нельзя — **распакуй** его (`x, y = point`) и собери новую точку, например `(x + 4, y)`.

**Задание 1.** Нарисуй домик: встань в первую вершину `move_to(house[0])`, а затем в цикле проведи линию в каждую вершину списка `house`.

**Задание 2.** Поставь маркер `dot` в каждой вершине домика (пройди список и вызывай `dot` для каждой точки).

**Задание 3.** Нарисуй домик, сдвинутый вправо на 4 клетки — у каждой вершины увеличь x на 4, собрав новую точку.

**Задание 4.** Нарисуй домик, поднятый вверх на 3 клетки — у каждой вершины увеличь y на 3.

**Задание 5.** Нарисуй домик, отражённый вниз — у каждой вершины поменяй знак y (было `(x, y)`, стало `(x, -y)`).

**Задание 6.** Нарисуй домик в 2 раза больше — умножь и x, и y каждой вершины на 2.

**Задание 7.** Нарисуй сразу два домика рядом: обычный и его копию со сдвигом вправо (два прохода по списку).

**Задание 8.** Нарисуй только крышу домика — это вершины с индексами 2, 3, 4, то есть срез `house[2:5]` (срез из Урока 3!).

**Задание 9.** Нарисуй домик, соединяя вершины в обратном порядке — используй перевёрнутый список `house[::-1]`.

**Задание 10.** Придумай свою фигуру: составь список из 5 и более точек-кортежей и нарисуй её линиями.

---

## ✅ Как проверить себя

- Кортеж пишешь в круглых скобках `( )` и не пытаешься менять его элементы.
- Точку раскладываешь распаковкой `x, y = point`, а не через `point[0]` и `point[1]`.
- Чтобы «сдвинуть» точку, собираешь новый кортеж, а не меняешь старый.
- Победителя/самый тёплый день ищешь перебором с запоминанием рекордсмена.

> 🏆 Осилил 🔴? В Уроке 5 — **словари**: хранение по ключу (имя → номер телефона), телефонная книга и инвентарь.
> 📚 Ещё задачи — в [банке заданий `05-коллекции.md`](../../банк-заданий/05-коллекции.md).
