# Практика урока 4 «Кортежи» ✍️

> **22 задачи** в 3 уровнях + 🐢 turtle-практикум (10 заданий). У каждой — **короткая подсказка «как делать»** (наводка, не решение). Помни: **кортеж менять нельзя.**
> 🟢 базовые · 🟡 средние · 🔴 со звёздочкой.
>
> 📌 Забыл тему — [итого.md](итого.md). Нужен разбор с нуля — [самоучитель.md](самоучитель.md).

---

## 🟢 Уровень 1 — базовые (кортеж, распаковка)

**1.1 — Точка.** Заведи кортеж `point = (4, 7)`. Выведи x, y по индексу и длину.
> 💡 `point[0]`, `point[1]`, `len(point)`.

**1.2 — Заморожен.** Попробуй `point[0] = 0` — прочитай, какую ошибку выдаст Python.
> 💡 Будет `TypeError` — кортеж менять нельзя.

**1.3 — Распаковка.** Разложи кортеж `(4, 7)` в переменные `x` и `y`, выведи их.
> 💡 `x, y = point`.

**1.4 — Три поля.** Распакуй `("Аня", 12, "Москва")` в `name, age, city` и выведи фразой.
> 💡 `name, age, city = ...`.

**1.5 — Цвет RGB.** Заведи кортеж-цвет `(255, 128, 0)`. Выведи, сколько в нём красного (первое число).
> 💡 `color[0]`.

**1.6 — Обмен.** Даны `a = 5`, `b = 9`. Поменяй их местами через кортеж.
> 💡 `a, b = b, a`.

**1.7 — Кортеж из одного.** Создай кортеж из ОДНОГО элемента (число 7). Проверь `type`.
> 💡 `(7,)` — с запятой!

**1.8 — Список кортежей.** Заведи `points = [(0,0), (2,2)]` и выведи каждую точку циклом.
> 💡 `for p in points: print(p)`.

---

## 🟡 Уровень 2 — средние (распаковка в цикле, возврат)

**2.1 — Точки с распаковкой.** Дан список точек. Выведи `x=.., y=..` через `for x, y in points`.
> 💡 `for x, y in points:`.

**2.2 — Люди.** Дан `[("Аня", 12), ("Боря", 13)]`. Выведи `Аня — 12 лет` распаковкой.
> 💡 `for name, age in people:`.

**2.3 — Возврат двух значений.** Напиши функцию, возвращающую минимум и максимум списка.
> 💡 `return min(nums), max(nums)`; `low, high = ...`.

**2.4 — Частное и остаток.** Функция `divmod2(a, b)` возвращает `a//b` и `a%b`. Распакуй результат.
> 💡 `return a // b, a % b`.

**2.5 — Сумма координат.** Дан список точек — посчитай сумму всех x и сумму всех y.
> 💡 `for x, y in points: sx += x; sy += y`.

**2.6 — Ближе к нулю.** Из списка точек найди ту, у которой x == 0.
> 💡 `for x, y in points: if x == 0: ...`.

**2.7 — Кортеж как есть.** Дан кортеж чисел — посчитай его сумму и максимум (`sum`, `max` работают).
> 💡 `sum(tpl)`, `max(tpl)`.

**2.8 — Срез кортежа.** Дан кортеж из 6 чисел — выведи его первые 3 (срез работает и с кортежем).
> 💡 `tpl[:3]`.

**2.9 — Расстояние.** Даны две точки. Посчитай расстояние между ними.
> 💡 `x1,y1 = p1`; `x2,y2 = p2`; `((x2-x1)**2 + (y2-y1)**2) ** 0.5`.

**2.10 — Отражение точки.** Дан кортеж `(x, y)` — собери новый кортеж `(y, x)` (отражение по диагонали).
> 💡 `x, y = p`; `new = (y, x)`.

---

## 🔴 Уровень 3 — со звёздочкой

**3.1 — Центр точек.** Найди «среднюю» точку списка: средний x и средний y.
> 💡 Суммируй x и y в цикле, подели на `len(points)`.

**3.2 — Список цветов.** Заведи список кортежей-цветов. Выведи только «тёмные» (сумма RGB < 300).
> 💡 `for r, g, b in colors: if r + g + b < 300: ...`.

**3.3 — Сдвиг всех точек.** Дан список точек — собери НОВЫЙ список, сдвинув все на (+2, +1).
> 💡 `for x, y in points: shifted.append((x + 2, y + 1))`.

**3.4 — Самая дальняя.** Найди точку, наиболее удалённую от начала координат (0, 0).
> 💡 Считай `x**2 + y**2` для каждой, запоминай максимум.

**3.5 — 🎓 Кортеж-ключ.** Заведи словарь-предпросмотр: ключ — кортеж `(город, "погода")`, значение — число. Достань по ключу.
> 💡 `d = {("Москва", "погода"): 5}`; `d[("Москва", "погода")]`.

**3.6 — 🎓 Мин и макс сразу.** Напиши функцию, возвращающую минимум, максимум И среднее списка (кортеж из трёх).
> 💡 `return min(n), max(n), sum(n)/len(n)`; `lo, hi, avg = ...`.

---

## 🐢 Практикум «Художник по точкам» (10 заданий, на компьютере)

Открой [`материалы/художник.py`](материалы/художник.py). Готова `house` — список вершин-кортежей. Команды: `move_to(p)`, `line_to(p)`, `dot(p)`. Меняй `TASK`. Для преобразований **распакуй** кортеж: `x, y = point`, собери новый.

**🟢 Рисуем:** **1** нарисуй домик (`move_to(house[0])` + цикл `line_to`) · **2** поставь `dot` в каждой вершине.
**🟡 Двигаем (распаковка + новый кортеж):** **3** сдвинь вправо на 4 (`(x+4, y)`) · **4** сдвинь вверх на 3 (`(x, y+3)`) · **5** отрази по вертикали (`(x, -y)`) · **6** увеличь в 2 раза (`(x*2, y*2)`).
**🔴 Сложнее:** **7** нарисуй два домика рядом (два прохода со сдвигом) · **8** только крышу — срез `house[2:5]` · **9** в обратном порядке — `house[::-1]` · **10** свой рисунок: составь список из 5+ точек-кортежей и нарисуй.
> 💡 Кортеж менять нельзя — поэтому для сдвига собираем НОВЫЙ: `line_to((x + 4, y))`.

---

## ✅ Как проверить себя

- Кортеж пишешь в круглых скобках и не пытаешься менять.
- Точку раскладываешь распаковкой `x, y = point`, а не `point[0]`, `point[1]`.
- Для сдвига собираешь новый кортеж, а не меняешь старый.

> 🏆 Осилил 🔴? В Уроке 5 — **словари**: хранение по ключу (имя → номер), телефонная книга и инвентарь.
> 📚 Ещё задачи — в [банке заданий `05-коллекции.md`](../../банк-заданий/05-коллекции.md).
