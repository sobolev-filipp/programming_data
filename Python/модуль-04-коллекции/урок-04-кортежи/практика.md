# Практика урока 4 «Кортежи» ✍️

> **22 задачи** в 3 уровнях + 🐢 turtle-практикум (10 заданий). Читай задание целиком — там сказано, что сделать и что должно получиться. Подсказка есть только там, где легко застрять.
> 🟢 базовые · 🟡 средние · 🔴 со звёздочкой.
>
> 📌 Забыл тему — [итого.md](итого.md). Нужен разбор с нуля — [самоучитель.md](самоучитель.md).

---

## 🟢 Уровень 1 — базовые (создать, прочитать, распаковать)

**1.1 — Точка.** Создай кортеж `point = (4, 7)` — это координаты точки. Выведи отдельно первое число (x) и второе (y) по индексу, а затем длину кортежа.

**1.2 — Кортеж заморожен.** Возьми тот же `point` и попробуй записать `point[0] = 0`. Запусти и прочитай, какую ошибку выдаёт Python — так ты убедишься, что кортеж менять нельзя.

**1.3 — Распаковка.** Разложи кортеж `(4, 7)` в две отдельные переменные `x` и `y` одной строкой, потом выведи `x` и `y`.
> 💡 Это та самая распаковка из урока — слева два имени, справа кортеж.

**1.4 — Анкета.** Дан кортеж `("Аня", 12, "Москва")`. Распакуй его в три переменные `name`, `age`, `city` и выведи фразу вида `Аня, 12 лет, из города Москва`.

**1.5 — Цвет.** Заведи кортеж-цвет `color = (255, 128, 0)` (это RGB — красный, зелёный, синий). Выведи, сколько в нём красного (первое число) и синего (последнее).

**1.6 — Обмен.** Даны `a = 5` и `b = 9`. Поменяй их значения местами в одну строку через кортеж и выведи `a` и `b` после обмена.
> 💡 Вспомни трюк `a, b = b, a`.

**1.7 — Кортеж из одного.** Создай кортеж, в котором ровно ОДИН элемент — число 7. Выведи его `type`, чтобы проверить, что это кортеж, а не число.
> 💡 Тут легко ошибиться: одна деталь превращает `(7)` в кортеж.

**1.8 — Список точек.** Заведи список из трёх точек-кортежей, например `[(0, 0), (2, 5), (4, 1)]`. Выведи каждую точку с новой строки, пройдя список циклом.

---

## 🟡 Уровень 2 — средние (распаковка в цикле, готовые функции)

**2.1 — Точки с распаковкой.** Дан список точек `[(0,0), (3,4), (6,0)]`. Пройди его так, чтобы на каждом шаге сразу были отдельные `x` и `y`, и выведи строки вида `x=3, y=4`.
> 💡 Распаковку можно писать прямо в заголовке цикла `for`.

**2.2 — Список людей.** Дан список `[("Аня", 12), ("Боря", 13)]`. Выведи для каждого строку `Аня — 12 лет`, распаковывая пару прямо в цикле.

**2.3 — Частное и остаток.** Вызови готовую функцию `divmod(23, 4)` — она вернёт пару-кортеж. Распакуй результат в `q` и `r` и выведи `частное = .., остаток = ..`.
> 💡 `divmod` уже написана за тебя — её нужно только вызвать и распаковать.

**2.4 — Сумма координат.** Дан список точек. Посчитай отдельно сумму всех x и сумму всех y (два накопителя), выведи обе суммы.

**2.5 — Отражение точки.** Дана точка `p = (3, 8)`. Собери НОВЫЙ кортеж, в котором x и y поменялись местами — `(8, 3)`. Выведи его.
> 💡 Кортеж не меняем — распаковываем и собираем новый.

**2.6 — Первые числа кортежа.** Дан кортеж из шести чисел. Возьми из него первые три (срез работает и с кортежем) и выведи.

**2.7 — Сумма и максимум кортежа.** Дан кортеж чисел. Посчитай его сумму и найди наибольшее число — готовые функции работают и с кортежем.

**2.8 — Расстояние между точками.** Даны две точки `p1 = (0, 0)` и `p2 = (3, 4)`. Распакуй обе и посчитай расстояние между ними по формуле `√((x2−x1)² + (y2−y1)²)`. Должно получиться 5.0.
> 💡 Квадратный корень — это возведение в степень `** 0.5`.

---

## 🔴 Уровень 3 — со звёздочкой

**3.1 — Центр точек.** Дан список точек. Найди «среднюю» точку: средний x и средний y (сумма всех x делить на количество, так же для y). Выведи центр как кортеж.

**3.2 — Тёмные цвета.** Дан список цветов-кортежей `[(30,30,30), (250,200,10), ...]`. Собери в новый список только «тёмные» — те, где сумма трёх чисел меньше 300.
> 💡 В цикле удобно распаковать сразу `for r, g, b in colors`.

**3.3 — Сдвинуть все точки.** Дан список точек. Собери НОВЫЙ список, в котором каждая точка сдвинута на +2 по x и +1 по y (старый список не меняем).

**3.4 — Самая дальняя точка.** Дан список точек. Найди ту, что дальше всех от начала координат `(0, 0)`, и выведи её.
> 💡 Сравнивать удобно по `x*x + y*y` — чем больше, тем дальше.

**3.5 — Ближайшая к нулю.** Тот же список — найди, наоборот, самую близкую к `(0, 0)` точку.

**3.6 — Зеркальный список.** Дан список точек. Собери новый список, где у каждой точки x и y поменяны местами (отражение по диагонали).

---

## 🐢 Практикум «Художник по точкам» (10 заданий, на компьютере)

Открой [`материалы/художник.py`](материалы/художник.py). На координатном поле уже готова фигура `house` — список её вершин, каждая вершина это кортеж `(x, y)`. Меняй номер задания в переменной `TASK` и пиши алгоритм в отмеченном блоке.

**Команды каркаса:**
- `move_to(point)` — перенести перо в точку `point` **без линии** (чтобы начать фигуру).
- `line_to(point)` — провести **линию** в точку `point` от текущего положения.
- `dot(point)` — поставить точку-маркер в вершине.

Здесь `point` — это всегда кортеж `(x, y)`. Чтобы двигать фигуру, кортеж менять нельзя — **распакуй** его (`x, y = point`) и собери новую точку, например `(x + 4, y)`.

**Задание 1.** Нарисуй домик: встань в первую вершину `move_to(house[0])`, а затем в цикле проведи линию в каждую вершину списка `house`.
**Задание 2.** Поставь маркер `dot` в каждой вершине домика (пройди список и вызывай `dot` для каждой точки).
**Задание 3.** Нарисуй домик, сдвинутый вправо на 4 клетки — у каждой вершины увеличь x на 4, собрав новую точку.
**Задание 4.** Нарисуй домик, поднятый вверх на 3 клетки — у каждой вершины увеличь y на 3.
**Задание 5.** Нарисуй домик, отражённый вниз — у каждой вершины поменяй знак y (было `(x, y)`, стало `(x, -y)`).
**Задание 6.** Нарисуй домик в 2 раза больше — умножь и x, и y каждой вершины на 2.
**Задание 7.** Нарисуй сразу два домика рядом: обычный и его копию со сдвигом вправо (два прохода по списку).
**Задание 8.** Нарисуй только крышу домика — это вершины с индексами 2, 3, 4, то есть срез `house[2:5]` (срез из Урока 3!).
**Задание 9.** Нарисуй домик, соединяя вершины в обратном порядке — используй перевёрнутый список `house[::-1]`.
**Задание 10.** Придумай свою фигуру: составь список из 5 и более точек-кортежей и нарисуй её линиями.

---

## ✅ Как проверить себя

- Кортеж пишешь в круглых скобках `( )` и не пытаешься менять его элементы.
- Точку раскладываешь распаковкой `x, y = point`, а не через `point[0]` и `point[1]`.
- Чтобы «сдвинуть» точку, собираешь новый кортеж, а не меняешь старый.

> 🏆 Осилил 🔴? В Уроке 5 — **словари**: хранение по ключу (имя → номер телефона), телефонная книга и инвентарь.
> 📚 Ещё задачи — в [банке заданий `05-коллекции.md`](../../банк-заданий/05-коллекции.md).
