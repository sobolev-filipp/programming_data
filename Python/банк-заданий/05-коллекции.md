# 🏦 Банк заданий 05 — Коллекции

**Темы (накопительно):** всё из [банка 03](03-циклы.md) + **списки вглубь** (`append/insert/extend`, `remove/pop/del/clear`, изменение по индексу, `[-1]`, `count/index`, `sort/sorted/reverse`), кортежи, срезы, словари, множества.

> 🎯 Отдельный пул для домашки, резерва и повторения — **не повторяет** практику урока. Уровни: 🟢 базовые · 🟡 средние · 🔴 со звёздочкой. Подсказки — короткие наводки. Ответы спрятаны в конце (открой `▸ Ответы`, только если застрял).
>
> ⚠️ Помни: методы-изменялки (`append/sort/remove/reverse`) меняют список **на месте** и возвращают `None` — не пиши `lst = lst.метод(...)`.

---

## 📋 Списки: изменение, добавление, удаление (Урок 1)

### 🟢 Базовые

1. Заведи список из 5 городов. Выведи первый, последний (`[-1]`) и длину.
2. В списке `["чай", "кофе", "сок"]` замени средний элемент на `"вода"` по индексу.
3. Создай пустую корзину `[]` и добавь `append`-ом 4 товара. Выведи её.
4. Из списка удали один элемент по значению (`remove`).
5. Сними последний элемент списка через `pop` в переменную и выведи, что снял.
6. Добавь в список новый элемент на позицию 0 (`insert`).
7. Посчитай длину списка после двух `append` и одного `remove`.
8. Выведи предпоследний элемент списка через отрицательный индекс.

### 🟡 Средние

9. К списку `["меч", "щит"]` добавь сразу три предмета через `extend`.
10. В списке `[1, 2, 2, 3, 2]` посчитай, сколько раз встречается `2` (`count`).
11. Найди позицию элемента `"Москва"` в списке городов (`index`).
12. Отсортируй список чисел по возрастанию и по убыванию (`sort` / `sort(reverse=True)`).
13. Разверни список из 6 элементов задом наперёд (`reverse`).
14. Удали элемент по индексу 2 через `del`.
15. Дан `data = [3, 1, 2]`. Получи отсортированную копию через `sorted`, не меняя `data`. Выведи оба.
16. Выведи список дел с номерами (`1. ...`, `2. ...`) через `enumerate`.

### 🔴 Со звёздочкой

17. 🎓 Собери приложение «Список дел»: меню в `while` (добавить/удалить/показать/выход), удаление через `pop(number - 1)`.
18. 🎓 Безопасно удали элемент: только если он есть (`if x in lst`), иначе сообщи «нет такого».
19. 🎓 Удали ВСЕ вхождения числа из списка (`while x in lst: lst.remove(x)`).
20. 🎓 Поменяй местами первый и последний элементы (`lst[0], lst[-1] = lst[-1], lst[0]`).
21. 🎓 «Топ игроков»: добавь результат, отсортируй по убыванию, выведи первые три.

---

## 📊 Действия со списками: итоги, сортировка, фильтр (Урок 2)

### 🟢 Базовые

22. Дан список чисел — выведи их сумму и количество (`sum`, `len`).
23. Выведи максимум и минимум списка (`max`, `min`).
24. Посчитай среднее списка оценок, округли до 1 знака (`round(sum/len, 1)`).
25. Отсортируй список чисел по возрастанию новым списком (`sorted`).
26. Отсортируй тот же список по убыванию (`sorted(..., reverse=True)`).
27. Выведи размах списка — разницу максимума и минимума.
28. Дан список температур за неделю — выведи самую высокую, низкую и среднюю.

### 🟡 Средние

29. Дан список очков — выведи три лучших результата (топ-3 через `sorted` + индексы).
30. Два списка (имена, очки) — выведи имя игрока с максимальным счётом (`index` + `max`).
31. Отсортируй список слов по длине (`key=len`).
32. Отсортируй имена без учёта регистра (`key=str.lower`).
33. Дан `data` — получи отсортированную копию через `sorted`, докажи, что оригинал цел.
34. Выведи имя аутсайдера — игрока с минимальным счётом.
35. Отсортируй города по алфавиту и выведи с номерами (`enumerate(sorted(...))`).

### 🔴 Со звёздочкой

36. 🎓 Собери в новый список фильтром тех, у кого счёт ≥ 80 (`[]`+`for`+`if`+`append`).
37. 🎓 Из списка чисел собери только чётные.
38. 🎓 Посчитай средний балл, затем собери в список тех, кто выше среднего.
39. 🎓 Посчитай сумму и максимум списка ЦИКЛОМ (без `sum`/`max`) и сверь со встроенными.
40. 🐢 В каркасе `диаграмма.py` построй столбчатую диаграмму по списку (`for v in scores: bar(v)`), потом отсортируй перед выводом.

---

## ✂️ Срезы: нарезка данных (Урок 3)

### 🟢 Базовые

41. Дан список из 8 элементов — выведи первые 3 (`[:3]`) и последние 3 (`[-3:]`).
42. Выведи список без первого и последнего элемента (`[1:-1]`).
43. Выведи элементы с индексами 2, 3, 4 (`[2:5]`) — помни, 5 не входит.
44. Сделай копию списка через срез `[:]` и выведи её.
45. Выведи первую и вторую половину списка из 10 чисел (`[:5]`, `[5:]`).

### 🟡 Средние

46. Выведи каждый второй элемент (`[::2]`) и каждый третий (`[::3]`).
47. Разверни список задом наперёд (`[::-1]`).
48. Спроси слово, выведи его наоборот (`word[::-1]`).
49. Проверь, палиндром ли слово (`word == word[::-1]`).
50. Выведи первые 3 буквы слова и последние 3 (`word[:3]`, `word[-3:]`).
51. Дан список очков — выведи топ-3 (`sorted(scores, reverse=True)[:3]`).

### 🔴 Со звёздочкой

52. 🎓 Покажи разницу `a = lst` и `b = lst[:]`: измени оба, сравни с `lst` (ловушка ссылки).
53. 🎓 Выведи последние 4 элемента в обратном порядке (`lst[-4:][::-1]`).
54. 🎓 Удали через срез элементы со 2-го по 4-й (`del lst[2:5]`).
55. 🐢 В каркасе `пылесос_срез.py` убери первые 5 клеток (`cells[:5]`), каждую вторую (`cells[::2]`) и все справа налево (`cells[::-1]`).

---

## 🔒 Кортежи: координаты и константы (Урок 4)

### 🟢 Базовые

56. Заведи кортеж `point = (4, 7)` — выведи оба числа по индексу и длину.
57. Попробуй изменить кортеж (`point[0] = 0`) — прочитай, какую ошибку выдаст Python.
58. Распакуй кортеж `(4, 7)` в переменные `x`, `y` и выведи их.
59. Поменяй местами два значения через кортеж (`a, b = b, a`).
60. Создай кортеж из одного элемента (число 7) — не забудь запятую `(7,)`.
61. Дан список точек `[(0,0), (2,2)]` — выведи каждую точку циклом.

### 🟡 Средние

62. Дан список точек — выведи `x=.., y=..` через `for x, y in points`.
63. Вызови готовую `divmod(23, 4)`, распакуй результат в `q, r` и выведи частное и остаток.
64. Дан кортеж чисел — посчитай его сумму и максимум (`sum`, `max` работают и с кортежем).
65. Дан список точек — посчитай сумму всех x и всех y.
66. Из точки `(x, y)` собери новую `(y, x)` (отражение по диагонали).
67. Возьми срез кортежа из 6 чисел — первые три (`tpl[:3]`).

### 🔴 Со звёздочкой

68. 🎓 Найди «среднюю» точку списка: средний x и средний y.
69. 🎓 Сдвинь все точки списка на (+2, +1), собрав НОВЫЙ список кортежей.
70. 🎓 Дан список точек — найди самую дальнюю от начала координат `(0, 0)` (по `x*x + y*y`).
71. 🐢 В каркасе `художник.py` нарисуй домик (задание 1), сдвинь его вправо (`(x+4, y)`) и нарисуй только крышу срезом (`house[2:5]`).

---

## 🔑 Словари: ключ → значение (Урок 5)

### 🟢 Базовые

72. Создай словарь `ages` (имя → возраст) из трёх человек. Выведи возраст одного из них по ключу и общее число людей через `len`.
73. Заведи словарь `capitals` (страна → столица, 3 пары). Выведи столицу одной страны.
74. Создай пустой словарь `book = {}` и добавь в него два контакта (имя → телефон). Выведи книгу.
75. Дан словарь цен `{"хлеб": 40, "молоко": 70}`. Измени цену молока и добавь новый товар. Выведи словарь.
76. Для словаря-книги проверь через `in`, есть ли контакт «Аня» и есть ли «Гена» (выведи `True/False`).

### 🟡 Средние

77. Достань телефон через `.get` для существующего имени и для отсутствующего — с запасным значением `"не найден"` (программа не должна падать).
78. Пройди словарь `ages` циклом и выведи каждого в виде `Аня — 12 лет`.
79. Для словаря цен посчитай общую стоимость через `sum(...values())` и выведи отдельно все ключи и все значения.
80. Найди в словаре `ages` имя самого старшего человека (пройди ключи, запоминай рекорд).
81. Мини-словарь-переводчик `{"cat": "кот", "dog": "пёс"}`: спроси слово (`input`) и выведи перевод через `.get` (или `"нет"`).

### 🔴 Со звёздочкой

82. 🎓 Спроси слово и посчитай словарём, сколько раз встречается каждая буква (`d[ch] = d.get(ch, 0) + 1`).
83. 🎓 Дан список голосов — посчитай словарём голоса за каждого и определи победителя.
84. 🎓 Слей два словаря-инвентаря так, чтобы количество одинаковых предметов складывалось.
85. 🎓 Создай вложенный словарь-карточки героев (`{"Маг": {"hp": 70}}`) и выведи здоровье Мага (`heroes["Маг"]["hp"]`).

---

## 🧊 Множества + перебор словаря (Урок 6)

### 🟢 Базовые

86. Убери дубли из списка `[1, 2, 2, 3, 3, 3]` через `set(...)` и выведи, сколько разных чисел.
87. Проверь через `in`, есть ли «код» в множестве `{"питон", "код", "игра"}`.
88. Даны `{1,2,3}` и `{2,3,4}`. Выведи объединение и пересечение.
89. Создай пустое множество (не `{}`!) и добавь в него три элемента.

### 🟡 Средние

90. Дан словарь `{"кофе":120, "чай":80}` — выведи его парами через `.items()` строками `кофе — 120`.
91. Даны друзья двух людей (два множества) — выведи общих и всех вместе.
92. Из списка слов с повторами выведи, сколько разных слов (через множество).
93. Дано множество приглашённых и множество пришедших — выведи, кто не пришёл (разность).

### 🔴 Со звёздочкой

94. Спроси слово и выведи, из скольких РАЗНЫХ букв оно состоит (`set(слово)`).
95. Дан список голосов — счётчиком посчитай голоса и выведи парами `.items()`, затем назови победителя.
96. Убери дубли из списка, СОХРАНИВ порядок появления (множество `seen` + список).
97. Даны два множества — выведи, что есть только у одного из них (симметрическая разность `^`).

---

## 🗃️ Мини-база данных: список словарей (Урок 7)

### 🟢 Базовые

98. Создай список из трёх героев-словарей (`"имя"`, `"hp"`). Выведи имя второго и число записей.
99. Пройди список героев и выведи каждого строкой `Маг — hp 70`.
100. Добавь в таблицу нового героя через `append` и уменьши `hp` первого на 10.

### 🟡 Средние

101. Дан список товаров (`"название"`, `"цена"`). Собери в новый список все дешевле 100.
102. Дан список учеников (`"имя"`, `"оценка"`) — посчитай средний балл класса.
103. Найди в списке героев запись по имени (перебор + флаг `None`) и выведи её.
104. Посчитай словарём-счётчиком, сколько героев каждого класса (поле `"класс"`).

### 🔴 Со звёздочкой

105. Построй рейтинг игроков по очкам через пары `(очки, имя)` и `sort`.
106. Выведи топ-3 игроков по полю (пары-кортежи + `sort` + срез `[:3]`).
107. Создай героя с полем-списком `"вещи"` и добавь в его инвентарь два предмета.
108. Сделай мини-базу с меню: показать / добавить / найти / удалить / выход.

---

<details>
<summary>▸ Ответы (подсмотри, только если застрял)</summary>

**1.** `cities[0]`, `cities[-1]`, `len(cities)`.
**2.** `drinks[1] = "вода"`.
**3.** `cart = []`, затем 4× `cart.append("...")`.
**5.** `last = nums.pop()` — снимает последний и возвращает.
**6.** `lst.insert(0, "...")`.
**8.** `lst[-2]`.
**9.** `items.extend(["лук", "зелье", "карта"])`.
**10.** `nums.count(2)` → 3.
**12.** `nums.sort()` затем `nums.sort(reverse=True)`.
**15.** `new = sorted(data)`; `data` не меняется.
**17.**
```python
tasks = []
while True:
    choice = input("[1] добавить [2] удалить [3] показать [4] выход: ")
    if choice == "1":
        tasks.append(input("Дело: "))
    elif choice == "2":
        n = int(input("Номер: "))
        if 1 <= n <= len(tasks):
            tasks.pop(n - 1)
    elif choice == "3":
        for i, t in enumerate(tasks, start=1):
            print(f"{i}. {t}")
    elif choice == "4":
        break
```
**19.** `while 2 in nums: nums.remove(2)`.
**20.** `lst[0], lst[-1] = lst[-1], lst[0]`.
**21.** `scores.append(x)`; `scores.sort(reverse=True)`; вывести `scores[0]`, `scores[1]`, `scores[2]`.

**22.** `sum(nums)`, `len(nums)`.
**24.** `round(sum(marks) / len(marks), 1)`.
**27.** `max(nums) - min(nums)`.
**30.** `players[scores.index(max(scores))]`.
**31.** `sorted(words, key=len)`.
**32.** `sorted(names, key=str.lower)`.
**33.** `new = sorted(data)`; `data` не меняется.
**36.** `strong = []`; `for s in scores: `→` if s >= 80: strong.append(s)`.
**37.** `evens = []`; `for n in nums: `→` if n % 2 == 0: evens.append(n)`.
**39.** `total = 0; best = nums[0]`; `for x in nums: total += x; `→` if x > best: best = x`.

**41.** `lst[:3]` и `lst[-3:]`.
**42.** `lst[1:-1]`.
**43.** `lst[2:5]` (индексы 2,3,4).
**44.** `copy = lst[:]`.
**46.** `lst[::2]`, `lst[::3]`.
**47.** `lst[::-1]`.
**49.** `if word == word[::-1]: print("палиндром")`.
**51.** `sorted(scores, reverse=True)[:3]`.
**52.** `a = lst` — то же имя (меняются вместе); `b = lst[:]` — копия (независима).
**53.** `lst[-4:][::-1]` — сначала хвост, потом разворот.
**54.** `del lst[2:5]`.

**56.** `point[0]`, `point[1]`, `len(point)`.
**58.** `x, y = point`.
**59.** `a, b = b, a`.
**60.** `(7,)` — кортеж; `(7)` — просто число.
**62.** `for x, y in points: print(f"x={x}, y={y}")`.
**63.** `q, r = divmod(23, 4)` → q=5, r=3.
**64.** `sum(tpl)`, `max(tpl)`.
**66.** `x, y = p`; `new = (y, x)`.
**69.** `for x, y in points: shifted.append((x + 2, y + 1))`.
**70.** запоминай точку с наибольшим `x*x + y*y` в цикле (как поиск максимума).

**72.** `ages["Боря"]`, `len(ages)`.
**74.** `book["Аня"] = "555-01"` (и так же второй).
**76.** `"Аня" in book` → `True/False`.
**77.** `book.get("Гена", "не найден")`.
**78.** `for name in ages: print(f"{name} — {ages[name]} лет")`.
**79.** `sum(prices.values())`; `list(prices.keys())`, `list(prices.values())`.
**80.** заведи `best_name`/`best_age`, обновляй в цикле по ключам.
**81.** `en_ru.get(word, "нет")`.
**82.** `counts = {}`; `for ch in word: counts[ch] = counts.get(ch, 0) + 1`.
**83.** счётчик `res[v] = res.get(v, 0) + 1`, затем поиск максимума по ключам.
**84.** `for k in bag2: total[k] = total.get(k, 0) + bag2[k]`.
**85.** `heroes["Маг"]["hp"]` — два шага доступа.

**86.** `len(set([1,2,2,3,3,3]))` → 3.
**88.** `a | b`, `a & b`.
**89.** `s = set(); s.add(...)` ×3.
**90.** `for k, v in d.items(): print(f"{k} — {v}")`.
**91.** `a & b` (общие), `a | b` (все).
**93.** `приглашённые - пришедшие`.
**94.** `len(set(слово))`.
**95.** счётчик `.get(v,0)+1`, потом максимум по `items()`.
**96.** `seen=set(); res=[]; for x: if x not in seen: seen.add(x); res.append(x)`.
**97.** `a ^ b`.
**98.** `heroes[1]["имя"]`, `len(heroes)`.
**99.** `for h in heroes: print(f"{h['имя']} — hp {h['hp']}")`.
**101.** `if g["цена"] < 100: дешёвые.append(g)`.
**103.** `найден=None; for h: if h["имя"]==name: найден=h`.
**104.** `c[h["класс"]] = c.get(h["класс"],0)+1`.
**105.** `пары.append((p["очки"], p["имя"])); пары.sort(reverse=True)`.
**106.** пары-кортежи + `.sort(reverse=True)` + `[:3]`.
**107.** `h={"имя":..., "вещи":[]}; h["вещи"].append("меч")`.

</details>
