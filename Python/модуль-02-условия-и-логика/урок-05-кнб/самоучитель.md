# Самоучитель урока 5 «Камень-ножницы-бумага» 🆘

> Пропустил урок или разбираешься сам? Всё с нуля: краткая тема → задание. Первые 5 — **с пошаговым разбором**, дальше — **с мини-подсказками**.
> Нужен редактор и файл `проба.py`. Это финал модуля — опирается на Уроки 1–4.

---

## 📖 Что нужно знать за 2 минуты

Игра = случайный ход компьютера + сравнение + лестница победителя.
```python
import random
comp = random.choice(["камень", "ножницы", "бумага"])
player = input("Выбор: ").lower().strip()

if player == comp:
    print("Ничья")
elif player == "камень" and comp == "ножницы":
    print("Ты выиграл")
# ... ещё 2 победы игрока ...
else:
    print("Компьютер выиграл")   # умный else
```
Порядок: **ничья → победы игрока → else**. По обе стороны `and` — полные сравнения.

---

## Часть A — задания с пошаговым разбором

### Задание 1. Случайный ход компьютера
**Тема:** `random.choice`.
**Задача:** вывести случайную фигуру.

**Разбор по шагам:**
1. Подключим библиотеку: `import random`.
2. Выберем случайно:
```python
import random
comp = random.choice(["камень", "ножницы", "бумага"])
print(comp)
```
3. Запусти пару раз — меняется.

✅ Компьютер «ходит» случайно.

---

### Задание 2. Ввод игрока
**Тема:** `input` + приведение.
**Задача:** спросить фигуру и убрать регистр/пробелы.

**Разбор по шагам:**
1. `player = input("Твой выбор: ").lower().strip()`.
2. Проверь: введи `Камень` — сравнение с `"камень"` теперь сработает.
```python
player = input("Твой выбор: ").lower().strip()
print("Ты выбрал:", player)
```

✅ Любой регистр приведён к нижнему.

---

### Задание 3. Ничья
**Тема:** первое условие.
**Задача:** если фигуры равны — «Ничья».

**Разбор по шагам:**
1. Сравним игрока и компьютер:
```python
import random
player = input("Выбор: ").lower().strip()
comp = random.choice(["камень", "ножницы", "бумага"])
if player == comp:
    print("Ничья!")
else:
    print("Фигуры разные — играем дальше")
```
2. Ничью проверяем **первой** — это упростит остальное.

✅ Одинаковые фигуры → ничья.

---

### Задание 4. Одна победа игрока
**Тема:** `and`.
**Задача:** камень бьёт ножницы.

**Разбор по шагам:**
1. Условие победы — два факта сразу (через `and`):
```python
if player == "камень" and comp == "ножницы":
    print("Ты выиграл! Камень тупит ножницы.")
```
2. По обе стороны `and` — полные сравнения (не `and "ножницы"`).

✅ Победа игрока — это `and` двух условий.

---

### Задание 5. Полная лестница победителя
**Тема:** ничья → 3 победы → else.
**Задача:** определить исход в любом случае.

**Разбор по шагам:**
1. Ничья первой веткой.
2. Три победы игрока через `elif ... and ...`.
3. Всё остальное — `else` (победа компьютера).
```python
if player == comp:
    print("Ничья!")
elif player == "камень" and comp == "ножницы":
    print("Ты выиграл!")
elif player == "ножницы" and comp == "бумага":
    print("Ты выиграл!")
elif player == "бумага" and comp == "камень":
    print("Ты выиграл!")
else:
    print("Компьютер выиграл!")
```
4. `else` накрывает три победы компьютера сразу — не пиши их отдельно.

✅ Готовая игра! Проверь три исхода (зафиксируй `comp` для отладки).

---

## Часть B — задания с мини-подсказками

### Задание 6. Монетка
Компьютер бросает `орёл`/`решка`.
> 💡 `random.choice(["орёл","решка"])`.

### Задание 7. Комментарии к победам
К каждой победе добавь пояснение.
> 💡 Свой текст в ветке.

### Задание 8. Защита ввода
Не фигура → «Не понял выбор» (первой веткой).
> 💡 Три `!=` через `and`.

### Задание 9. Победа через or
Три победы игрока — в одну ветку.
> 💡 `(a and b) or (c and d) or (e and f)`, скобки!

### Задание 10. Счёт раунда
Заведи `wins`/`losses`, прибавь по итогу.
> 💡 `+= 1` в нужных ветках.

### Задание 11. Чемпион раунда
Сравни копилки и объяви победителя.
> 💡 `if wins > losses:` …

### Задание 12. Чёт-нечет
Компьютер загадал 1–10, ты — «чёт»/«нечет».
> 💡 `randint` + `% 2`.

### Задание 13. Больше-меньше
Компьютер загадал 1–5, ты вводишь число, кто больше.
> 💡 `randint` + лестница.

### Задание 14. Угадай руку
Монетка в `левой`/`правой`, угадай.
> 💡 `random.choice` + сравнение.

### Задание 15. 🎓 Три раунда
Сыграй серию из 3, объяви чемпиона.
> 💡 Блок раунда 3 раза, копилки.

### Задание 16. 🎓 Счёт ничьих
Добавь копилку `draws`.
> 💡 `draws += 1` в ничьей.

### Задание 17. 🎓 Ящерица и Спок
Добавь 2 фигуры и их пары.
> 💡 Больше `elif ... and ...`.

---

## ✅ Проверь себя

Уверенно делаешь 6–14 — тему понял и модуль закрыл. 15–17 (🎓) — если хочешь вперёд.

Дальше: **[практика.md](практика.md)** (20 задач) и **[викторина.html](викторина.html)**. Шпаргалка — [итого.md](итого.md).

> ➡️ Готов к **Модулю 3 «Циклы и черепашка»** — там повторы (раунды, вопросы) сожмутся в пару строк.
