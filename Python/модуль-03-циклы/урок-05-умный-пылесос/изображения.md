# Что показать на проекторе — урок 5 «Умный пылесос» 🖼

> Три SVG-схемы. Открываются в браузере (`Ctrl+O` или перетащи файл). Показывай в момент из [преподавателю.md](преподавателю.md).

---

## 1. `изображения/сенсоры.svg` 📡
**Когда:** Часть 1 (команды + сенсоры), 5–15 мин.

Комната-сетка с пылесосом у стены: `wall_right = True` (красная стрелка), остальные сенсоры — False (зелёные). Справа — список сенсоров и код `while not wall_right: right(); clean()`.

**Как обсудить:**
- Сенсор = «можно ли ехать в эту сторону»: True — там стена, False — можно.
- Свяжи с `while`: «едем, пока справа НЕ стена».
- Упомяни `x, y` — координаты пылесоса (нужны для шахматки/ромба).

---

## 2. `изображения/for-или-while.svg` 🤔
**Когда:** Часть 2 (выбор цикла), 15–27 мин. **Ключевая схема урока.**

Две панели: **for** (длина известна — `range(9)`) и **while** (длина неизвестна — `while not wall_right`).

**Как обсудить:**
- Главный вопрос: «Я знаю, сколько шагов?» Да → `for`. Нет («до стены») → `while`.
- Покажи риск `for`: ошибся в числе — выехал за стену. `while` надёжнее для неизвестной ширины.
- Это стержень всего практикума.

---

## 3. `изображения/змейка.svg` 🐍
**Когда:** Часть 3 (змейка), 27–41 мин.

Маршрут «змейкой» по строкам (стрелки вправо-влево через ряд) + код: `for`(строки) + `while`(до стены) + флаг `going_right`.

**Как обсудить:**
- Строка вправо до стены → вверх → следующая влево → вверх…
- Флаг `going_right` переключает направление (`= not going_right`).
- Подчеркни: здесь встречаются ВСЕ циклы модуля — это вершина.

---

## 💡 Общие советы
- Схемы светлые, читаются с задних парт; масштаб `Ctrl` + `+`.
- `for-или-while.svg` держи открытой весь практикум — дети постоянно выбирают цикл.
- Лучший эффект — **живой запуск** заданий на проекторе (задание 5, потом 11-змейка).
