# Разминка перед уроком 6 (5 минут) 🔁

> Повторяем всё про циклы — сегодня собираем из них программы. Устно или в `проба.py`.

---

## 1. Блиц-вопросы (устно)

1. Что делает `while True` + `break`? *(повторять, пока не выйдем по break)*
2. Как перебрать буквы слова? *(`for letter in word`)*
3. Что делает `s += "a"`? *(добавляет "a" к строке s)*
4. Как взять случайный элемент списка? *(`random.choice(список)`)*
5. Что вернёт `"кот" + str(5)`? *(`"кот5"`)*

---

## 2. Предскажи вывод

```python
s = ""
for c in "abc":
    s += c + c
print(s)
```

<details><summary>Ответ</summary>

`aabbcc` — каждая буква добавляется дважды.
</details>

---

## 3. Собери в голове

Как проверить, есть ли буква `а` в слове `word`?
Как склеить слово и число в один ник?

<details><summary>Ответ</summary>

`if "а" in word:` — оператор `in`.
`word + str(число)` — число через `str()`.
</details>

---

## 4. Мостик к теме

Мы прошли все циклы. Сегодня — **финал модуля**: соберём из циклов три настоящие программы — игру «Виселица», генератор паролей/ников и ASCII-арт. Покажем, на что способны циклы! 🏆
