# Кэш импорта Godot — не коммитим (создаётся заново при открытии проекта)
/.godot/

# Собранные билды (.exe и т.п.)
/build/

# 3D-модели: пока не выкладываем в общий доступ (лицензии/вес).
# Убери эти строки, когда оформишь models/ЛИЦЕНЗИИ.md и решишь публиковать.
/models/*.glb
/models/*.import
