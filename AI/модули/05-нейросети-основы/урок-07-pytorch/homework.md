# Урок 5.7. Самостоятельная работа на уроке

> **Как всегда:** в подсказках **мало готового кода**. Я даю **направление** — думать твоя задача.
>
> После каждого задания — **🤔 вопросы на размышление**. Записывай ответы в Markdown-ячейку.
>
> Тетрадка: продолжай `lesson-5-7-pytorch.ipynb`. Заголовок `## Самостоятельная работа`.

> **Главное правило урока:** PyTorch = наш код, но `loss.backward()` считает градиенты сам. Цикл: zero_grad → forward → loss → backward → step.

---

## Шпаргалка (из практики)

```python
import torch, torch.nn as nn

class Net(nn.Module):
    def __init__(self):
        super().__init__()
        self.fc1 = nn.Linear(n_in, n_hidden)
        self.fc2 = nn.Linear(n_hidden, 1)
    def forward(self, x):
        x = torch.relu(self.fc1(x))
        return torch.sigmoid(self.fc2(x))

net = Net()
loss_fn = nn.BCELoss()
opt = torch.optim.Adam(net.parameters(), lr=0.01)
for epoch in range(N):
    opt.zero_grad(); out = net(X); loss = loss_fn(out, y)
    loss.backward(); opt.step()
```

> Данные → тензоры: `torch.tensor(arr, dtype=torch.float32)`. Не забудь стандартизацию!

---

## Уровень 1. Понять PyTorch

### 1.1. Что чему соответствует (без кода!)

> **Задача:** связать PyTorch с нашим кодом из 5.6. **Ответы текстом/таблицей.**

Заполни таблицу «наш код ↔ PyTorch»:

| Наш код (5.6) | PyTorch |
|---------------|---------|
| `self.W1 = np.random.randn(30,16)` | ? |
| `X @ self.W1 + self.b1` | ? |
| наш ручной `backward` (dW1, dW2) | ? |
| `self.W1 -= lr*dW1` | ? |
| наша функция `sigmoid` | ? |

**🤔 Вопросы на размышление:**

1. Какую **самую трудную** часть нашего кода (5.5–5.6) PyTorch делает за нас? Одной строкой?
2. Что такое `requires_grad=True` и зачем оно?
3. Почему говорят, что «мы понимаем PyTorch лучше, чем те, кто начал сразу с него»?

---

### 1.2. Autograd руками-глазами (без кода или с проверкой)

> **Задача:** предсказать, что выдаст autograd. **Ответы текстом, потом проверь.**

Для каждой функции скажи, чему будет равен `x.grad` после `.backward()`:
1. `y = x**2`, при `x=4` → grad = ? (производная `2x`)
2. `y = 3*x`, при `x=10` → grad = ?
3. `y = x**2 + x`, при `x=2` → grad = ?

> **Подсказка:** производная — наклон функции (Урок 1.7). `(x²)' = 2x`, `(3x)' = 3`, `(x²+x)' = 2x+1`. Потом проверь в PyTorch.

**🤔 Вопросы:**

1. Совпали ли твои предсказания с PyTorch?
2. Чем autograd удобнее **численного** градиента из Урока 1.7? (точность, скорость)
3. Как autograd связан с нашим **backprop** из 5.5? (что он делает внутри)

---

## Уровень 2. Свои эксперименты

### 2.1. Поменяй архитектуру сети

> **Задача:** поэкспериментировать с PyTorch-сетью на раке груди.

**Что нужно сделать:**
1. Возьми сеть из практики.
2. Попробуй разные конфигурации:
   - больше нейронов в скрытом слое (`nn.Linear(30, 64)`);
   - **два** скрытых слоя (добавь `self.fc_mid = nn.Linear(16, 16)`);
3. Сравни точность.

> **Подсказка:** для второго скрытого слоя добавь его в `__init__` и проведи через него в `forward` (с `torch.relu`). PyTorch сам разберётся с градиентами!

**🤔 Вопросы:**

1. Помогло ли больше нейронов / больше слоёв? На сколько?
2. Заметил ли, что добавить слой в PyTorch — **легко** (не надо писать новый backward)? Сравни с тем, как это было бы в нашем NumPy-классе.
3. Где предел — когда добавление слоёв перестаёт помогать или вредит? (вспомни переобучение)

---

### 2.2. Сравни оптимизаторы SGD и Adam

> **Задача:** увидеть, почему Adam лучше простого градиентного спуска.

**Что нужно сделать:**
1. Обучи сеть с `torch.optim.SGD(net.parameters(), lr=0.01)`.
2. Обучи с `torch.optim.Adam(net.parameters(), lr=0.01)`.
3. Сравни **кривые потерь** (на одном графике) и финальную точность.

> **Подсказка:** SGD — это наш простой `вес = вес − lr·градиент` из 5.4. Adam — «умная» версия. Создавай сеть заново перед каждым обучением (одинаковый старт через `torch.manual_seed`).

**🤔 Вопросы:**

1. Какой оптимизатор учился **быстрее** (потеря падала круче)? Какой дал выше точность?
2. SGD — это в точности наш цикл из Урока 5.4. Чем Adam «умнее»?
3. Почему sklearn MLP был лучше нашей сети в 5.6? Связано ли это с оптимизатором?

---

## Уровень 3. Со звёздочкой

### 3.1. Перепиши XOR на PyTorch

> **Задача:** решить XOR (наш старый враг) средствами PyTorch.

**Что нужно сделать:**
1. Данные XOR в тензоры: `X = torch.tensor([[0,0],[0,1],[1,0],[1,1]], dtype=torch.float32)`, `y` столбцом.
2. Сеть 2 → 4 → 1 через `nn.Module`.
3. Обучи (Adam, ~2000 эпох), проверь предсказания.

> **Подсказка:** та же структура, что для рака, только `nn.Linear(2, 4)` и `nn.Linear(4, 1)`. Сравни, насколько это **короче** нашего ручного backprop из 5.5!

**🤔 Вопросы:**

1. Решила ли PyTorch-сеть XOR?
2. Сравни этот код с нашим ручным backprop XOR из Урока 5.5 — что проще?
3. Стало ли тебе понятнее, **что** PyTorch делает внутри, потому что ты писал это руками?

---

### 3.2. Загляни в веса PyTorch-сети

> **Задача:** убедиться, что внутри PyTorch — те же веса, что мы хранили в `self.W1`.

**Что нужно сделать:**
1. У обученной сети посмотри веса: `net.fc1.weight.shape`, `net.fc2.weight.shape`.
2. Посчитай **общее число параметров** сети (веса + смещения всех слоёв).
3. Сравни с нашей NumPy-сетью (там были `W1, b1, W2, b2`).

> **Подсказка:** `net.fc1.weight` — матрица весов первого слоя (как наш `W1`). `sum(p.numel() for p in net.parameters())` — общее число параметров.

**🤔 Вопросы:**

1. Какой формы веса `fc1`? Совпадает ли с нашим `W1` (30×16)? (учти: PyTorch хранит транспонированно)
2. Сколько всего параметров в сети? Это много или мало?
3. У ChatGPT ~**миллиарды** параметров. Как думаешь, во сколько раз больше нашей сети?

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Понимает, что такое тензор и autograd | |
| Понимает: `loss.backward()` = наш backprop сам | |
| Собрал сеть через `nn.Module`, обучил | |
| Видит соответствие «наш код ↔ PyTorch» | |
| **Уровень 2:** эксперименты с архитектурой/оптимизатором | |
| **Ответил на 🤔 вопросы размышления** | |

> **Минимум для «зачёта»:** Уровень 1 (обе задачи) + одна задача Уровня 2 + **ответы на вопросы**.
>
> **Без ответов на вопросы — задача не считается решённой.**
> **Главное — понять, что PyTorch автоматизирует то, что мы делали руками (особенно backward).**

---

> **Поздравляю — ты освоил PyTorch, инструмент настоящих ИИ-инженеров!** И у тебя огромное преимущество: ты понимаешь, что у него внутри, потому что построил всё сам. На следующем уроке — **финал Модуля 5**: научим PyTorch-сеть **распознавать рукописные цифры** (MNIST). Это «Hello World» нейросетей и твоя первая «настоящая» задача компьютерного зрения! 🔢🔥
