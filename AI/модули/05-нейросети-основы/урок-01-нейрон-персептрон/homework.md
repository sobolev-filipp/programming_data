# Урок 5.1. Самостоятельная работа на уроке

> **Как всегда:** в подсказках **мало готового кода**. Я даю **направление** — думать твоя задача.
>
> После каждого задания — **🤔 вопросы на размышление**. Записывай ответы в Markdown-ячейку.
>
> Тетрадка: продолжай `lesson-5-1-perceptron.ipynb`. Заголовок `## Самостоятельная работа`.

> **Главное правило урока:** нейрон = `w·x + b` + активация. Один нейрон разделяет данные **прямой** — и только линейно разделимое.

---

## Шпаргалка (из практики)

```python
# Предсказание нейрона
def predict(X, w, b):
    z = X @ w + b
    return (z > 0).astype(int)        # ступенька

# Обучение перцептрона (правило обновления)
# при ошибке: w += lr*(y_true - pred)*x ;  b += lr*(y_true - pred)
```

Таблицы истинности:
```
AND: [0,0,0,1]    OR: [0,1,1,1]    XOR: [0,1,1,0]
NAND:[1,1,1,0]    NOR:[1,0,0,0]
```

---

## Уровень 1. Понять нейрон

### 1.1. Нейрон руками (без кода!)

> **Задача:** посчитать нейрон на бумаге. **Ответы текстом/в тетради.**

Дан нейрон с весами `w = [2, 3]`, смещением `b = -4`, **ступенчатой** активацией (1 если `z > 0`, иначе 0).

**Посчитай выход нейрона** для входов:
1. `x = [1, 1]` → z = ? → выход = ?
2. `x = [1, 0]` → z = ? → выход = ?
3. `x = [0, 1]` → z = ? → выход = ?
4. `x = [0, 0]` → z = ? → выход = ?

> **Подсказка:** `z = w₁·x₁ + w₂·x₂ + b = 2·x₁ + 3·x₂ − 4`. Потом сравни с 0.

**🤔 Вопросы на размышление:**

1. Какую **логическую функцию** реализует этот нейрон? (посмотри на 4 выхода — это AND, OR или что-то ещё?)
2. Что произойдёт с выходами, если **увеличить** смещение `b` с −4 до −1? Нейрон станет «срабатывать» чаще или реже?
3. Какую роль играет смещение `b` — как оно связано с «порогом срабатывания»?

---

### 1.2. Связь с прошлым (без кода!)

> **Задача:** соединить нейрон с тем, что уже знаешь. **Ответы текстом.**

1. Запиши формулу взвешенной суммы нейрона. На какую формулу из Урока 3.3 она похожа?
2. Если у нейрона активация — **сигмоида** (а не ступенька), какой моделью из Модуля 3 он становится?
3. Перечисли соответствия: дендриты → ?, сила связей → ?, порог → ? (биологический ↔ искусственный нейрон).

**🤔 Вопросы:**

1. Почему говорят, что «ты уже умел строить нейроны, просто не знал этого»?
2. Зачем вообще нужна **функция активации** — что было бы, если бы нейрон выдавал просто `w·x + b` без неё?

---

## Уровень 2. Свой перцептрон

### 2.1. Обучи перцептрон функции NAND

> **Задача:** взять код перцептрона из практики и обучить **новую** логическую функцию.

**Что нужно сделать:**
1. Возьми `train_perceptron` и `predict` из практики.
2. Обучи перцептрон функции **NAND** (это «НЕ-И»): `[1, 1, 1, 0]` — выход 0, только когда оба входа 1.
3. Проверь, что предсказания совпали.
4. За сколько эпох выучил?

> **Подсказка:** меняется только `y` (целевые значения). Сам код перцептрона не трогай.

**🤔 Вопросы:**

1. Выучил ли перцептрон NAND? За сколько эпох?
2. NAND линейно разделим? Как ты это понял (по тому, выучился перцептрон или нет)?
3. Сравни число эпох для AND, OR, NAND. От чего, по-твоему, зависит «скорость» обучения?

---

### 2.2. Эксперимент со скоростью обучения

> **Задача:** понять роль `lr` (скорости обучения) — как в Уроке 1.7.

**Что нужно сделать:**
1. Обучи перцептрон функции AND с разными `lr`: `0.01, 0.1, 0.5, 1.0`.
2. Для каждого запиши, **за сколько эпох** выучил (или не выучил).
3. Сделай вывод о влиянии `lr`.

> **Подсказка:** передавай `lr` в `train_perceptron(X, y, lr=...)`. Чтобы видеть число эпох — пусть функция его возвращает/печатает.

**🤔 Вопросы:**

1. При каком `lr` перцептрон выучил **быстрее**? А медленнее?
2. Помнишь Урок 1.7 — там тоже была `lr`. Что общего у роли `lr` там и здесь?
3. Слишком большой `lr` — это всегда хорошо? (подумай, что было с расходящимся градиентным спуском в 1.7)

---

## Уровень 3. Со звёздочкой

### 3.1. Перцептрон на «настоящих» данных

> **Задача:** проверить перцептрон на линейно разделимом датасете (не игрушечном).

**Что нужно сделать:**
1. Сгенерируй 2 хорошо разделённых облака точек:
   ```python
   from sklearn.datasets import make_blobs
   X, y = make_blobs(n_samples=100, centers=2, random_state=42, cluster_std=1.5)
   ```
2. Обучи свой перцептрон (возможно, понадобится больше эпох).
3. Нарисуй точки и границу решения нейрона.
4. Посчитай долю верных предсказаний.

> **Подсказка:** на реальных данных перцептрон может не дойти до 0 ошибок (если облака чуть перекрываются) — это нормально. Смотри на долю верных. Граница рисуется как в практике 2.2.

**🤔 Вопросы:**

1. Справился ли перцептрон? Какая доля верных предсказаний?
2. Если облака **перекрываются**, перцептрон достигнет 100%? Почему?
3. Чем эта задача похожа на логистическую регрессию из Урока 3.4?

---

### 3.2. Докажи, что XOR требует слоёв (мысленный эксперимент)

> **Задача:** на бумаге показать, как **два** нейрона решают XOR. **В основном текст/рисунок.**

**Что нужно сделать (рассуждение в Markdown):**
1. Нарисуй 4 точки XOR на плоскости.
2. Идея: XOR = `(x₁ OR x₂) AND NOT(x₁ AND x₂)`. То есть XOR можно собрать из **OR, AND, NAND** — а их перцептрон умеет!
3. Опиши словами: первый «слой» из 2 нейронов считает OR и NAND, второй нейрон считает их AND.
4. (Со звёздочкой) попробуй реализовать это в коде: 2 нейрона → их выходы → 3-й нейрон.

> **Подсказка:** это и есть идея многослойной сети! Не страшно, если код не выйдет — главное **понять идею**: то, что не решает один нейрон, решают нейроны **в слоях**.

**🤔 Вопросы:**

1. Почему **комбинация** простых нейронов (OR, NAND, AND) решает то, что один не может?
2. Как это связано с тем, что будет в Уроке 5.3 (многослойная сеть)?
3. Можешь привести **житейскую** аналогию: задача, которую один человек не решит, а несколько вместе — да?

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Понимает нейрон = `w·x + b` + активация | |
| Умеет посчитать выход нейрона руками | |
| Понимает связь с линейной/логистической регрессией | |
| **Уровень 2:** обучил свой перцептрон, поиграл с lr | |
| Понимает **проблему XOR** (линейная разделимость) | |
| **Ответил на 🤔 вопросы размышления** | |

> **Минимум для «зачёта»:** Уровень 1 (обе задачи) + одна задача Уровня 2 + **ответы на вопросы**.
>
> **Без ответов на вопросы — задача не считается решённой.**
> **Главное — понять, что нейрон это знакомая формула + активация, и почему одного нейрона мало.**

---

> **Поздравляю — ты обучил свой первый нейрон!** Это фундамент всего, что будет дальше. А проблему XOR запомни: именно из-за неё нейроны **соединяют в слои**. На следующем уроке разберём **функции активации** — то, что даёт нейронам гибкость. 🧠
