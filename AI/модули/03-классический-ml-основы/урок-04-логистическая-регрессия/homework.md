# Урок 3.4. Самостоятельная работа на уроке

> **Как и в прошлый раз:** в подсказках **мало готового кода**. Я даю **направление** — а думать и писать код твоя задача. Так ты реально научишься, а не просто скопируешь.
>
> После каждого задания — **🤔 вопросы на размышление**. Записывай ответы в Markdown-ячейку рядом.
>
> Тетрадка: продолжай `lesson-3-4-logreg.ipynb`. Сделай заголовок `## Самостоятельная работа`.

> **Главное правило урока:** для классификации **accuracy — не главная метрика**. Всегда смотри **precision и recall** и думай, **что важнее для задачи**.

---

## Шпаргалка (минимум команд)

```python
from sklearn.linear_model import LogisticRegression
from sklearn.metrics import (accuracy_score, precision_score, recall_score,
                             f1_score, confusion_matrix, classification_report)

# Модель
model = LogisticRegression(max_iter=1000)
model.fit(X_train, y_train)
preds = model.predict(X_test)          # классы 0/1
probs = model.predict_proba(X_test)    # вероятности [P(0), P(1)]

# Метрики
accuracy_score(y_test, preds)
precision_score(y_test, preds)
recall_score(y_test, preds)
f1_score(y_test, preds)
classification_report(y_test, preds)

# Матрица ошибок
confusion_matrix(y_test, preds)        # [[TN, FP], [FN, TP]]
```

Напоминание формул (на случай, если будешь считать руками):
- `precision = TP / (TP + FP)` — верить ли «да».
- `recall = TP / (TP + FN)` — скольких «да» нашли.

---

## Уровень 1. Понять метрики

### 1.1. Confusion matrix руками (без кода!)

> **Задача:** научиться читать матрицу ошибок **до** того, как доверять компьютеру.

**Дано.** Спам-фильтр проверил 200 писем. Спам — это «положительный класс (1)». Результат:

| | Модель: «спам» | Модель: «не спам» |
|---|---|---|
| **Реально спам** | 35 | 15 |
| **Реально не спам** | 5 | 145 |

**Что нужно сделать (на бумаге или в Markdown, без Python):**
1. Выпиши **TP, FP, FN, TN** из таблицы.
2. Посчитай **accuracy** = (TP + TN) / всё.
3. Посчитай **precision** = TP / (TP + FP).
4. Посчитай **recall** = TP / (TP + FN).
5. Словами опиши, что означает **FP = 5** и **FN = 15** *для пользователя почты*.

> **Подсказка:** не путай строки и столбцы. Строка — что было **на самом деле**, столбец — что **сказала модель**. FP — модель сказала «спам», а письмо нормальное.

**🤔 Вопросы на размышление:**

1. Что для пользователя почты **хуже** — FP (важное письмо улетело в спам) или FN (один спам остался во «Входящих»)? Почему?
2. Исходя из ответа на вопрос 1 — какая метрика для спам-фильтра **важнее**: precision или recall?
3. Сначала **предскажи**, потом посчитай: precision будет больше или меньше recall в этих данных? Совпало?

---

### 1.2. «Обманчивая» accuracy

> **Задача:** на своём примере убедиться, что **высокая accuracy ≠ хорошая модель**.

**Сценарий.** Аэропорт проверяет багаж на опасные предметы. Из **10 000** сумок опасны только **20** (0.2%). «Ленивая» сигнализация **всегда** говорит «безопасно».

**Что нужно сделать (в Markdown, можно с маленькими вычислениями):**
1. Построй для этой «ленивой» модели confusion matrix (TP, FP, FN, TN).
2. Посчитай её **accuracy**.
3. Посчитай её **recall** для опасных сумок.
4. Сделай вывод: можно ли по одной accuracy доверять модели?

> **Подсказка:** «всегда безопасно» значит, что модель **никогда** не ставит класс «1». Сколько тогда TP? А чему равен recall, если TP = 0?

**🤔 Вопросы:**

1. Accuracy получилась очень высокой (>99%). Почему эта модель всё равно **катастрофически плоха**?
2. Какая метрика **сразу** разоблачает эту модель — accuracy, precision или recall?
3. Назови **3 примера из жизни**, где один класс очень редкий и accuracy будет обманывать (кроме багажа).

---

## Уровень 2. Своя модель классификации

### 2.1. Titanic с другими признаками

> **Задача:** обучить логистическую регрессию на Titanic, но **добавить новые признаки**, и проверить, станет ли лучше.

**Что нужно сделать:**
1. Загрузи Titanic (URL из практики).
2. Возьми признаки из практики (`Pclass, Sex, Age, Fare`) **плюс добавь** `SibSp` (число братьев/сестёр/супругов на борту) и `Parch` (число родителей/детей).
3. Не забудь: `Sex` → число, пропуски в `Age` → медиана.
4. Train/test split с `random_state=42, stratify=y`.
5. Обучи `LogisticRegression(max_iter=1000)`.
6. Выведи **accuracy, precision, recall, F1** и `classification_report`.
7. Сравни с результатом из практики (там было acc ≈ 0.80 на 4 признаках).

> **Подсказка:** после `df = pd.read_csv(url)` собери нужные столбцы списком — так удобнее их потом превращать. Помни про `.copy()`, чтобы pandas не ругался.

**🤔 Вопросы:**

1. Accuracy выросла, упала или почти не изменилась после добавления `SibSp` и `Parch`? Как думаешь, почему?
2. Сначала **предскажи**: новые признаки (родственники на борту) должны помогать предсказывать выживание? Проверь по результату — совпало с интуицией?
3. Посмотри на `model.coef_`. У `SibSp`/`Parch` вес **положительный** или **отрицательный**? Что это говорит: с семьёй выживали чаще или реже?

---

### 2.2. Найди, кого модель путает

> **Задача:** не просто получить метрики, а **разобраться**, на ком модель ошибается.

**Что нужно сделать:**
1. Возьми обученную модель из 2.1 и её предсказания на тесте.
2. Найди пассажиров, где `predictions != y_test` (модель ошиблась).
3. Отдельно посмотри на **FN** — кого модель назвала «погиб», а он **выжил**.
4. Выведи их признаки (`X_test` этих строк). Что у них общего?

> **Подсказка:** маску ошибок можно сделать сравнением двух массивов. Чтобы найти именно FN, нужно условие: «модель сказала 0, а правда 1». Объедини два условия через `&` (и не забудь скобки). Аккуратно с индексами — `y_test` это Series, удобно сравнивать через `.values` или `.reset_index(drop=True)`.

**🤔 Вопросы:**

1. Что общего у пассажиров, которых модель **пропустила** (FN — выжили, но модель сказала «погиб»)? Какой у них класс, пол, возраст?
2. Почему, по-твоему, модель ошиблась именно на них? Чего ей «не хватило»?
3. Какой **новый признак** можно было бы добавить, чтобы помочь модели с этими случаями?

---

## Уровень 3. Со звёздочкой

### 3.1. Кривая «порог → метрики»

> **Задача:** построить **график**, показывающий, как precision и recall меняются с порогом. Это «визитная карточка» дата-сайентиста.

**Что нужно сделать:**
1. Возьми вероятности выживания: `model.predict_proba(X_test)[:, 1]`.
2. Перебери пороги от 0.1 до 0.9 (например, `np.arange(0.1, 0.95, 0.05)`).
3. Для **каждого** порога посчитай precision и recall (как в практике 3.2).
4. Построй **график**: по оси X — порог, две линии — precision и recall.
5. Найди порог, при котором precision ≈ recall (линии пересекаются).

> **Подсказка:** заведи два пустых списка `precisions = []` и `recalls = []`, в цикле добавляй туда значения через `.append(...)`, потом построй `plt.plot(thresholds, precisions)` и `plt.plot(thresholds, recalls)`. Не забудь `plt.legend()`, чтобы различать линии.

**🤔 Вопросы:**

1. В какую сторону идёт линия **precision** при росте порога, а в какую — **recall**? Объясни **почему** (вспомни: высокий порог = модель осторожнее говорит «да»).
2. При каком пороге линии пересеклись? Что означает эта точка?
3. Если бы это была модель для **диагностики болезни**, какой порог с графика ты бы выбрал и почему? А для **спам-фильтра**?

---

### 3.2. Сравни две модели по F1

> **Задача:** показать, **зачем нужна F1** — когда precision и recall у моделей разные, а выбрать надо одну.

**Что нужно сделать:**
1. На данных Titanic обучи **две** модели:
   - `LogisticRegression(max_iter=1000)`
   - `KNeighborsClassifier(n_neighbors=5)` (импортируй из `sklearn.neighbors`)
2. Для каждой посчитай **precision, recall и F1**.
3. Собери результат в таблицу (Markdown).
4. Реши: **какая модель лучше** и по какой метрике ты это решаешь.

> **Подсказка:** KNN считает расстояния — вспомни Урок 3.3, что для него **важна стандартизация**! Попробуй обучить KNN и со `StandardScaler`, и без — сравни. Это объяснит часть разницы.

**🤔 Вопросы:**

1. У какой модели выше precision? У какой recall? Можно ли по этим двум числам сразу сказать, какая «лучше»?
2. Почему **F1** помогает выбрать, когда precision и recall «спорят»? (Вспомни пример из теории с моделью B: precision 1.0, recall 0.1.)
3. Помогла ли KNN стандартизация? На сколько изменился F1? Почему линейной регрессии она бы так не помогла?

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Правильно читает confusion matrix (TP/FP/FN/TN) | |
| Понимает, что **accuracy обманывает** на редком классе | |
| Различает, когда важна **precision**, а когда **recall** | |
| **Уровень 2:** обучил свою модель, посчитал все метрики | |
| **Ответил на 🤔 вопросы размышления** | |
| **Уровень 3:** построил кривую порога ИЛИ сравнил по F1 | |

> **Минимум для «зачёта»:** Уровень 1 (обе задачи) + одна задача Уровня 2 + **ответы на вопросы**.
>
> **Без ответов на вопросы — задача не считается решённой.**
> **Главное — понять, что метрика выбирается под задачу, а не прогнать код.**

---

> **Не пугайся,** если запутался в TP/FP/FN/TN — это путает даже взрослых инженеров. Возвращайся к табличке из теории и проговаривай: «первая буква — угадала ли, вторая — что сказала». Если не успел Уровень 3 — ничего страшного, мы к порогам ещё вернёмся в следующих модулях.
