# Урок 3.7. Самостоятельная работа на уроке

> **Как всегда:** в подсказках **мало готового кода**. Я даю **направление** — думать и писать код твоя задача.
>
> После каждого задания — **🤔 вопросы на размышление**. Записывай ответы в Markdown-ячейку.
>
> Тетрадка: продолжай `lesson-3-7-forest.ipynb`. Заголовок `## Самостоятельная работа`.

> **Главное правило урока:** оценивай модель **по кросс-валидации**, а не по одному «счастливому» split.

---

## Шпаргалка (минимум команд)

```python
from sklearn.ensemble import RandomForestClassifier, GradientBoostingClassifier
from sklearn.model_selection import cross_val_score

# Случайный лес
forest = RandomForestClassifier(n_estimators=100, random_state=42)
forest.fit(X_train, y_train)
forest.score(X_test, y_test)
forest.feature_importances_

# Кросс-валидация (5 фолдов)
scores = cross_val_score(forest, X, y, cv=5)
scores.mean()    # средняя точность
scores.std()     # разброс (стабильность)
```

> Напоминание: лесу и дереву **НЕ нужна** стандартизация. KNN и логрегрессии — нужна.

---

## Уровень 1. Понять ансамбль

### 1.1. «Мудрость толпы» руками (без кода!)

> **Задача:** на бумаге убедиться, что усреднение работает.

**Дано.** Пять «деревьев» оценивают, сколько лет человеку (правильный ответ — **40 лет**):

| Дерево | Ответ |
|--------|-------|
| 1 | 35 |
| 2 | 50 |
| 3 | 38 |
| 4 | 45 |
| 5 | 37 |

**Что нужно сделать (в Markdown, без Python):**
1. Посчитай **ошибку каждого** дерева (`|ответ − 40|`).
2. Посчитай **среднее** всех пяти ответов.
3. Посчитай **ошибку среднего** (ансамбля).
4. Сравни: ошибка ансамбля больше или меньше, чем у **типичного** отдельного дерева?

> **Подсказка:** среднее = (35+50+38+45+37)/5. Ошибка ансамбля — это `|среднее − 40|`.

**🤔 Вопросы на размышление:**

1. Почему ошибка среднего получилась **меньше**, чем у большинства отдельных деревьев?
2. Что было бы, если бы **все пять** деревьев дали одинаковый ответ (например, все 50)? Помогло бы усреднение? (это про важность **разнообразия**)
3. Назови **2 примера из жизни**, где «среднее мнение многих» точнее мнения одного.

---

### 1.2. Bagging и лес — на словах

> **Задача:** объяснить устройство случайного леса. **Кода нет — только текст в Markdown.**

**Что нужно сделать.** Ответь на вопросы (2–4 предложения каждый):

1. Что такое **bagging**? Как из одного набора данных получаются **разные** деревья?
2. Случайный лес делает деревья разными **двумя** способами. Какими? (вспомни раздел 3 теории)
3. Почему важно, чтобы деревья были **разными**? Что будет, если все деревья одинаковые?

**🤔 Вопросы:**

1. Почему случайный лес **меньше переобучается**, чем одно дерево, хотя состоит из таких же (даже глубоких) деревьев?
2. Чем **бустинг** отличается от **bagging** по способу построения деревьев? (одна фраза)

---

## Уровень 2. Своя модель

### 2.1. Случайный лес на ирисах + кросс-валидация

> **Задача:** обучить лес на Iris (Урок 3.5) и честно оценить его кросс-валидацией.

**Что нужно сделать:**
1. Загрузи Iris (URL из Урока 3.5). X — 4 признака, y — `species`.
2. Обучи `RandomForestClassifier(n_estimators=100, random_state=42)` через train/test split, посмотри test accuracy.
3. Оцени **ту же модель** через `cross_val_score(..., cv=5)` на **всех** данных.
4. Сравни: одиночный test и средняя CV — близки?
5. Посмотри `feature_importances_`.

> **Подсказка:** для кросс-валидации НЕ нужно делать split — передавай в `cross_val_score` целые `X` и `y`, функция сама всё разделит 5 раз.

**🤔 Вопросы:**

1. Какая точность по кросс-валидации? На ирисах она очень высокая — почему этот датасет «лёгкий»?
2. Совпала ли важность признаков с тем, что показало **дерево** в Уроке 3.6 (там лидировали `petal_*`)?
3. Сравни лес с KNN (Урок 3.5, там было ~0.97) — кто лучше на ирисах? Большая ли разница?

---

### 2.2. Подбор n_estimators по кросс-валидации

> **Задача:** найти, сколько деревьев реально нужно.

**Что нужно сделать:**
1. Возьми Titanic (подготовка как в практике).
2. Перебери `n_estimators` из `[1, 5, 10, 25, 50, 100, 200, 500]`.
3. Для каждого посчитай **среднюю CV-точность** и **std** (`cross_val_score`, `cv=5`).
4. Построй график: по X — число деревьев, по Y — средняя CV-точность.
5. Найди, после какого числа деревьев точность выходит **на плато**.

> **Подсказка:** структура — как подбор k (Урок 3.5) или глубины (Урок 3.6), но метрика — `cross_val_score(...).mean()`. Можно добавить «усы» разброса через `plt.errorbar(...)`, но это не обязательно.

**🤔 Вопросы:**

1. После какого `n_estimators` точность почти перестаёт расти? Есть ли смысл в 500 деревьях?
2. Что происходит с **разбросом (std)** при росте числа деревьев? Почему?
3. **Сначала предскажи, потом проверь:** `n_estimators=1` даст результат лучше или хуже одного `DecisionTree`? (подумай: лес из 1 дерева — это что?)

---

## Уровень 3. Со звёздочкой

### 3.1. Лес против бустинга

> **Задача:** сравнить bagging (лес) и boosting (градиентный бустинг) на одних данных.

**Что нужно сделать:**
1. Возьми Titanic.
2. Обучи и сравни по кросс-валидации (`cv=5`) три модели:
   - `RandomForestClassifier(n_estimators=100, random_state=42)`
   - `GradientBoostingClassifier(n_estimators=100, random_state=42)`
   - (если установлен) `XGBClassifier` из `xgboost` — **необязательно**.
3. Собери результаты в таблицу (средняя CV + std).

> **Подсказка:** `GradientBoostingClassifier` импортируется из `sklearn.ensemble` — установка не нужна. XGBoost — отдельная библиотека (`pip install xgboost`), если её нет, просто пропусти.

**🤔 Вопросы:**

1. Кто оказался точнее — лес или бустинг? Велика ли разница на Titanic?
2. В чём принципиальное **различие** в том, как эти два метода строят деревья? (вспомни раздел 4: параллельно vs по очереди)
3. Бустинг часто называют «королём табличных данных». Почему же мы **по умолчанию** часто берём именно лес, а не бустинг? (подумай про простоту и риск переобучения)

---

### 3.2. Honest pipeline (по-взрослому)

> **Задача:** сравнить «наивную» и «честную» кросс-валидацию для KNN со стандартизацией. Это тонкий, но важный момент.

**Что нужно сделать:**
1. Возьми Titanic. Цель — оценить **KNN со стандартизацией** по кросс-валидации.
2. **Наивный способ:** стандартизируй **все** X через `StandardScaler` заранее, потом `cross_val_score(knn, X_scaled, y, cv=5)`.
3. **Честный способ:** собери `Pipeline` из `StandardScaler` + `KNeighborsClassifier`, и `cross_val_score(pipeline, X, y, cv=5)`.
4. Сравни средние оценки.

> **Подсказка:**
> ```python
> from sklearn.pipeline import make_pipeline
> pipe = make_pipeline(StandardScaler(), KNeighborsClassifier(n_neighbors=7))
> ```
> В Pipeline стандартизация происходит **внутри каждого фолда** только на его train-части — без «подглядывания» в test. Это правильно.

**🤔 Вопросы:**

1. Сильно ли отличаются оценки «наивного» и «честного» способов? (на Titanic разница обычно мала, но она есть)
2. Почему «честный» способ **правильнее**? В чём «подглядывание» наивного способа? (вспомни правило `fit` только на train из Урока 3.3)
3. Где ещё в ML важно «не давать модели подсматривать в тест»?

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Понимает идею ансамбля («мудрость толпы») | |
| Знает, почему лес лучше одного дерева | |
| Умеет применять **`cross_val_score`** | |
| Понимает, почему один split ненадёжен | |
| **Уровень 2:** обучил лес, подобрал n_estimators | |
| **Ответил на 🤔 вопросы размышления** | |

> **Минимум для «зачёта»:** Уровень 1 (обе задачи) + одна задача Уровня 2 + **ответы на вопросы**.
>
> **Без ответов на вопросы — задача не считается решённой.**
> **Главное — понять, ПОЧЕМУ ансамбль сильнее одиночки и зачем нужна кросс-валидация.**

---

> **Не пугайся** Pipeline (3.2) — это задание «на вырост», подробно разберём в Модуле 4. А вот привычку **оценивать по `cross_val_score`** выработай уже сейчас: на следующем уроке — **контрольная по всему Модулю 3**, и там это пригодится!
