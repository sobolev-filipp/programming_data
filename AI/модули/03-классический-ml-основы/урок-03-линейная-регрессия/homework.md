# Урок 3.3. Самостоятельная работа на уроке

> **Сегодня задания построены немного иначе:** в подсказках **меньше готового кода**. Я буду давать **направление**, а **думать и кодировать** — твоя задача.
>
> После каждого задания — **вопросы на размышление**. Запиши ответы в Markdown-ячейке.
>
> Тетрадка: продолжай `lesson-3-3-linreg.ipynb`. Раздели: `## Самостоятельная работа`.

> **Главное правило:** для регрессии используем **MAE, MSE, RMSE, R²** — **не accuracy**.

---

## Шпаргалка (минимум команд)

```python
from sklearn.linear_model import LinearRegression
from sklearn.preprocessing import StandardScaler
from sklearn.metrics import mean_absolute_error, mean_squared_error, r2_score

# Модель
model = LinearRegression()
model.fit(X_train, y_train)
preds = model.predict(X_test)

# Веса
model.coef_, model.intercept_

# Стандартизация
scaler = StandardScaler()
X_train_s = scaler.fit_transform(X_train)
X_test_s = scaler.transform(X_test)    # БЕЗ fit!
```

---

## Уровень 1. Понять линейную регрессию

### 1.1. Своя простая линейная регрессия

> **Задача:** реши **сам** маленькую регрессию по данным учёбы. Не подсматривай в практику.

**Дано:**
```python
X = np.array([[1], [3], [5], [7], [9]])
y = np.array([2.5, 3.5, 4.0, 4.5, 5.0])
```

**Что нужно сделать:**
1. **Обучи** `LinearRegression` на этих данных.
2. **Выведи** `model.coef_` и `model.intercept_`.
3. **Предскажи** оценку для x = 4 и x = 12.
4. **Построй** scatter + линию модели (как в практике 1.3).

> **Подсказка:** для построения линии используй `np.linspace(0, 15, 100).reshape(-1, 1)` как вход в `model.predict`.

**🤔 Вопросы на размышление (запиши в Markdown):**

1. Что физически означают значения `w` и `b`, которые ты получил?
2. Если ученик «учится 0 часов», какую оценку предскажет модель? Имеет ли это смысл?
3. Что произойдёт с моделью, если добавить точку **(100, 5.0)** (один час → пятёрка)? Попробуй и опиши.

---

### 1.2. Метрики «руками»

> **Задача:** посчитай **MAE, MSE, RMSE** **без sklearn**, по формуле.

**Дано:**
```python
y_true = np.array([100, 200, 300, 400, 500])
y_pred = np.array([110, 180, 320, 390, 490])
```

**Что нужно сделать:**

1. **Подумай и запиши** в Markdown: какая у тебя интуиция — **MAE будет больше или меньше RMSE**? Почему?

2. **Посчитай MAE** через формулу:
   - подсказка: модуль через `np.abs`, среднее через `.mean()`.

3. **Посчитай MSE и RMSE** через формулу:
   - подсказка: квадрат через `** 2`, корень через `np.sqrt`.

4. **Проверь** через sklearn — должны совпасть.

**🤔 Вопросы:**

1. Угадал ли ты, что больше — MAE или RMSE? Почему так получилось?
2. Если бы одна из ошибок была **в 10 раз больше** (например, |y_pred[2] - y_true[2]| = 200 вместо 20), как изменились бы MAE и RMSE? Какая чувствительнее?
3. Если бы все предсказания **идеально совпали** с правильными, чему были бы равны MAE, MSE и R²?

---

## Уровень 2. Применение на реальных данных

### 2.1. Boston Housing — твой второй датасет

> **Задача:** обучи `LinearRegression` на Boston Housing и **посчитай все 4 метрики**.

**Подсказка по загрузке:**
```python
url = "https://raw.githubusercontent.com/selva86/datasets/master/BostonHousing.csv"
df = pd.read_csv(url)
```

> **Целевая переменная:** `medv` (median value, цена дома в тысячах $).

**Что нужно сделать:**

1. **Изучи датасет:** размер, столбцы, пропуски, `describe()`.
2. **Раздели** на X (все столбцы кроме `medv`) и y.
3. **Train/test split** с `random_state=42`.
4. **Обучи** `LinearRegression`.
5. **Посчитай и распечатай** MAE, MSE, RMSE, R².
6. **Построй** scatter: реальные vs предсказанные цены.

> **Подсказка по форме:** в Boston цены **в тысячах**, поэтому RMSE будет небольшое число (типа 4-5).

**🤔 Вопросы:**

1. Чему равно R²? Это лучше или хуже, чем мы получили на California Housing на уроке?
2. Какой признак, по-твоему, **самый важный** для предсказания цены? Подсказка: посмотри на `model.coef_` и сопоставь с именами столбцов.
3. Найди район, для которого модель **сильнее всего ошиблась** (max(|y_test - predictions|)). Что у него такого особенного?

---

### 2.2. Сравнение признаков

> **Задача:** проверить, **сколько признаков** реально нужно для хорошего предсказания.

**Что нужно сделать:**

1. **Возьми** ту же модель Boston Housing.
2. **Сначала** обучи модель с **одним** признаком — `rm` (число комнат). Запиши R².
3. **Потом** обучи с **двумя** — `rm` и `lstat` (% бедного населения). Запиши R².
4. **Потом** с **всеми** признаками. Запиши R².

> **Подсказка:** для одного признака используй `X = df[['rm']]` (двойные скобки!).

**🤔 Вопросы:**

1. Какой R² ты получил для каждого варианта? Запиши **в виде таблицы** в Markdown.
2. Сильно ли вырос R² при добавлении признаков? Делай вывод: **достаточно** ли 1-2 признаков, или нужны все?
3. Какой признак самый информативный — `rm` или `lstat`? Сделай вывод по R² моделей с одним признаком.

---

## Уровень 3. Со звёздочкой — стандартизация

### 3.1. Эффект стандартизации на разных моделях

> **Задача:** провести **эксперимент**: показать, **где** стандартизация важна, а **где** нет.

**Дано — California Housing с предыдущей практики.**

**Что нужно сделать:**

1. **Возьми X_train, X_test, y_train, y_test** из практики 2.
2. **Сделай** X_train_scaled, X_test_scaled через `StandardScaler`.
3. **Обучи 4 модели** (импортируй сам, если нужно):
   - `LinearRegression`
   - `KNeighborsRegressor(n_neighbors=5)`
   - `DecisionTreeRegressor(random_state=42)`
   - `RandomForestRegressor(n_estimators=50, random_state=42)`
4. **Каждую** модель обучи **дважды** — на оригинальном X и на scaled X.
5. **Запиши R²** в таблицу.

**Шаблон таблицы для ответа в Markdown:**

```markdown
| Модель | R² без станд. | R² со станд. | Прирост |
|--------|---------------|--------------|---------|
| LinearRegression | ... | ... | ... |
| KNN-5 | ... | ... | ... |
| DecisionTree | ... | ... | ... |
| RandomForest | ... | ... | ... |
```

**🤔 Вопросы (главное):**

1. У какой модели стандартизация дала **самый большой** прирост? Почему?
2. У какой модели стандартизация **не помогла** (или даже немного навредила)? Почему?
3. **Сформулируй правило:** «Стандартизация важна, когда модель ...» (продолжи).
4. Если бы тебе в реальной работе дали датасет — **с чего бы ты начал**: со стандартизации или без?

---

### 3.2. Своя реализация градиентного спуска (вспоминаем 1.7)

> **Задача:** **обучи `y = w·x + b`** через **свой** градиентный спуск, без sklearn. Это **ровно то**, что мы делали в Уроке 1.7.

**Дано:**
```python
np.random.seed(0)
x_data = np.linspace(0, 10, 30)
y_data = 2 * x_data + 5 + np.random.normal(0, 1, 30)    # y ≈ 2x + 5 + шум
```

**Что нужно сделать:**

1. **Напиши функцию потерь** `loss(w, b)` — сумма квадратов отклонений.
2. **Напиши численный градиент** функции **двух переменных** (как на уроке 1.7).
3. **Запусти градиентный спуск:**
   - старт: `w = 0`, `b = 0`.
   - `lr = 0.001`.
   - 1000 шагов.
4. **Сравни** найденные `w, b` с `LinearRegression().fit(X, y)`.

> **Подсказка:** аккуратно с массивом параметров. Удобно делать так:
> ```python
> params = np.array([w_start, b_start])
> def loss_for_params(params):
>     w, b = params[0], params[1]
>     ...
> ```

**🤔 Вопросы:**

1. К каким значениям `w` и `b` сошёлся твой градиентный спуск?
2. Совпадают ли они с тем, что нашёл sklearn?
3. Что произойдёт, если **уменьшить** lr до `0.0001`? Сколько шагов теперь нужно для сходимости?
4. Что произойдёт, если **увеличить** lr до `0.05`? Запусти и опиши, что увидел.

> **Это важное упражнение:** ты только что **с нуля** реализовал базу всего ML — оптимизацию параметров градиентным спуском. **Поздравь себя.**

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Понимает смысл `coef_` и `intercept_` | |
| Использует **MAE/MSE/RMSE/R²**, а не accuracy для регрессии | |
| **Уровень 2:** обучил модель на Boston, посчитал метрики | |
| **Ответил на вопросы размышления** | |
| **Уровень 3:** провёл эксперимент со стандартизацией | |

> **Минимум для «зачёта»:** Уровень 1 + одна задача Уровня 2 + **ответы на вопросы**.
>
> **Без ответов на вопросы — задача не считается решённой.** Цель урока — не запустить код, а **понять**.
