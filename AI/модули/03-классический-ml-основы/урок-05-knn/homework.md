# Урок 3.5. Самостоятельная работа на уроке

> **Как всегда:** в подсказках **мало готового кода**. Я даю **направление** — думать и писать код твоя задача.
>
> После каждого задания — **🤔 вопросы на размышление**. Записывай ответы в Markdown-ячейку.
>
> Тетрадка: продолжай `lesson-3-5-knn.ipynb`. Заголовок `## Самостоятельная работа`.

> **Главное правило урока:** KNN считает **расстояния**, поэтому **всегда стандартизируй признаки** перед обучением.

---

## Шпаргалка (минимум команд)

```python
from sklearn.neighbors import KNeighborsClassifier
from sklearn.preprocessing import StandardScaler
from sklearn.metrics import accuracy_score, classification_report, confusion_matrix

# Стандартизация (для KNN обязательна!)
scaler = StandardScaler()
X_train_s = scaler.fit_transform(X_train)
X_test_s = scaler.transform(X_test)      # БЕЗ fit!

# Модель
knn = KNeighborsClassifier(n_neighbors=5)
knn.fit(X_train_s, y_train)
preds = knn.predict(X_test_s)
knn.score(X_test_s, y_test)              # точность одной командой
```

Напоминание: **евклидово расстояние** между точками A и B (2 признака):
`d = sqrt((x1_A - x1_B)² + (x2_A - x2_B)²)` — теорема Пифагора.

---

## Уровень 1. Понять KNN

### 1.1. KNN руками (без кода!)

> **Задача:** прожить алгоритм KNN «вручную», чтобы понять его изнутри.

**Дано.** Есть 5 обучающих точек (1 признак — «рост в см») с классами:

| Точка | Рост | Класс |
|-------|------|-------|
| A | 150 | баскетбол ❌ |
| B | 160 | баскетбол ❌ |
| C | 185 | баскетбол ✅ |
| D | 190 | баскетбол ✅ |
| E | 195 | баскетбол ✅ |

(✅ = «подходит для баскетбола», ❌ = «нет»)

Приходит **новый человек ростом 180 см**.

**Что нужно сделать (на бумаге / в Markdown, без Python):**
1. Посчитай **расстояние** от новой точки (180) до каждой из 5 (для одного признака расстояние — это просто модуль разности, `|180 - рост|`).
2. Отсортируй точки по близости.
3. При **k=1**: какой класс предскажет KNN?
4. При **k=3**: какой класс? (посмотри на 3 ближайших и проголосуй)
5. При **k=5**: какой класс?

> **Подсказка:** не считай корни и квадраты — для **одного** признака расстояние это просто `|разность|`. Например, от 180 до 160 расстояние = 20.

**🤔 Вопросы на размышление:**

1. Совпали ли ответы при k=1, k=3 и k=5? Если нет — почему?
2. Сначала **предскажи**, потом проверь: при каком k ответ «баскетбол ✅», а при каком «❌»?
3. Почему k=4 здесь было бы **плохой идеей**? (вспомни про ничью)

---

### 1.2. «Ленивое обучение» на словах

> **Задача:** объяснить своими словами, чем KNN отличается от логистической регрессии. **Кода нет — только текст в Markdown.**

**Что нужно сделать.** Ответь на вопросы (2–4 предложения каждый):

1. Что делает логистическая регрессия в момент `fit`? А что делает KNN в момент `fit`?
2. Какая из двух моделей **быстрее обучается**? Какая **быстрее предсказывает**? Почему?
3. Где «хранится знание» у логистической регрессии, а где — у KNN?

**🤔 Вопросы:**

1. Представь, что у тебя **10 миллионов** обучающих объектов. Какой модели будет тяжелее **предсказывать** новый объект — KNN или логистической регрессии? Почему?
2. Назови **2 примера из жизни**, где принцип «посмотри на похожих и сделай как они» реально работает (например, рекомендации фильмов).

---

## Уровень 2. Своя модель KNN

### 2.1. KNN на Titanic (со стандартизацией!)

> **Задача:** применить KNN к датасету Titanic из Урока 3.4 и **сравнить** с логистической регрессией.

**Что нужно сделать:**
1. Загрузи Titanic (URL из Урока 3.4).
2. Возьми признаки `Pclass, Sex, Age, Fare`. Не забудь: `Sex` → число, пропуски `Age` → медиана.
3. Train/test split (`random_state=42, stratify=y`).
4. **Стандартизируй** признаки через `StandardScaler` (для KNN это обязательно!).
5. Обучи `KNeighborsClassifier(n_neighbors=5)` на **стандартизированных** данных.
6. Посчитай accuracy, precision, recall, F1 (как в Уроке 3.4).
7. Сравни с результатом логистической регрессии из Урока 3.4 (там было acc ≈ 0.80).

> **Подсказка:** код подготовки данных у тебя уже есть из Урока 3.4 — переиспользуй его. Главное **новое** — добавить `StandardScaler` ПЕРЕД обучением KNN.

**🤔 Вопросы:**

1. KNN получился лучше, хуже или примерно как логистическая регрессия? Как думаешь, почему?
2. **Эксперимент:** обучи KNN **без** стандартизации (на обычных X_train/X_test) и сравни accuracy. На сколько упало? Объясни, **какой признак виноват** (подсказка: у какого признака самые большие числа — `Age` или `Fare`?).
3. Где KNN ошибается чаще — путает выживших или погибших? (посмотри confusion matrix)

---

### 2.2. Подбор k для Titanic

> **Задача:** найти оптимальное k для Titanic, построив график «точность от k».

**Что нужно сделать:**
1. Возьми **стандартизированные** данные Titanic из задачи 2.1.
2. Перебери k от 1 до 30 (как в практике 2.1).
3. Для каждого k посчитай accuracy на тесте, сохрани в список.
4. Построй график «точность от k».
5. Найди лучшее k через `np.argmax`.

> **Подсказка:** структура цикла — как в практике (`for k in range(1, 31)`, внутри создаёшь модель, обучаешь, `.append(score)`).

**🤔 Вопросы:**

1. Какое k оказалось лучшим? Это маленькое, среднее или большое значение?
2. Как выглядит график — есть ли «холм» (рост, потом спад) как на ирисах? Или по-другому?
3. При k=1 точность выше или ниже, чем при оптимальном k? Объясни через «переобучение».

---

## Уровень 3. Со звёздочкой

### 3.1. Граница решений (decision boundary)

> **Задача:** **нарисовать** границу решений KNN — увидеть, как k меняет её форму. Это эффектная визуализация, которую любят показывать на собеседованиях.

**Что нужно сделать:**
1. Возьми Iris, но **только 2 признака** (`petal_length, petal_width`) — чтобы рисовать на плоскости.
2. Обучи KNN с **k=1** на этих 2 признаках.
3. Нарисуй область, закрашенную по предсказанному классу (см. подсказку ниже), поверх неё — точки данных.
4. Повтори с **k=15**.
5. Сравни две картинки.

> **Подсказка по «закрашиванию» (это новый приём):**
> ```python
> # 1. Создаём сетку точек по всей плоскости
> x_min, x_max = X[:,0].min()-1, X[:,0].max()+1
> y_min, y_max = X[:,1].min()-1, X[:,1].max()+1
> xx, yy = np.meshgrid(np.linspace(x_min, x_max, 200),
>                      np.linspace(y_min, y_max, 200))
> # 2. Предсказываем класс для каждой точки сетки
> #    (нужно превратить виды в числа, чтобы рисовать цветом)
> # 3. Рисуем: plt.contourf(xx, yy, предсказания) + plt.scatter(точки)
> ```
> Не страшно, если не получится сразу — это **необязательное** задание со звёздочкой. Главное — попробовать и посмотреть на результат.

**🤔 Вопросы:**

1. У какого k граница **«рваная»**, а у какого — **гладкая**? Совпало с теорией (раздел 4)?
2. Какая граница, по-твоему, лучше **обобщает** на новые данные — рваная (k=1) или гладкая (k=15)? Почему?
3. Что было бы с границей при **k = 120** (все обучающие точки)? Попробуй предсказать **до** запуска.

---

### 3.2. KNN-регрессия

> **Задача:** убедиться, что KNN умеет предсказывать не только класс, но и **число**.

**Что нужно сделать:**
1. Возьми датасет California Housing из Урока 3.3 (или Boston Housing).
2. Импортируй `KNeighborsRegressor` (из `sklearn.neighbors`).
3. **Стандартизируй** признаки.
4. Обучи `KNeighborsRegressor(n_neighbors=5)`.
5. Посчитай **R² и RMSE** (метрики регрессии из Урока 3.3 — НЕ accuracy!).

> **Подсказка:** для регрессии метрики другие. Используй `r2_score` и `mean_squared_error` из `sklearn.metrics`, как в Уроке 3.3. Не перепутай: для **числа** accuracy не считают.

**🤔 Вопросы:**

1. Чем отличается «голосование» в KNN-классификации от того, как KNN-**регрессия** получает ответ? (вспомни раздел 6 теории)
2. Какой R² получился? Сравни с линейной регрессией из Урока 3.3 (там было ≈ 0.64 на California). KNN лучше или хуже?
3. Если у 3 ближайших квартир цены 4, 5 и 9 млн — что предскажет KNN-регрессия при k=3?

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Понимает «голосование соседей» и роль k | |
| Считает расстояние (хотя бы для 1 признака) | |
| **Всегда стандартизирует** перед KNN | |
| **Уровень 2:** обучил KNN, сравнил с логрегрессией | |
| **Ответил на 🤔 вопросы размышления** | |
| **Уровень 3:** граница решений ИЛИ KNN-регрессия | |

> **Минимум для «зачёта»:** Уровень 1 (обе задачи) + одна задача Уровня 2 + **ответы на вопросы**.
>
> **Без ответов на вопросы — задача не считается решённой.**
> **Главное — понять идею «похожие голосуют» и связку KNN↔стандартизация, а не прогнать код.**

---

> **Не пугайся,** если граница решений (3.1) не получится с первого раза — это самое сложное задание модуля. А вот **связку «KNN → обязательно StandardScaler»** запомни железно: это спросят на любом собеседовании по ML.
