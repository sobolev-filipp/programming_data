# Контрольная работа по Модулю 3

> **Время:** ~64 минуты.
> **Тетрадка:** `lesson-3-8-control.ipynb` в твоём GitHub-репозитории.
> **Можно:** документация scikit-learn, твои `summary.md` уроков 3.1–3.7.
> **Нельзя:** ChatGPT/Claude, помощь одноклассников.

> **Совет:** делай задачу за задачей. Не сидишь на одной дольше 9 минут — переходи к следующей.

**Датасеты:**
```python
TITANIC = "https://raw.githubusercontent.com/datasciencedojo/datasets/master/titanic.csv"
BOSTON  = "https://raw.githubusercontent.com/selva86/datasets/master/BostonHousing.csv"
IRIS    = "https://raw.githubusercontent.com/mwaskom/seaborn-data/master/iris.csv"
```

---

## Задача 1. Типы задач ML (10 баллов)

> Урок 3.1. **Кода не нужно** — отвечай текстом в Markdown-ячейке.

**А)** Для каждой задачи определи: это **классификация**, **регрессия** или **кластеризация**? И **с учителем** (supervised) или **без учителя** (unsupervised)?

1. Предсказать **цену** подержанного автомобиля по пробегу и году.
2. Определить, **спам** письмо или нет.
3. Разбить **покупателей** магазина на группы по поведению (заранее групп не знаем).
4. Предсказать **оценку** ученика (от 2 до 5) — как **число**.
5. Распознать на фото **кошку, собаку или птицу**.

**Б)** Ответь на 2 вопроса (1–2 предложения каждый):
1. Чем **обучение с учителем** отличается от **обучения без учителя**?
2. Что такое **признак** (feature) и что такое **целевая переменная** (target)? Приведи пример из задачи 1.

---

## Задача 2. Признаки и train/test split (10 баллов)

> Урок 3.2.

**А)** Ученик написал код. Найди **3 ошибки** (текстом):
```python
df = pd.read_csv(TITANIC)
X = df[['Pclass', 'Age', 'Sex']]
y = df['Survived']
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)
model.fit(X_train, y_train)
```

**Б)** Напиши **правильный** код подготовки Titanic для классификации:
- признаки `Pclass, Sex, Age, Fare` (помни: `Sex` — строка, `Age` — с пропусками);
- целевая `Survived`;
- split 80/20, с `random_state=42` и сохранением долей классов.

**В)** Объясни (текстом): зачем нужен **`stratify=y`** и почему важен **`random_state`**?

---

## Задача 3. Линейная регрессия (15 баллов)

> Урок 3.3. Датасет **Boston Housing** (цель — `medv`, цена дома в тысячах $).

**А)** Загрузи Boston, раздели на X (все столбцы кроме `medv`) и y. Train/test split (`random_state=42`).

**Б)** Обучи `LinearRegression`. Посчитай и распечатай **MAE, RMSE, R²**.

**В)** Ответь текстом:
1. Что означает твой **R²** простыми словами?
2. Почему здесь **нельзя** использовать `accuracy`?

**Г)** Обучи `KNeighborsRegressor(n_neighbors=5)` на этих же данных **двумя способами**: без стандартизации и со `StandardScaler`. Сравни **R²**. Объясни разницу.

---

## Задача 4. Логистическая регрессия и метрики (15 баллов)

> Урок 3.4. Датасет **Titanic** (используй подготовку из Задачи 2).

**А)** Обучи `LogisticRegression(max_iter=1000)`. Распечатай **accuracy**.

**Б)** Построй **confusion matrix**. Подпиши, где TP, FP, FN, TN.

**В)** Посчитай **precision, recall, F1** (или выведи `classification_report`).

**Г)** Ответь текстом:
1. Чего модель делает больше — **пропускает выживших (FN)** или **зря «спасает» погибших (FP)**? Как это видно из матрицы?
2. Представь, что эта модель решает, кого первым сажать в спасательную шлюпку. Какая метрика важнее — **precision или recall**? Почему?

---

## Задача 5. KNN (15 баллов)

> Урок 3.5. Датасет **Iris** (цель — `species`).

**А)** Загрузи Iris, подготовь X (4 признака) и y. Split (`random_state=42, stratify=y`). **Стандартизуй** признаки.

**Б)** Обучи `KNeighborsClassifier(n_neighbors=5)` на стандартизированных данных. Распечатай accuracy.

**В)** **Подбери лучшее k:** перебери k от 1 до 20, для каждого посчитай accuracy на тесте, построй **график** «точность от k» и назови лучшее k.

**Г)** Ответь текстом: почему для KNN стандартизация **важнее**, чем для дерева решений?

---

## Задача 6. Дерево решений (15 баллов)

> Урок 3.6. Датасет **Titanic**.

**А)** Обучи `DecisionTreeClassifier(max_depth=3, random_state=42)`. Распечатай accuracy на тесте.

**Б)** **Покажи переобучение:** обучи дерево **без** ограничения глубины и распечатай точность **на train и на test**. Объясни, что значит большой разрыв.

**В)** Распечатай **важность признаков** (`feature_importances_`). Какой признак главный?

**Г)** Ответь текстом: нужна ли дереву стандартизация? Почему?

---

## Задача 7. Случайный лес и кросс-валидация (20 баллов)

> Урок 3.7. Датасет **Titanic**.

**А)** Обучи `RandomForestClassifier(n_estimators=100, random_state=42)`. Сравни его accuracy **на train и test** с одним деревом из Задачи 6. Что заметил?

**Б)** Оцени лес через **кросс-валидацию** (`cross_val_score`, `cv=5`). Распечатай **среднюю** точность и **std**.

**В)** Объясни текстом: почему оценка по кросс-валидации **надёжнее**, чем по одному train/test split?

**Г)** Распечатай важность признаков леса. Совпадает ли главный признак с деревом из Задачи 6?

---

## ★ Бонус (20 баллов)

> **Решать ТОЛЬКО** если задачи 1–7 готовы.
> Датасет — встроенный в sklearn (грузить из интернета не надо): **breast cancer** (рак груди: злокачественная/доброкачественная опухоль по 30 числовым признакам).

```python
from sklearn.datasets import load_breast_cancer
data = load_breast_cancer()
X = pd.DataFrame(data.data, columns=data.feature_names)
y = data.target          # 0 = злокачественная, 1 = доброкачественная
```

**Твоя задача — провести полный ML-эксперимент:**

1. Раздели на train/test (`random_state=42, stratify=y`).
2. Сравни **три модели** по **кросс-валидации** (`cv=5`):
   - `LogisticRegression(max_iter=5000)` — **со** стандартизацией;
   - `KNeighborsClassifier(n_neighbors=5)` — **со** стандартизацией;
   - `RandomForestClassifier(n_estimators=100, random_state=42)` — без стандартизации.
3. Выбери **лучшую** модель по средней CV-точности.
4. Для лучшей модели на тесте посчитай **precision и recall** для класса «злокачественная» (класс 0).
5. **Ответь текстом:** это **медицинская** задача — пропустить злокачественную опухоль очень опасно. Какая метрика тут критична и почему? Что бы ты сделал с **порогом**, чтобы реже пропускать больных?

---

## Сдача

1. Сохрани `lesson-3-8-control.ipynb` в Colab.
2. Скачай (`File → Download .ipynb`).
3. Загрузи в GitHub в папку `homework/lesson-3-8/`.
4. Коммит с сообщением `Контрольная по Модулю 3`.

**Покажи преподавателю**, что файл появился на GitHub. Только тогда работа считается сданной.

---

## Шкала оценок

| Баллов | Оценка |
|--------|--------|
| 90+ | Отлично |
| 70–89 | Хорошо |
| 50–69 | Удовлетворительно |
| <50 | Пересдача |

**Удачи! Это финал Модуля 3 — после него ты владеешь полным набором классического ML. 🚀**
