# Урок 6.8. Самостоятельная работа — твоя «обучаемая машина»

> **Главное задание модуля:** примени конвейер с урока к **СВОИМ** категориям. Сегодня твоя машина различала камень-ножницы-бумагу — теперь научи её распознавать то, что интересно **тебе**.
>
> Подсказки — **направление, не готовый код**. Конвейер у тебя уже есть (из практики) — меняй в нём только данные.
>
> После каждого задания — **🤔 вопросы на размышление**. Тетрадка: `lesson-6-8-project.ipynb`.

> **Главное правило проекта:** «обучаемая машина» = **тот же код, другие данные**. Меняешь папки с картинками — решаешь новую задачу.

---

## Шпаргалка (весь конвейер из практики)

```python
# 1. Данные по папкам:   my_data/класс1/  my_data/класс2/  ...
train_data = datasets.ImageFolder('my_data', transform=train_tf)

# 2. Аугментация — в train_tf (Resize 224 + flip/rotation + Normalize ImageNet)
# 3. Модель: готовая ResNet, заморозка, голова под N классов:
net.fc = nn.Linear(512, N)          # N = число твоих классов
# 4. Обучение: цикл (Adam, CrossEntropyLoss), учим net.fc
# 5. Оценка: точность + матрица ошибок
# Предсказание: predict("фото.jpg")
```

---

## Уровень 1. Понять проект

### 1.1. Карта проекта (без кода!)

> **Задача:** проверить, что понял конвейер. **Ответы текстом.**

1. Назови **5 шагов** конвейера по порядку.
2. Зачем в проекте **transfer learning**, а не CNN с нуля? (вспомни про объём данных)
3. Что показывает **матрица ошибок** такого, чего не показывает одна «точность»?
4. Что такое **инференс** и чем он отличается от обучения?
5. Что значит «обучаемая машина» — что в коде нужно поменять для новой задачи, а что нет?

**🤔 Вопросы на размышление:**

1. В нашей матрице модель путала **бумагу и ножницы**, а камень — никогда. Почему именно так? (подумай про форму жестов)
2. Если бы точность была 94%, но **камень** путался с бумагой в половине случаев — это была бы «хорошая» модель? Почему важно смотреть **матрицу**, а не только процент?
3. Наша модель обучена на фото рук **на белом фоне**. Будет ли она так же точна на фото твоей руки на фоне комнаты? Почему? (это про train/test распределение)

---

### 1.2. Анализ своей матрицы (после практики)

> **Задача:** разобрать результат своей модели. **Ответы текстом.**

1. Какой класс твоя модель распознаёт **лучше** всех? Какой — **хуже**?
2. Какие два класса она **путает** чаще всего? Почему они похожи?
3. Сколько всего ошибок (сумма всех чисел **вне** диагонали)?

**🤔 Вопросы:**

1. Как можно **уменьшить** путаницу между похожими классами? (больше данных? другие аугментации? больше эпох?)
2. Если добавить 4-й класс «рука на ребре» — на какие классы он, скорее всего, будет похож?

---

## Уровень 2. Своя «обучаемая машина»

### 2.1. Собери свой датасет (2–4 класса)

> **Задача — главная в модуле:** научи машину распознавать **твои** категории.

**Что нужно сделать:**
1. Выбери 2–4 класса, которые тебе интересны. Идеи: **твои кроссовки разных брендов**, **виды твоих питомцев/игрушек**, **карточки/монеты**, **фрукты на кухне**, **эмодзи нарисованные от руки**, **жесты руки** (свои!).
2. Собери по **30–60 фото** на класс (сними телефоном или скачай). Разложи по папкам: `my_data/класс1/`, `my_data/класс2/`...
3. Загрузи папку в Colab, прогони **тот же конвейер** (только смени путь и число классов в голове).
4. Обучи, оцени матрицей ошибок, проверь на новых фото.

> **Подсказки:**
> - Структура папок — как у RPS: имя папки = класс. `ImageFolder` сам разберётся.
> - Голова: `nn.Linear(512, N)`, где N — число твоих классов.
> - Снимай на **однородном фоне** и с разных ракурсов — так аугментация поможет, а модель не зацепится за фон.
> - 30–60 фото хватает **именно** благодаря transfer learning (с нуля не хватило бы!).

**🤔 Вопросы:**

1. Сколько фото на класс тебе понадобилось для приемлемой точности?
2. Какие классы машина путала? Это были **похожие** объекты?
3. Что было бы, если снять все фото класса на **одном** фоне (например, все кошки на диване)? На что «обучилась» бы модель — на кошку или на диван?

---

### 2.2. Эксперимент: больше эпох / другая аугментация

> **Задача:** улучшить свою модель.

**Что нужно сделать:**
1. Обучи свою модель сначала 3 эпохи, потом 8–10. Сравни точность и матрицу.
2. Поменяй аугментацию (добавь/убери поворот, `ColorJitter`). Сравни.

> **Подсказка:** меняй только число эпох (`range(...)`) и `train_tf`. Остальное не трогай.

**🤔 Вопросы:**

1. Больше эпох помогли? До какого момента росла точность?
2. Какая аугментация помогла, какая навредила? (вспомни 6.5: аугментация должна сохранять класс)
3. Можно ли «переобучить» даже с transfer learning? Как это заметить по train/test?

---

## Уровень 3. Со звёздочкой

### 3.1. Доделай игру

> **Задача:** превратить бонус в настоящую игру.

**Что нужно сделать:**
1. Сделай игру «до 3 побед»: цикл раундов, счёт игрока и компьютера.
2. После каждого раунда показывай счёт; объяви победителя в конце.

> **Подсказка:** оберни `play()` в цикл `while`, считай победы в переменных. Используй навыки из Модулей 1–2.

**🤔 Вопросы:**

1. Где здесь работа **нейросети**, а где — **обычного кода**? Раздели роли.
2. Что будет, если модель **ошибётся** в распознавании жеста? Как это влияет на честность игры?
3. Как сделать игру честнее/устойчивее к ошибкам распознавания?

---

### 3.2. Детектор + классификатор вместе (исследование)

> **Задача:** соединить два инструмента модуля.

**Что нужно сделать (можно частично теоретически):**
1. Подумай: как с помощью **YOLO** найти на фото объект (например, руку/предмет), **вырезать** его рамкой и подать в **свой классификатор**?
2. Если получится — попробуй на фото.

> **Подсказка:** YOLO даёт рамку `(x1,y1,x2,y2)`. По ней можно вырезать кусок картинки (`img.crop`) и отдать в `predict()`. Это связка «детекция → классификация».

**🤔 Вопросы:**

1. Зачем сначала **детектировать**, а потом **классифицировать**? (что даёт вырезание объекта)
2. Где такая связка нужна в жизни? (распознавание номеров машин, лиц, товаров)
3. Чем «детекция + классификация» мощнее, чем что-то одно?

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Прошёл **весь** конвейер (5 шагов) | |
| Получил работающую модель и точность | |
| **Построил и прочитал матрицу ошибок** | |
| Понимает идею «обучаемой машины» (код универсален) | |
| **Уровень 2:** свой датасет / свои категории | |
| **Ответил на 🤔 вопросы размышления** | |

> **Минимум для «зачёта»:** рабочий классификатор + матрица ошибок + свой датасет (Уровень 2.1) + **ответы на вопросы**.
>
> **Без ответов на вопросы — задача не считается решённой.**
> **Главное — понять: ты построил универсальный конвейер. Меняешь данные — решаешь любую задачу распознавания.**

---

> 🎉 **Ты завершил Модуль 6 — Компьютерное зрение!** Ты прошёл путь от «картинка — это массив чисел» до собственного ИИ-проекта, который учится распознавать что угодно. Ты умеешь: готовить данные, аугментировать, брать готовые сети (transfer learning), обучать, **честно оценивать** модель и даже находить объекты детектором. Это — реальный набор инженера компьютерного зрения.
>
> Дальше — **Модуль 7: обработка текста (NLP)**. Компьютер научился **видеть** — теперь научим его **читать и понимать тексты**: от «мешка слов» до умных моделей, которые отвечают на вопросы. 🚀
