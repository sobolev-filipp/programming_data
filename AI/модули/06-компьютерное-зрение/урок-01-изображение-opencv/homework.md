# Урок 6.1. Самостоятельная работа на уроке

> **Как всегда:** в подсказках **мало готового кода**. Я даю **направление** — думать твоя задача.
>
> После каждого задания — **🤔 вопросы на размышление**. Записывай ответы в Markdown-ячейку.
>
> Тетрадка: продолжай `lesson-6-1-images.ipynb`. Заголовок `## Самостоятельная работа`.

> **Главное правило урока:** картинка = массив `(H, W, 3)` чисел 0–255. Любая обработка — это операции над массивом (NumPy, Урок 1.4).

---

## Шпаргалка (из практики)

```python
from skimage import data           # img = data.chelsea() / data.astronaut() / data.coffee()
import cv2, numpy as np, matplotlib.pyplot as plt

img.shape                          # (H, W, 3)
img[y, x]                          # пиксель [R, G, B]
img[:, :, 0]                       # красный канал
cv2.cvtColor(img, cv2.COLOR_RGB2GRAY)   # серое
cv2.resize(img, (w, h))            # размер
img[y1:y2, x1:x2]                  # обрезка (срез)
cv2.GaussianBlur(img, (15,15), 0)  # размытие
```

> Своя картинка: `from PIL import Image; img = np.array(Image.open('файл.jpg'))`.

---

## Уровень 1. Понять изображение

### 1.1. Картинка как числа (без кода!)

> **Задача:** проверить понимание. **Ответы текстом.**

1. Что такое пиксель у серой картинки? А у цветной?
2. Что означает форма `(480, 640, 3)`? Расшифруй каждое число.
3. Какой пиксель `(R, G, B)`: чисто синий? жёлтый? белый? чёрный?
4. Сколько всего чисел в цветной картинке 200×300?

**🤔 Вопросы на размышление:**

1. Почему яркость пикселя — число от **0 до 255**, а не, скажем, от 0 до 1000? (вспомни про байт)
2. Цветная картинка занимает в **3 раза** больше места, чем серая того же размера. Почему?
3. Почему фото высокого разрешения «весит» много мегабайт? (посчитай числа для 4000×3000)

---

### 1.2. Ловушка BGR (без кода!)

> **Задача:** объяснить классическую ошибку. **Ответы текстом.**

1. В каком порядке OpenCV (`cv2.imread`) загружает каналы?
2. Что случится с цветами, если показать такую картинку через matplotlib без преобразования?
3. Как починить (какая команда)?

**🤔 Вопросы:**

1. Почему «синий кот» — это признак ошибки BGR/RGB? (что с чем перепуталось)
2. Почему PIL/skimage этой проблемы не имеют?

---

## Уровень 2. Свои эксперименты

### 2.1. Создай картинку из чисел

> **Задача:** убедиться, что картинка это просто числа — собрать её **с нуля**.

**Что нужно сделать:**
1. Создай массив `(100, 100, 3)` из нулей (чёрная картинка): `np.zeros((100,100,3), dtype=np.uint8)`.
2. Сделай левую половину **красной** (R=255), правую — **синей** (B=255).
3. Нарисуй белый квадрат в центре (область → 255 по всем каналам).
4. Покажи результат.

> **Подсказка:** работай срезами. `img[:, :50, 0] = 255` — левая половина, красный канал. Это чистый NumPy (Урок 1.4)!

**🤔 Вопросы:**

1. Получилось ли нарисовать картинку **только числами**, без фотоаппарата?
2. Что это доказывает про природу изображений?
3. Как сделать пиксель **жёлтым**? А **фиолетовым**? (какие каналы включить)

---

### 2.2. Поэкспериментируй с каналами

> **Задача:** понять роль каналов, «ломая» их.

**Что нужно сделать:**
1. Возьми цветную картинку (`data.coffee()` или свою).
2. **Обнули** красный канал (`img[:,:,0] = 0`) — посмотри, что стало с цветами.
3. **Поменяй местами** красный и синий каналы — посмотри (это и есть «эффект BGR»!).
4. Усиль зелёный (умножь канал на 1.5, аккуратно с переполнением).

> **Подсказка:** делай копию `img.copy()` перед изменением, чтобы не испортить оригинал. Для перестановки каналов: `img2 = img[:, :, ::-1]`.

**🤔 Вопросы:**

1. Что стало с картинкой без красного канала? Какие цвета пропали?
2. Перестановка R↔B — на что похож эффект? (вспомни ловушку BGR!)
3. Зачем вообще нужны 3 канала, а не один? Что мы теряем в серой картинке?

---

## Уровень 3. Со звёздочкой

### 3.1. Свой фильтр-размытие

> **Задача:** понять, что размытие — это усреднение, собрав его руками.

**Что нужно сделать:**
1. Возьми **серую** картинку.
2. Напиши простое размытие: каждый пиксель = **среднее** его и 8 соседей (окно 3×3).
3. Сравни свой результат с `cv2.GaussianBlur`.

> **Подсказка:** для каждого пикселя (кроме краёв) усредни область `gray[y-1:y+2, x-1:x+2].mean()`. Это медленно через циклы, но наглядно. Так работает **свёртка** — окно бежит по картинке! (На 6.2 узнаем быстрый способ.)

**🤔 Вопросы:**

1. Похож ли твой результат на `GaussianBlur`? В чём идея размытия одной фразой?
2. «Окно 3×3 бежит по картинке и усредняет» — как это связано со **свёрткой** (тема 6.2)?
3. Что было бы, если в окне не усреднять, а брать **разницу** соседей? (подсказка: это выделение краёв!)

---

### 3.2. Выдели края (Canny)

> **Задача:** увидеть, как находят **контуры** объектов — основу распознавания.

**Что нужно сделать:**
1. Возьми серую картинку.
2. Примени детектор краёв: `edges = cv2.Canny(gray, 100, 200)`.
3. Покажи рядом оригинал и края.

> **Подсказка:** `cv2.Canny` находит резкие переходы яркости = границы объектов. Результат — чёрно-белая картинка с белыми контурами.

**🤔 Вопросы:**

1. Что выделил детектор краёв? На что похож результат (контурный рисунок?)
2. Почему **края** так важны для распознавания объектов? (форма = контуры)
3. Как думаешь, свёрточные сети (6.2+) тоже ищут края? Зачем?

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Понимает «картинка = массив (H,W,3)» | |
| Различает серое (1 канал) и цветное (3 канала) | |
| Умеет разделять каналы, делать серым | |
| Знает ловушку BGR | |
| **Уровень 2:** создал/изменил картинку через массивы | |
| **Ответил на 🤔 вопросы размышления** | |

> **Минимум для «зачёта»:** Уровень 1 (обе задачи) + одна задача Уровня 2 + **ответы на вопросы**.
>
> **Без ответов на вопросы — задача не считается решённой.**
> **Главное — понять, что картинка это массив чисел, и зрение это математика над ним.**

---

> **Не пугайся** новой темы — картинка это просто **массив NumPy**, который ты уже умеешь крутить (Урок 1.4). А слово **«свёртка»** запомни: размытие и края, что мы делали, работают через свёртку, и она — сердце компьютерного зрения. На следующем уроке разберём её до конца и поймём, как сети **сами учат** фильтры, чтобы видеть! 👁️