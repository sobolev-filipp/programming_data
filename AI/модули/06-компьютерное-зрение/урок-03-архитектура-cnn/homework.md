# Урок 6.3. Самостоятельная работа на уроке

> **Как всегда:** в подсказках **мало готового кода**. Я даю **направление** — думать твоя задача.
>
> После каждого задания — **🤔 вопросы на размышление**. Записывай ответы в Markdown-ячейку.
>
> Тетрадка: продолжай `lesson-6-3-cnn.ipynb`. Заголовок `## Самостоятельная работа`.

> **Главное правило урока:** CNN = извлекатель признаков (свёртки + пулинг) + классификатор (Linear). Размер ↓, каналы ↑.

---

## Шпаргалка (из практики)

```python
class CNN(nn.Module):
    def __init__(self):
        super().__init__()
        self.conv1 = nn.Conv2d(1, 16, 3, padding=1)   # in→out каналов
        self.conv2 = nn.Conv2d(16, 32, 3, padding=1)
        self.pool = nn.MaxPool2d(2)                    # уменьшает вдвое
        self.fc1 = nn.Linear(32*7*7, 64)
        self.fc2 = nn.Linear(64, 10)
    def forward(self, x):
        x = self.pool(torch.relu(self.conv1(x)))
        x = self.pool(torch.relu(self.conv2(x)))
        x = x.view(x.size(0), -1)
        x = torch.relu(self.fc1(x))
        return self.fc2(x)
```

---

## Уровень 1. Понять архитектуру

### 1.1. Конвейер CNN (без кода!)

> **Задача:** проверить понимание структуры. **Ответы текстом.**

1. Из каких **двух частей** состоит CNN? Что делает каждая?
2. Что делает `Conv2d`, а что `MaxPool2d`?
3. Почему по ходу сети **размер падает**, а **каналы растут**?
4. Зачем в конце «вытягивают» признаки (`view`) и ставят `Linear`?

**🤔 Вопросы на размышление:**

1. Почему свёрточную часть называют «глаза», а полносвязную — «мозг» сети?
2. Что было бы, если убрать свёртки и оставить только `Linear`? (на что станет похоже — вспомни 5.8)
3. Зачем **два** свёрточных слоя, а не один? (вспомни иерархию: края → детали)

---

### 1.2. Посчитай размеры (без кода!)

> **Задача:** проследить, как меняется картинка. **Расчёты в Markdown.**

Картинка входит 28×28×1. Проследи размер и каналы после каждого шага сети из практики:
1. После `conv1` (16 фильтров, padding=1) → размер? каналов?
2. После `pool` → размер? каналов?
3. После `conv2` (32 фильтра) → размер? каналов?
4. После `pool` → размер? каналов?
5. После `view` → сколько чисел в строке?

> **Подсказка:** `padding=1` сохраняет размер (свёртка не уменьшает). Пулинг 2×2 делит размер пополам. Каналы = число фильтров слоя.

**🤔 Вопросы:**

1. Сколько раз картинка уменьшилась (от 28 до скольких)?
2. Откуда берётся число `32*7*7` в `fc1`? Объясни каждый множитель.
3. Если бы пулингов было 3 (а не 2), какой стал бы размер? (28 → ... → ... → ...)

---

## Уровень 2. Свои эксперименты

### 2.1. Измени архитектуру

> **Задача:** поэкспериментировать со своей CNN на MNIST.

**Что нужно сделать:**
1. Возьми CNN из практики.
2. Попробуй изменения (по одному):
   - больше фильтров (`conv1`: 1→32, `conv2`: 32→64) — не забудь поправить `fc1`!
   - **третий** свёрточный слой;
   - больше эпох (5–8).
3. Сравни точность.

> **Подсказка:** если меняешь число каналов/слоёв — изменится размер перед `fc1`. Если ошибёшься — PyTorch напишет ожидаемое число в ошибке, поправь его в `nn.Linear`.

**🤔 Вопросы:**

1. Какое изменение дало больше всего? До скольких % дошёл?
2. Помогает ли бесконечно увеличивать фильтры/слои? Где предел? (переобучение, скорость)
3. Заметил, что менять архитектуру CNN в PyTorch легко? Что PyTorch делает за тебя? (backward!)

---

### 2.2. Где CNN ошибается

> **Задача:** найти, на каких цифрах ошибается твоя CNN, и сравнить с MLP.

**Что нужно сделать:**
1. Найди картинки теста, где CNN ошиблась (как в 5.8 для MLP).
2. Посмотри на них — трудные ли это цифры?
3. (Если есть MLP из 5.8) сравни: на скольких ошибается каждая?

> **Подсказка:** код поиска ошибок — как в 5.8. Сравни число ошибок CNN и MLP на одном тесте.

**🤔 Вопросы:**

1. На каких цифрах CNN ошибается? Объективно ли они трудные?
2. Меньше ли ошибок у CNN, чем у MLP? Во сколько раз?
3. Почему даже хорошая CNN иногда ошибается? (что в данных)

---

## Уровень 3. Со звёздочкой

### 3.1. Загляни в фильтры CNN

> **Задача:** увидеть, что реально выучила сеть.

**Что нужно сделать:**
1. У обученной CNN возьми веса первого слоя: `net.conv1.weight`.
2. Узнай их форму (сколько фильтров, какого размера).
3. Нарисуй несколько фильтров как картинки 3×3.

> **Подсказка:** `net.conv1.weight.shape` → `[16, 1, 3, 3]` (16 фильтров 3×3). Каждый `net.conv1.weight[i, 0]` — фильтр, покажи через `imshow`. Это и есть «детекторы узоров», которые сеть выучила сама!

**🤔 Вопросы:**

1. Какой формы веса первого слоя? Сколько там фильтров?
2. Похожи ли выученные фильтры на наши ручные (края)? (могут быть похожи на детекторы краёв/пятен)
3. Что это доказывает про главную идею 6.2 («сеть учит фильтры сама»)?

---

### 3.2. CNN на Fashion-MNIST

> **Задача:** проверить CNN на более сложных картинках (одежда вместо цифр).

**Что нужно сделать:**
1. Загрузи `datasets.FashionMNIST` (та же форма 28×28, но это одежда: футболки, кроссовки...).
2. Обучи **ту же** CNN (менять не надо — тоже 10 классов!).
3. Сравни точность с MNIST.

> **Подсказка:** `datasets.FashionMNIST(root='./data', train=True, download=True, transform=transform)`. Архитектура CNN та же. Fashion-MNIST труднее цифр → точность будет ниже (~90%).

**🤔 Вопросы:**

1. Какая точность на Fashion-MNIST? Ниже, чем на цифрах? Почему сложнее?
2. Пришлось ли менять **архитектуру** CNN, или подошла та же? (это сила переиспользуемого кода)
3. Где сеть путается на одежде? (похожие вещи — рубашка/футболка?)

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Понимает конвейер CNN (признаки + классификатор) | |
| Собрал рабочую CNN на PyTorch | |
| Обучил на MNIST, получил ~98–99% | |
| Понимает «размер ↓, каналы ↑» | |
| **Уровень 2:** эксперимент с архитектурой | |
| **Ответил на 🤔 вопросы размышления** | |

> **Минимум для «зачёта»:** рабочая CNN на MNIST (~98%+) + одна задача Уровня 2 + **ответы на вопросы**.
>
> **Без ответов на вопросы — задача не считается решённой.**
> **Главное — собрать CNN и понять, почему свёртки видят лучше MLP.**

---

> **Ты построил настоящую свёрточную сеть — "глаза" для ИИ!** И она побила обычную сеть на тех же цифрах. Это та самая архитектура, которой видят беспилотники, телефоны и медицинские сканеры. На следующем уроке возьмём задачу посложнее — **цветные** картинки реальных объектов (CIFAR-10): самолёты, машины, кошки, собаки. Там свёртки покажут себя по-настоящему! 🚗🐱🔥