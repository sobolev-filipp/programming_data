# Урок 6.2. Самостоятельная работа на уроке

> **Как всегда:** в подсказках **мало готового кода**. Я даю **направление** — думать твоя задача.
>
> После каждого задания — **🤔 вопросы на размышление**. Записывай ответы в Markdown-ячейку.
>
> Тетрадка: продолжай `lesson-6-2-conv.ipynb`. Заголовок `## Самостоятельная работа`.

> **Главное правило урока:** свёртка = фильтр бежит по картинке и считает взвешенную сумму. Числа в фильтре = какой узор он ищет. Сеть **учит** фильтры сама.

---

## Шпаргалка (из практики)

```python
from scipy.signal import convolve2d
result = convolve2d(gray, filter_3x3, mode='valid')   # свёртка
plt.imshow(np.abs(result), cmap='gray')               # показать (модуль)

# фильтры
edge_v = [[-1,0,1],[-2,0,2],[-1,0,1]]    # вертик. края
edge_h = [[-1,-2,-1],[0,0,0],[1,2,1]]    # гориз. края
blur   = np.ones((3,3))/9                 # размытие
sharp  = [[0,-1,0],[-1,5,-1],[0,-1,0]]    # резкость

def max_pool(img, size=2):
    h=img.shape[0]//size*size; w=img.shape[1]//size*size
    return img[:h,:w].reshape(h//size,size,w//size,size).max(axis=(1,3))
```

---

## Уровень 1. Понять свёртку

### 1.1. Свёртка руками (без кода!)

> **Задача:** посчитать одно значение свёртки на бумаге.

Дан фильтр и кусочек картинки 3×3:
```
Фильтр:       Картинка:
0 1 0          10 20 30
1 0 1          40 50 60
0 1 0          70 80 90
```

**Посчитай** результат свёртки в этой точке: перемножь совпадающие клетки и сложи.

> **Подсказка:** только там, где в фильтре **1**, пиксель учитывается. Где **0** — нет. Сложи нужные пиксели.

**🤔 Вопросы на размышление:**

1. Какие пиксели «вошли» в результат, а какие нет? Почему (что в фильтре)?
2. Чем свёртка похожа на **нейрон** (`w·x`)? Что тут «веса», а что «вход»?
3. Если фильтр маленький (3×3), а картинка большая (500×500) — сколько раз фильтр «приложится» к картинке (примерно)?

---

### 1.2. Главная идея CNN (без кода!)

> **Задача:** объяснить прорыв свёрточных сетей. **Ответы текстом.**

1. Раньше фильтры (краёв, углов) **придумывали руками**. В чём была проблема такого подхода?
2. Что делает свёрточная сеть **вместо** этого? Как она получает фильтры?
3. Опиши «иерархию признаков»: что находят первые слои, что последние?

**🤔 Вопросы:**

1. Откуда сеть «знает», какие фильтры ей нужны для распознавания котов? (никто ведь не говорит «ищи усы»)
2. Чем свёртка **лучше** «вытягивания» картинки в строку (как было на MNIST)? Назови 2 причины.
3. Почему CNN на MNIST даёт 99%, а наша MLP давала 97%? Что свёртка «понимает», чего не понимала MLP?

---

## Уровень 2. Свои эксперименты

### 2.1. Придумай свой фильтр

> **Задача:** поэкспериментировать с числами фильтра и увидеть эффект.

**Что нужно сделать:**
1. Возьми серую картинку.
2. Попробуй **свои** фильтры 3×3 (меняй числа):
   - все одинаковые положительные (что будет?);
   - фильтр диагональных краёв (придумай);
   - сильная резкость (центр большой, вокруг отрицательные).
3. Покажи результаты, опиши, что находит каждый.

> **Подсказка:** экспериментируй! Например, диагональный край: `[[-2,-1,0],[-1,0,1],[0,1,2]]`. Смотри, какие линии выделяются.

**🤔 Вопросы:**

1. Какой эффект дал фильтр из одинаковых чисел? (на что похоже)
2. Удалось ли сделать фильтр для **диагональных** краёв? Какие линии он выделил?
3. Что объединяет все фильтры «краёв»? (подсказка: сумма чисел фильтра ≈ 0)

---

### 2.2. Свёртка + пулинг по очереди

> **Задача:** повторить «конвейер» CNN — свёртка, потом пулинг.

**Что нужно сделать:**
1. Возьми картинку, примени фильтр краёв (свёртка).
2. К результату примени **max pooling** 2×2.
3. Покажи все три: оригинал → края → края+пулинг.
4. Сравни размеры.

> **Подсказка:** это мини-версия того, что делает CNN: свёртка находит узор, пулинг уменьшает. В настоящей сети это повторяется много раз.

**🤔 Вопросы:**

1. Как изменился размер после свёртки? После пулинга?
2. Сохранились ли края после пулинга? Что потерялось?
3. Зачем чередовать свёртку и пулинг? (что даёт каждая операция)

---

## Уровень 3. Со звёздочкой

### 3.1. Несколько фильтров = несколько карт признаков

> **Задача:** показать, что в CNN на одном слое **много** фильтров.

**Что нужно сделать:**
1. Возьми картинку.
2. Примени **4 разных** фильтра (края верт./гориз., резкость, размытие).
3. Покажи все 4 результата вместе — это «**карты признаков**» одного слоя CNN.
4. Объясни: каждый фильтр «видит» своё.

> **Подсказка:** в реальной CNN первый свёрточный слой имеет 32–64 фильтра, каждый даёт свою карту признаков. Мы делаем 4 — для наглядности.

**🤔 Вопросы:**

1. Чем отличаются 4 карты признаков? Что «увидел» каждый фильтр?
2. Если у слоя 64 фильтра — сколько карт признаков он создаст из одной картинки?
3. Зачем сети **много** фильтров, а не один? (что даёт разнообразие)

---

### 3.2. Сравни цифру MNIST: MLP-взгляд vs свёртка

> **Задача:** понять, почему свёртка лучше для картинок.

**Что нужно сделать:**
1. Возьми одну цифру MNIST (или нарисуй цифру 28×28).
2. **«Вытяни» её в строку 784** (как делала MLP) — и подумай: видны ли тут «соседние» пиксели?
3. Примени к той же цифре **фильтр краёв** (свёртка) — увидишь контур цифры.
4. Сравни два «взгляда».

> **Подсказка:** при вытягивании пиксель [0][0] и [0][1] (соседи!) оказываются в строке рядом, а [0][0] и [1][0] (тоже соседи по вертикали!) — далеко (на 28 позиций). Свёртка видит обоих соседей.

**🤔 Вопросы:**

1. После «вытягивания» сохраняется ли информация, что два пикселя были **соседями по вертикали**?
2. Свёртка смотрит на окошко — видит ли она вертикальных соседей? Почему это важно для формы цифры?
3. Сделай вывод: почему для **картинок** свёртка подходит лучше, чем «вытягивание»?

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Понимает свёртку (фильтр бежит, считает сумму) | |
| Понимает, что числа фильтра = искомый узор | |
| Знает главную идею: **сеть учит фильтры сама** | |
| Понимает пулинг (уменьшить, взять максимум) | |
| **Уровень 2:** свой фильтр / свёртка+пулинг | |
| **Ответил на 🤔 вопросы размышления** | |

> **Минимум для «зачёта»:** Уровень 1 (обе задачи) + одна задача Уровня 2 + **ответы на вопросы**.
>
> **Без ответов на вопросы — задача не считается решённой.**
> **Главное — понять свёртку и то, что сеть учит фильтры сама.**

---

> **Свёртка — самая важная идея компьютерного зрения, и ты её понял!** Маленький фильтр, бегущий по картинке, ищет узор — а сеть учит тысячи таких фильтров сама. На следующем уроке мы соберём из свёрток и пулинга **настоящую свёрточную сеть (CNN)** и обучим её — она будет видеть в разы лучше нашей старой MLP! 👁️🔥