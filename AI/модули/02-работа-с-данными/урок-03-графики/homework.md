# Урок 2.3. Самостоятельная работа на уроке

> **В этом задании ты сам строишь графики и читаешь их.** Я даю **постановку**, ты пишешь код. После каждой задачи — **вопросы на размышление** (главное!).
>
> Тетрадка: продолжай `lesson-2-3-graphics.ipynb`. Раздели: `## Самостоятельная работа`.

> **Главное правило:** у каждого графика — **обязательно** `title` и `xlabel/ylabel`. График без подписей — это не график.

---

## Шпаргалка (минимум команд)

```python
import matplotlib.pyplot as plt
import seaborn as sns
sns.set_theme()

# Базовые
plt.plot(x, y) / plt.scatter(x, y) / plt.bar(c, v) / plt.hist(d, bins=30)

# Настройка
plt.figure(figsize=(10, 5))
plt.title("..."), plt.xlabel("..."), plt.ylabel("...")
plt.legend(), plt.grid()
plt.savefig('file.png', dpi=150)  # ДО show()!
plt.show()

# Subplots
fig, axes = plt.subplots(2, 2, figsize=(10, 8))
axes[0, 0].plot(x, y)
axes[0, 0].set_title("...")

# Seaborn
sns.heatmap(df.corr(), annot=True, cmap='coolwarm', center=0)
sns.scatterplot(data=df, x='col1', y='col2', hue='категория')
sns.countplot(data=df, x='col', hue='категория')
sns.histplot(data=df, x='col', kde=True)
```

---

## Уровень 1. Базовые графики

> Используй очищенный Titanic с урока.

### 1.1. Гистограмма цен билетов

> **Задача:** распределение `Fare` с 40 столбиками и **подписями**.

**Подсказки:**
- `bins=40` — число столбиков.
- `edgecolor='black'` — контур.
- Не забудь `title, xlabel, ylabel`.

**🤔 Вопросы:**

1. Какое **«типичное» значение** цены — около какой суммы большинство билетов?
2. **Сколько процентов** билетов стоят больше 100? Можешь оценить по графику.
3. Гистограмма **«скошена вправо»** или **«симметрична»**? Что это значит про среднее и медиану (вспомни Урок 1.6)?

---

### 1.2. Столбики по полу

> **Задача:** показать, сколько мужчин и женщин.

**2 способа на выбор:**
- `sns.countplot(data=df, x='Sex')` — Seaborn делает сам.
- Или через `value_counts()` + `plt.bar`.

**🤔 Вопросы:**

1. **Сначала предскажи**: больше мужчин или женщин? **Во сколько раз**?
2. Если бы выживаемость одинаково зависела от пола — у кого было бы больше **абсолютное число выживших**? Объясни.
3. Сравни 2 способа: какой проще? Какой даёт больше контроля над стилем?

---

### 1.3. Scatter возраст vs цена

> **Задача:** построить точечный график `(Age, Fare)`.

**Подсказки:**
- `alpha=0.5` — прозрачность. Без неё точки сольются.
- Подписи осей **обязательны**.

**🤔 Вопросы:**

1. Видна ли **связь** между возрастом и ценой? Какая корреляция (примерно)?
2. **Что особенного** в области `Age = 0..10`? (есть ли там очень дорогие билеты?)
3. **Сколько примерно «выбросов»** ты видишь — точек с очень большой ценой (>200)?

---

## Уровень 2. Сравнение и Seaborn

### 2.1. Гистограммы цены по полу

> **Задача:** построить **две** гистограммы на одном графике — цены билетов мужчин и женщин.

**Подсказки:**
- Раздели данные: `df[df['Sex'] == 'male']['Fare']` и аналогично для `female`.
- `alpha=0.6` — прозрачность.
- `label=...` + `plt.legend()`.

**После графика** распечатай **средние** мужчин и женщин.

**🤔 Главные вопросы:**

1. **Сначала предскажи**: кто платил больше — мужчины или женщины? Объясни логически.
2. Потом проверь — **совпало**? Если женщины платили больше — почему?
3. Какое **открытие** этот график говорит про **выбор класса** между мужчинами и женщинами на Titanic?

---

### 2.2. Subplots — 2×2 разных графика

> **Задача:** сделать фигуру **2×2** с четырьмя графиками:

1. Гистограмма возраста.
2. Гистограмма цены.
3. Столбики — число пассажиров в каждом классе.
4. Scatter — Age vs Fare.

**Подсказки:**
- `fig, axes = plt.subplots(2, 2, figsize=(12, 8))`.
- `axes[i, j]` — обращение.
- `set_title`, `set_xlabel`, `set_ylabel` — у subplots **методы с set_**.
- В конце — `plt.tight_layout()`.

**🤔 Вопросы:**

1. Какой график **самый информативный** в твоём отчёте? Почему?
2. Если бы не было `plt.tight_layout()`, что произошло бы? Попробуй убрать и посмотри.
3. Хотел бы ты **добавить пятый график** — какой и зачем?

---

### 2.3. Heatmap корреляций

> **Задача:** построить тепловую карту корреляций числовых столбцов.

**Подсказки:**
- Выбери числовые столбцы: `['Survived', 'Pclass', 'Age', 'SibSp', 'Parch', 'Fare']`.
- `.corr()` — матрица корреляций.
- `sns.heatmap(..., annot=True, cmap='coolwarm', center=0, fmt='.2f')`.

**🤔 Главные вопросы:**

1. **Какие 2 пары** признаков связаны **сильнее всего** (по модулю)?
2. Какие признаки сильнее всего **коррелируют с `Survived`** (целевой переменной)? Что это означает для ML?
3. **Сначала предскажи**: какая корреляция между `Age` и `Survived` — положительная, отрицательная или нулевая? Потом проверь.

---

## Уровень 3. Со звёздочкой

### 3.1. Iris — pairplot

> **Задача:** загрузить Iris и построить **сетку всех пар** через `sns.pairplot`.

**Загрузка:** `iris = pd.read_csv("https://raw.githubusercontent.com/mwaskom/seaborn-data/master/iris.csv")`.

**Что нужно:**
```python
sns.pairplot(iris, hue='species')
plt.show()
```

**🤔 Главные вопросы:**

1. **Какие 2 признака** лучше всего разделяют **виды**? (Где облака точек 3 цветов не перекрываются?)
2. **Какие 2 признака** хуже всего разделяют? (Где все 3 цвета смешиваются?)
3. Это знание **пригодится в ML**: какой простейший классификатор сработает на Iris по petal_length и petal_width? **Дерево решений или KNN**?

---

### 3.2. Свой график-отчёт

> **Задача:** сделать **2×2 отчёт** для Titanic с акцентом на выживание.

**Что должно быть:**

1. Гистограмма возраста — выжил vs не выжил (две гистограммы с alpha).
2. Гистограмма цены — выжил vs не выжил.
3. `countplot` — выживаемость по классам.
4. `scatterplot` — Age vs Fare с цветом по Survived.

**В конце:** `plt.savefig('titanic_report.png', dpi=150, bbox_inches='tight')`.

**🤔 Вопросы:**

1. **Какие 3 главных вывода** ты можешь сделать по этим 4 графикам? Запиши в Markdown.
2. **Какой признак** кажется **самым важным** для предсказания `Survived`? Объясни визуально.
3. Если бы ты был **аналитиком** и тебе нужно было показать этот отчёт начальнику — добавил бы ты ещё что-то? Что?

---

### 3.3. Тренд (regplot)

> **Задача:** на Iris построить `sns.regplot` для двух пар признаков и сравнить.

**Пары:**
- `sepal_length` vs `sepal_width`.
- `petal_length` vs `petal_width`.

**🤔 Вопросы:**

1. У какой пары линия **круче** идёт вверх? Это означает сильную связь — какую корреляцию?
2. У какой пары линия **почти горизонтальная**? Это означает что — связи нет или связь слабая?
3. Подтверди догадку: посчитай корреляции через `iris[['sepal_length', 'sepal_width']].corr()` и аналогично для petal. **Совпало**?

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Каждый график имеет title и подписи | |
| Использует разные типы графиков уместно | |
| Применил Seaborn (heatmap или scatterplot) | |
| **Ответил на вопросы размышления** | |
| **Уровень 3:** pairplot или сохранение в файл | |

**Минимум:** все 3 задачи Уровня 1 + **ответы на вопросы**.

> **Главное сегодня — научиться ВИДЕТЬ закономерности в данных.** Графики — это «очки», через которые невидимые числа становятся **видимыми историями**.
