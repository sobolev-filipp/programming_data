# Урок 2.3. Самостоятельная работа на уроке

> **Не домашка.** Решаешь **на уроке**, в оставшиеся ~9 минут.
> Тетрадка: продолжай `lesson-2-3-graphics.ipynb`. Раздели заголовком: `## Самостоятельная работа`.
> К концу занятия покажи преподавателю.

> **Совет:** **у каждого графика обязательно** title и подписи осей (xlabel, ylabel).

---

## Уровень 1. Базовые графики

Используй очищенный Titanic с урока.

### 1.1. Гистограмма цен билетов

Построй гистограмму `Fare` с 40 столбиками. Заголовок: «Распределение цен билетов». Подписи осей.

### 1.2. Столбики по полу

Сколько пассажиров мужчин и женщин? Построй `bar` или `sns.countplot`. Подписи.

### 1.3. Scatter возраст vs цена

`Age` по X, `Fare` по Y. С прозрачностью `alpha=0.5`. Подписи и заголовок.

---

## Уровень 2. Сравнение и Seaborn

### 2.1. Гистограммы цены по полу

Постройте **две гистограммы** на одной фигуре:
- цена билетов мужчин — синие
- цена билетов женщин — розовые

С прозрачностью `alpha=0.6`, легендой. Кто платил дороже?

### 2.2. Subplots — 2×2 разных графика

Создай фигуру **2×2** с четырьмя разными графиками:
1. Гистограмма возраста.
2. Гистограмма цены.
3. Bar — число пассажиров в каждом классе.
4. Scatter — Age vs Fare.

У каждого — свой заголовок.

### 2.3. Heatmap корреляций

Построй heatmap корреляций для столбцов `Survived, Pclass, Age, SibSp, Parch, Fare`. С подписями чисел.

Какие 2 пары признаков связаны сильнее всего?

---

## Уровень 3. Со звёздочкой

### 3.1. Iris — pairplot

Загрузи Iris:
```python
iris = pd.read_csv("https://raw.githubusercontent.com/mwaskom/seaborn-data/master/iris.csv")
```

Построй `sns.pairplot(iris, hue='species')`.

**Что видно?** Можно ли отделить **species** только по двум столбцам? Каким?

### 3.2. Свой график-отчёт

Сделай **«разворот отчёта»** — фигуру 2×2 для Titanic, где видно:
- Возраст выживших и погибших на одной гистограмме (с легендой).
- Цена выживших и погибших.
- Bar — выживаемость по классам (как `countplot(... hue='Survived')`).
- Scatter — Age vs Fare с цветом по `Survived`.

Каждый график с заголовком. Размер фигуры (12, 10).

**Сохрани в файл** через `plt.savefig('titanic_report.png', dpi=150)`.

### 3.3. Тренд

На датасете Iris построй `sns.regplot` для пары `sepal_length` и `sepal_width`. Видна ли связь?

Сравни это с `petal_length` и `petal_width`.

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Каждый график имеет title и подписи осей | |
| Использует разные типы графиков уместно | |
| Применил Seaborn хотя бы один раз (heatmap или scatter) | |
| Если есть Уровень 3 — pairplot или сохранение в файл | |

**Минимум:** все 3 задачи Уровня 1.
