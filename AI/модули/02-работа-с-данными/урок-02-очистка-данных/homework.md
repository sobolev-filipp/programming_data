# Урок 2.2. Самостоятельная работа на уроке

> **В этом задании ты сам чистишь Titanic.** Я даю **постановку**, ты пишешь код. После каждой задачи — **вопросы на размышление**.
>
> Тетрадка: продолжай `lesson-2-2-cleaning.ipynb`. Раздели: `## Самостоятельная работа`.

> **Главное правило:** после `fillna` — **присвой обратно**! `df['col'] = df['col'].fillna(...)`, иначе изменения не сохранятся.

---

## Шпаргалка (минимум команд)

```python
# Найти пропуски
df.isna().sum()                 # число
df.isna().sum() / len(df) * 100 # в процентах

# Удалить
df = df.dropna()                            # все строки с любым NaN
df = df.dropna(subset=['col'])              # только если NaN в col
df = df.drop(columns=['col'])               # столбец

# Заполнить
df['col'] = df['col'].fillna(df['col'].mean())
df['col'] = df['col'].fillna(df['col'].median())
df['col'] = df['col'].fillna(df['col'].mode()[0])

# Дубликаты
df.duplicated().sum()
df = df.drop_duplicates()

# Apply
df['new'] = df['col'].apply(func)              # к каждому элементу
df['new'] = df.apply(lambda row: ..., axis=1)  # к каждой строке
```

---

## Уровень 1. Обязательная база

### 1.1. Анализ пропусков

> **Задача:** загрузить **свежий** Titanic и посчитать пропуски в каждом столбце.

**URL:** `https://raw.githubusercontent.com/datasciencedojo/datasets/master/titanic.csv`

**Что нужно:**

1. Форма таблицы.
2. Полная сводка через `info()`.
3. **Число** пропусков в каждом столбце.
4. **Доля** пропусков (в процентах, округли до 2 знаков).

**Подсказка:** называй переменную `df_raw`, чтобы не путать с очищенным `df` с урока.

**🤔 Вопросы:**

1. **Сначала предскажи**: в каком столбце больше всего пропусков? Сколько? Потом проверь — совпало?
2. Какие 3 столбца имеют пропуски? У какого процент критичный (>50%)?
3. Если бы ты **удалил все строки с любым NaN** через `dropna()` — сколько строк осталось бы? **Сначала предскажи**, потом проверь через `df_raw.dropna().shape`.

---

### 1.2. Очистка

> **Задача:** примени **3 разные стратегии** для 3 разных столбцов.

**Решения:**

1. **Cabin** (~77% пропусков) → **удалить** столбец.
2. **Age** (~20%) → **заполнить медианой**.
3. **Embarked** (2 пропуска) → **заполнить модой**.

**После очистки** проверь, что `df_raw.isna().sum()` везде нули.

**🤔 Вопросы:**

1. **Почему именно эти стратегии**? Почему Cabin удаляем, а Age заполняем?
2. **Сначала предскажи**: медианный возраст ≈ 28 или ≈ 40? Какое было бы среднее? Какая разница и почему?
3. Что произойдёт, если ты **забудешь** присвоить результат обратно (`df_raw['Age'].fillna(...)` без `df_raw['Age'] = ...`)? Изменится ли датасет?

---

### 1.3. Новый признак

> **Задача:** добавить столбец `family_size` (размер семьи на борту).

**Формула:** `family_size = SibSp + Parch + 1` (+1 — сам пассажир).

**Что распечатать:**

1. Среднее.
2. Максимум.
3. Распределение `value_counts().sort_index()`.

**🤔 Вопросы:**

1. **Сколько пассажиров плыли одни** (family_size = 1)? Какая это доля от общего?
2. Кто-то плыл с **очень большой семьёй** (5+)? Сколько таких?
3. **Какое значение** family_size имеет **максимальную выживаемость**? Сначала предскажи. Потом посчитай:
   ```python
   df_raw.groupby('family_size')['Survived'].mean()
   ```

---

## Уровень 2. `.apply()` и категории

### 2.1. Группы цен

> **Задача:** превратить **число** (цена билета) в **категорию** через `.apply()`.

**Категории:**
- `< 10` → `'cheap'`
- `10 ≤ fare < 50` → `'medium'`
- `≥ 50` → `'expensive'`

**Что сделать:**

1. Напиши функцию `fare_category(fare)`.
2. Применяя через `.apply()`, создай столбец `fare_category`.
3. Посчитай через `value_counts()` сколько каждой категории.

**🤔 Вопросы:**

1. **Сначала предскажи**: какая категория самая многочисленная? Почему?
2. Что произойдёт, если ты напишешь `elif fare > 50` (вместо `else`)? Будет ли категория для `fare == 50` ровно?
3. Можно ли решить эту задачу **без `.apply()`** — через `pd.cut`? Если знаешь — попробуй.

---

### 2.2. «Семейный» индикатор

> **Задача:** создать столбец `is_alone` и посчитать **разницу в выживаемости**.

**Что сделать:**

1. `df_raw['is_alone'] = df_raw['family_size'] == 1` (булев столбец).
2. Посчитай **долю выживших** среди одиночек и среди семейных.

**Подсказка:** для одиночек — `df_raw[df_raw['is_alone'] == True]['Survived'].mean()`. Для семейных — наоборот.

**🤔 Главные вопросы:**

1. **У кого выживаемость выше — у одиночек или у семейных**? **Сначала предскажи** (как ты думаешь — почему?), потом проверь.
2. **На сколько процентов** выживаемость отличается? Это **большая** разница?
3. Какие **2-3 причины** ты можешь предположить, объясняющие эту разницу?

---

### 2.3. Титул

> **Задача:** извлечь **титул** (Mr., Mrs., Miss., Master.) из имени.

**Пример:** `"Braund, Mr. Owen Harris"` → `"Mr."`.

**Что сделать:**

1. Напиши функцию `get_title(name)`.
2. Применяя через `.apply()`, создай столбец `title`.
3. Подсчёт через `value_counts()`.

**Подсказка по парсингу:**
- `name.split(', ')` → `['Braund', 'Mr. Owen Harris']`.
- Берёшь `[1]` → `'Mr. Owen Harris'`.
- Снова `.split('. ')` → `['Mr', 'Owen Harris']`.
- Берёшь `[0]` + добавь точку → `'Mr.'`.

**🤔 Вопросы:**

1. Какие **3 самых редких титула** в датасете? Назови.
2. Какой титул имеет **самую высокую выживаемость**? Проверь:
   ```python
   df_raw.groupby('title')['Survived'].mean().sort_values(ascending=False)
   ```
3. **Master.** — это **мальчик до 13 лет**. Какая у него выживаемость? Почему она высокая (вспомни «дети первыми»)?

---

## Уровень 3. Со звёздочкой

### 3.1. Возрастная пирамида

> **Задача:** разбить возраст на **десятилетия** (0, 10, 20, 30, ...).

**Подсказка:**
- `// 10` — целочисленное деление. `28 // 10 = 2`.
- `× 10` — округление до 10-летий. `2 × 10 = 20`.

**Что сделать:**

1. Создай столбец `age_decade`.
2. Преобразуй к `int` через `.astype(int)`.
3. Посчитай `value_counts().sort_index()`.

**🤔 Вопросы:**

1. **Сколько детей** (0-9 лет) было на Titanic?
2. **Какое десятилетие** самое многочисленное?
3. Что произойдёт, если ты напишешь `Age / 10` (одна слэша) вместо `// 10`? Чем результат будет отличаться?

---

### 3.2. Заполнить возраст по группам

> **Задача:** **умнее** заполнить пропуски в Age — медианой **по полу**.

**Идея:** мужчины и женщины разного возраста. Лучше заполнить **каждым своим средним**.

**Что сделать:**

1. Загрузи **свежий** Titanic.
2. Через `groupby('Sex')['Age'].median()` получи медианы.
3. Через `.apply(axis=1)` для каждой строки: если NaN — взять медиану по полу. Иначе — оставить.

**Подсказка:** `pd.isna(row['Age'])` — **правильный способ** проверки на NaN. **НЕ** `row['Age'] == NaN`!

**🤔 Главные вопросы:**

1. Какая медиана возраста у **мужчин**? У **женщин**? Большая ли разница?
2. **Почему `row['Age'] == NaN` не работает**? Что вернёт это сравнение?
3. Сравни **простое заполнение медианой** (как было в 1.2) и **заполнение по полу**. Какое **точнее** и почему?

---

### 3.3. Найти «странные» цены

> **Задача:** найти пассажиров с `Fare == 0` и посмотреть, кто они.

**Что нужно:**

1. Сколько таких пассажиров?
2. Какой у них класс билета? (`value_counts()` по `Pclass`).

**🤔 Вопросы:**

1. **Кто это мог быть**? Назови 2-3 гипотезы.
2. В реальном проекте — что бы ты с ними **сделал**: оставил? удалил? заменил среднему?
3. Если бы ML-модель **училась** на этих данных — повлияли бы эти «нулевые» цены на качество модели? Как?

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Работает с `isna().sum()` | |
| Заполняет NaN осмысленно | |
| Использует `.apply()` правильно | |
| Создаёт новые признаки | |
| **Уровень 3:** понимает специфику NaN (`pd.isna()`) | |
| **Ответил на вопросы размышления** | |

**Минимум:** все 3 задачи Уровня 1 + **ответы на вопросы**.

> **Главное открытие сегодня — задача 2.2.** Если ты понял, **почему семейные выживают чаще одиночек** — ты сделал свой **первый аналитический вывод** по данным.
