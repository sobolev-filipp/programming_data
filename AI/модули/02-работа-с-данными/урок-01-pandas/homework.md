# 📝 Самостоятельная работа · Урок 2.1 «Pandas»

**Темы:** DataFrame и Series, просмотр (`head`/`info`/`describe`), выбор столбцов и строк (`loc`/`iloc`), фильтры (`&`/`|`, `.isin`), сортировка (`sort_values`), новый столбец, `value_counts`, `groupby`.

> 🎯 **Как работать:** решай по порядку и **запускай после каждой задачи**. Уровни: 🟢 базовые (обязательно) · 🟡 средние · 🔴 со звёздочкой. В каждой задаче сказано, **что сделать и что должно получиться**. Подсказка 💡 — наводка на идею, **не готовый код**, и есть не у каждой задачи.
>
> 📌 Правило дня: для фильтров — **`&` и `|`** (НЕ `and`/`or`), и каждое условие **в скобках**. Тетрадка та же — `lesson-2-1-pandas.ipynb`, раздел `## Самостоятельная работа`.

**Сначала создай таблицу — с ней работаем весь урок:**
```python
import pandas as pd
df = pd.DataFrame({
    "name":  ["Иван","Мария","Пётр","Ольга","Алина","Никита","Дарья","Егор"],
    "class": ["9А","9А","9Б","9Б","9А","9В","9В","9Б"],
    "age":   [15, 14, 15, 16, 14, 15, 16, 15],
    "math":  [4, 5, 3, 5, 4, 3, 5, 4],
    "phys":  [3, 5, 4, 4, 5, 3, 4, 5],
    "rus":   [5, 5, 4, 4, 5, 4, 3, 4],
})
```

---

## Шпаргалка

```python
df.shape, df.columns              # форма, имена столбцов (без скобок!)
df.head(), df.describe()          # просмотр, статистика
df['math']                        # один столбец   |   df[['name','math']] — несколько
df.iloc[0]                        # строка по номеру   |   df.loc[0,'name'] — ячейка
df[df['age'] > 14]                # фильтр
df[(df['age']>=15) & (df['phys']>=4)]   # два условия — в скобках, через &
df['class'].value_counts()         # счётчик значений
df.sort_values('age', ascending=False)  # сортировка
df.groupby('class')['math'].mean() # статистика по группам
```

---

## 🟢 Уровень 1 — базовый (просмотр, выбор, фильтры, сортировка)

**1. Размер таблицы.** Выведи форму таблицы (строки, столбцы) и список имён столбцов. Должно быть `(8, 6)` и 6 названий.
> 💡 `shape` и `columns` — это атрибуты, пишутся **без** скобок.

**2. Первый взгляд.** Выведи первые 5 строк (`head`) и сводную статистику по числовым столбцам (`describe`).

**3. Выбор столбцов.** Выведи отдельно столбец `math`, а потом два столбца сразу — `name` и `math`.
> 💡 Один столбец — одинарные скобки, несколько — двойные `df[['a','b']]`.

**4. Выбор строк.** Выведи первую строку таблицы (по номеру) и отдельно — имя ученика из строки с индексом 3.

**5. Сколько в каждом классе.** Посчитай, сколько учеников в каждом классе. Ожидаемо: 9А — 3, 9Б — 3, 9В — 2.

**6. Фильтр по возрасту.** Выведи всех, кому 14 лет. Должно найтись двое (Мария, Алина).

**7. Фильтр по оценке.** Выведи всех, у кого пятёрка по математике.

**8. Два условия.** Выведи учеников, которым **15 лет или больше** И у кого физика **≥ 4**.
> 💡 «И» — это `&`, каждое условие в скобках; `and` тут не сработает.

**9. Класс из списка.** Выведи всех, кто учится в 9А **или** 9Б (одной проверкой).
> 💡 Для «значение из списка» удобнее метод `.isin([...])`, чем два условия через `|`.

**10. Сортировка по возрасту.** Выведи таблицу, отсортированную по возрасту от старших к младшим. Проверь: сам `df` после этого не изменился.

**11. Сортировка по двум столбцам.** Отсортируй по `math` (по убыванию), а при равной математике — по `phys` (по убыванию).

---

## 🟡 Уровень 2 — средний (новый столбец, топы, группы)

**12. Средний балл.** Добавь столбец `avg` — средняя из `math`, `phys`, `rus`, округлённая до 2 знаков.
> 💡 Обязательно внешние скобки вокруг суммы: `(df['math'] + df['phys'] + df['rus']) / 3` — иначе деление применится только к `rus`.

**13. Топ-3 ученика.** Выведи трёх учеников с самым высоким `avg`, показав только столбцы `name`, `class`, `avg`.
> 💡 Отсортируй по `avg` по убыванию, возьми срез первых трёх и выбери нужные столбцы.

**14. Хорошисты.** Выведи учеников со средним `avg ≥ 4.5` и посчитай, сколько их.

**15. Троечники.** Выведи тех, у кого `avg < 3.5`, и посчитай, сколько их. Покажи столбцы `name` и `avg`.

**16. Средний по классам.** Через `groupby` посчитай средний `avg` в каждом классе. В каком классе он выше всего?

**17. Математика по классам.** *(на подумать)* Через `groupby` посчитай средний балл по математике в каждом классе и отсортируй результат по убыванию.

---

## 🔴 Уровень 3 — со звёздочкой (реальный CSV)

**18. Загрузи Iris.** Скачай датасет прямо в Colab и осмотри его:
```python
iris = pd.read_csv("https://raw.githubusercontent.com/mwaskom/seaborn-data/master/iris.csv")
```
Выведи его форму и посчитай, сколько цветов каждого вида (`species`).

**19. Лепестки по видам.** Через `groupby` посчитай среднюю длину лепестка (`petal_length`) для каждого вида. У какого вида лепестки самые длинные, у какого — самые короткие?
> 💡 `iris.groupby('species')['petal_length'].mean()`.

**20. Самый «нестабильный» класс.** *(на подумать)* Вернись к своему `df`. Для каждого класса посчитай разницу между максимальной и минимальной оценкой по математике (`max - min`). У какого класса разброс больше всего?
> 💡 Удобно через `groupby('class')['math']` и функции `.max()`/`.min()`.

**21. Почему `&`, а не `and`?** *(коротко в Markdown-ячейке)* Объясни одной фразой, почему в фильтрах Pandas условия соединяют через `&`/`|`, а не через `and`/`or`.

---

## Что проверит преподаватель

| Что | Зачёт ✅ |
|-----|----------|
| Выбирает столбцы и строки (`loc`/`iloc`) | |
| Фильтрует через `&`/`|` (не `and`/`or`) | |
| Сортирует через `sort_values` | |
| Создаёт новый столбец арифметикой | |
| **🔴:** применил `groupby` на реальном датасете | |

> ✅ **Сделал 🟢 — уверенно смотришь, фильтруешь и сортируешь таблицу. Осилил 🟡 — считаешь по группам. Взял 🔴 — исследуешь настоящий датасет.**
>
> **Минимум для зачёта:** 🟢 целиком + задачи 12–14 из 🟡.
