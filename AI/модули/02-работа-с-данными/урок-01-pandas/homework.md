# Урок 2.1. Самостоятельная работа на уроке: Pandas

> **Не домашка.** Эти задания решаешь **прямо на уроке**, в оставшиеся ~9 минут.
> Тетрадка: продолжай `lesson-2-1-pandas.ipynb`. Раздели заголовком: `## Самостоятельная работа`.
> К концу занятия покажи преподавателю свою тетрадку.

---

## Уровень 1. Обязательная база

Используй тот же `df` со студентами, что мы создали на уроке.

### 1.1. Описать данные

Распечатай:
1. Форму таблицы.
2. Имена всех столбцов.
3. Среднюю оценку по русскому (`rus`).
4. Сколько учеников в **каждом** классе.

### 1.2. Простые фильтры

Выведи только тех, у кого:
1. Возраст 14 лет.
2. Оценка по математике 5.
3. Возраст ≥ 15 **И** оценка по физике ≥ 4.
4. Класс — 9А **или** 9Б.

### 1.3. Сортировка

1. Отсортируй учеников по убыванию **возраста**.
2. Отсортируй по убыванию `math`, при равенстве — по убыванию `phys`.

---

## Уровень 2. Новый столбец + сортировка

### 2.1. Средняя оценка

Добавь столбец `avg` — средняя оценка по трём предметам (math + phys + rus) / 3. Округли до 2 знаков (`.round(2)`).

### 2.2. Топ-3 учеников

Через `sort_values` и срез `[:3]` выведи топ-3 учеников по `avg`.
Покажи только колонки `name`, `class`, `avg`.

### 2.3. Списки «хорошистов» и «троечников»

- «Хорошисты»: avg ≥ 4.5. Сколько их?
- «Троечники»: avg < 3.5. Сколько их? (если нет таких — это нормально)

---

## Уровень 3. Со звёздочкой

### 3.1. Реальный CSV из интернета

Загрузи **известный учебный датасет** Iris (про цветы):

```python
url = "https://raw.githubusercontent.com/mwaskom/seaborn-data/master/iris.csv"
iris = pd.read_csv(url)
iris.head()
```

Распечатай:
1. Форму таблицы.
2. Какие виды цветов (`species`) и сколько каждого (`value_counts`).
3. Среднюю длину лепестка (`petal_length`) **для каждого вида**.

> Подсказка: для последнего шага — отфильтруй по виду и посчитай среднее. Или попробуй `iris.groupby('species')['petal_length'].mean()`.

### 3.2. Найти самый «нестабильный» класс

В нашем `df` со студентами:
1. Для каждого класса посчитай **разницу** между максимальной и минимальной оценкой по `math`.
2. Найди класс с наибольшей разницей — это самый «разношёрстный».

Подсказка: попробуй `df.groupby('class')['math'].max()` минус `df.groupby('class')['math'].min()`.

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Умеет получать форму, столбцы, статистику | |
| Делает фильтры с `&`, `|`, `isin` правильно | |
| Сортирует через `sort_values` | |
| Добавил новый столбец `avg` через арифметику | |
| Если есть Уровень 3 — загрузил CSV из интернета | |

**Минимум для «зачёта»:** все 3 задачи Уровня 1.
