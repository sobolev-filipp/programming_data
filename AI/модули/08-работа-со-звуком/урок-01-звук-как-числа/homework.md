# Урок 8.1. Самостоятельная работа на уроке

> **Как всегда:** в подсказках **мало готового кода**. Я даю **направление** — думать твоя задача.
>
> После каждого задания — **🤔 вопросы на размышление**. Записывай ответы в Markdown-ячейку.
>
> Тетрадка: продолжай `lesson-8-1-sound-as-numbers.ipynb`. Заголовок `## Самостоятельная работа`.

> **Главное правило урока:** звук = массив чисел (замеры волны). Частота → высота, амплитуда → громкость, sample rate → сколько замеров в секунду.

---

## Шпаргалка (из практики)

```python
import numpy as np
from scipy.io import wavfile
from IPython.display import Audio

sr = 44100
t = np.linspace(0, dur, int(sr*dur), endpoint=False)
wave = np.sin(2*np.pi*freq*t)        # тон частоты freq
Audio(wave, rate=sr)                 # послушать
wavfile.write("file.wav", sr, (wave*16000).astype(np.int16))  # сохранить
sr2, data = wavfile.read("file.wav") # загрузить
```

---

## Уровень 1. Понять звук как числа

### 1.1. Теория своими словами (без кода!)

> **Задача:** проверить понимание. **Ответы текстом.**

1. Что такое звук физически? Как он превращается в числа?
2. Что такое **частота дискретизации (sample rate)**? В чём измеряется?
3. Что такое **отсчёт (сэмпл)**?
4. Чем **частота звука** отличается от **частоты дискретизации**? (две разные вещи!)
5. Чем **амплитуда** отличается от **частоты** (на слух)?

**🤔 Вопросы на размышление:**

1. Картинка — массив чисел (пиксели), звук — массив чисел (отсчёты). В чём **разница** в том, как эти числа расположены? (подсказка: сетка vs ...)
2. Почему для музыки берут 44 100 Гц, а для распознавания речи хватает 16 000? Что мы теряем при меньшем sample rate?
3. Назови **3 устройства/приложения**, которые внутри хранят звук как числа.

---

### 1.2. Посчитай (без кода, потом проверь)

> **Задача:** научиться считать размер аудио.

Посчитай **в уме/на бумаге**, потом проверь кодом (`len`):
1. Сколько чисел в **3 секундах** звука при sample rate **44 100**?
2. Сколько — в **10 секундах** при **16 000**?
3. Файл содержит **480 000** чисел при sample rate **48 000**. Сколько это **секунд**?

**🤔 Вопросы:**

1. Как из числа отсчётов и sample rate узнать **длительность**? (формула словами)
2. Почему запись с sample rate 44 100 «тяжелее» (больше чисел), чем 16 000, при той же длительности?

---

## Уровень 2. Свои эксперименты

### 2.1. Сыграй мелодию

> **Задача:** собрать мелодию из тонов.

**Что нужно сделать:**
1. Возьми частоты нот (примерно): До=262, Ре=294, Ми=330, Фа=349, Соль=392, Ля=440, Си=494.
2. Сгенерируй несколько нот по 0.4 сек и **склей** их в один массив (`np.concatenate`).
3. Послушай свою мелодию.

> **Подсказка:** сделай функцию `make_tone(freq, dur)` (как в практике). Собери список тонов, склей: `melody = np.concatenate([...])`. Послушай через `Audio(melody, rate=44100)`.

**🤔 Вопросы:**

1. Узнаётся ли мелодия на слух? Что будет, если поменять ноты местами?
2. Как сделать паузу между нотами? (подсказка: массив из нулей `np.zeros`)
3. Длительность ноты — это про что: частоту, амплитуду или **время**? (сколько отсчётов)

---

### 2.2. Сложи два тона (аккорд)

> **Задача:** услышать, что звуки **складываются**.

**Что нужно сделать:**
1. Сгенерируй два тона (например, 440 и 550 Гц).
2. **Сложи** их массивы (поэлементно) и послушай.
3. Нарисуй кусочек получившейся волны — она уже не простая синусоида!

> **Подсказка:** `both = tone1 + tone2`. Чтобы не было слишком громко/искажений, поделить на 2. Реальный звук (голос, музыка) — это **сумма многих** волн.

**🤔 Вопросы:**

1. Как звучит сумма двух тонов по сравнению с одним?
2. Похожа ли волна-сумма на простую синусоиду? Почему она сложнее?
3. Если любой звук — это сумма синусоид, то голос — это сколько синусоид? (много или одна?)

---

## Уровень 3. Со звёздочкой

### 3.1. Услышь плохое качество

> **Задача:** понять роль sample rate на слух.

**Что нужно сделать:**
1. Сгенерируй тон (или мелодию) при sample rate 44 100 — послушай.
2. Сделай то же при очень низком sample rate (например, 4 000 или 2 000) — послушай.
3. Сравни.

> **Подсказка:** меняй `sr` и в генерации (`linspace`), и в `Audio(..., rate=sr)`. При низком sample rate высокие звуки искажаются/пропадают.

**🤔 Вопросы:**

1. Как изменился звук при низком sample rate? Что «потерялось»?
2. Почему телефонный звонок звучит «глуше», чем музыка с CD? (связь с sample rate)
3. Есть ли смысл брать sample rate 1 000 000? Что это даст и чем плохо?

---

### 3.2. Сделай сирену или шум

> **Задача:** поиграть с необычными звуками.

**Что нужно сделать (выбери одно):**
- **Сирена:** частота, которая **плавно растёт и падает** со временем (частота зависит от `t`).
- **Шум:** массив случайных чисел (`np.random.uniform(-1, 1, ...)`) — послушай «шипение».

> **Подсказка для сирены:** сделай `freq` функцией времени, например `freq = 440 + 200*np.sin(2*np.pi*1*t)`, и подставь в формулу волны. Для шума — просто случайные числа вместо синуса.

**🤔 Вопросы:**

1. Как звучит твой звук? Похож на что-то из жизни?
2. Шум — это «упорядоченные» числа или случайные? Чем он отличается от тона?
3. Где в реальной жизни встречается шум, а где — чистые тоны?

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Понимает, что звук = массив чисел | |
| Различает sample rate и частоту звука | |
| Различает амплитуду (громкость) и частоту (высоту) | |
| Сгенерировал и послушал звук | |
| Сохранил/загрузил WAV | |
| **Уровень 2:** мелодия или аккорд | |
| **Ответил на 🤔 вопросы размышления** | |

> **Минимум для «зачёта»:** генерация + прослушивание звука + сохранение/загрузка WAV + одна задача Уровня 2 + **ответы на вопросы**.
>
> **Без ответов на вопросы — задача не считается решённой.**
> **Главное — понять: звук это массив чисел, и им можно управлять (частота, громкость, длительность).**

---

> **Ты понял, что звук — это массив чисел, и даже сделал звук своими руками!** Теперь компьютер «слышит» так же, как видит и читает — всё сводится к числам. Но смотреть на список из 88 000 чисел неудобно. На следующем уроке (8.2) мы научимся **рисовать** звук: строить **волну** и **осциллограмму**, чтобы видеть звук глазами. А там и до распознавания речи рукой подать! 🚀
