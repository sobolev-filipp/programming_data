# Урок 7.4. Самостоятельная работа на уроке

> **Как всегда:** в подсказках **мало готового кода**. Я даю **направление** — думать твоя задача.
>
> После каждого задания — **🤔 вопросы на размышление**. Записывай ответы в Markdown-ячейку.
>
> Тетрадка: продолжай `lesson-7-4-rnn-lstm.ipynb`. Заголовок `## Самостоятельная работа`.

> **Главное правило урока:** RNN/LSTM читают текст **по порядку** и **помнят** прочитанное (память = скрытое состояние). Мешок слов так не умеет.

---

## Шпаргалка (из практики)

```python
# Текст → последовательность номеров (порядок сохранён!)
def encode(s, length=30):
    ids = [vocab.get(w, 1) for w in tokenize(s)][:length]
    return ids + [0]*(length - len(ids))

# LSTM-классификатор
self.emb  = nn.Embedding(vocab_size, 32, padding_idx=0)
self.lstm = nn.LSTM(32, 64, batch_first=True)
self.fc   = nn.Linear(64, 2)
# forward: emb → lstm → h[-1] (память после последнего слова) → fc
```

---

## Уровень 1. Понять идею

### 1.1. Теория своими словами (без кода!)

> **Задача:** проверить понимание. **Ответы текстом.**

1. Почему мешок слов и эмбеддинги **теряют порядок** слов? Приведи пример, где это важно.
2. Как RNN читает текст? Что она делает на **каждом** слове?
3. Что такое **скрытое состояние** (память)? Что в нём хранится?
4. Зачем нужна **LSTM**, если есть простая RNN? (что чинит)
5. Где применяют RNN/LSTM? Назови 3 области.

**🤔 Вопросы на размышление:**

1. На практике мешок слов дал фразам «movie good not bad» и «movie bad not good» **одинаковый** вектор. Сможет ли RNN их различить **в принципе**? Почему?
2. Простая RNN «забывает» начало длинной фразы. Объясни, почему это плохо на примере длинного отзыва.
3. Чем «читать по порядку» (RNN) **похоже** на то, как читаешь ты сам(а)?

---

### 1.2. Сравни подходы (таблица, без кода)

> **Задача:** разложить по полочкам. Заполни таблицу (текстом):

| Подход | Учитывает порядок? | Есть память? | Где встречали |
|--------|:------------------:|:------------:|---------------|
| Мешок слов / TF-IDF | ? | ? | 7.1–7.2 |
| Эмбеддинги | ? | ? | 7.3 |
| RNN / LSTM | ? | ? | сегодня |

**🤔 Вопросы:**

1. Какой подход **первым** научился учитывать порядок?
2. Эмбеддинги дали словам **смысл**, но не дали **порядок**. RNN добавила порядок. Можно ли их **объединить**? (подсказка: в нашей LSTM есть и `nn.Embedding`, и `nn.LSTM` — что это значит?)

---

## Уровень 2. Свои эксперименты

### 2.1. Меняем размер памяти и число эпох

> **Задача:** посмотреть, как настройки влияют на LSTM.

**Что нужно сделать:**
1. Обучи LSTM с памятью **64** (как в практике), потом с **16** и **128** (`nn.LSTM(32, ...)`).
2. Попробуй 4 и 8 эпох. Сравни точность.

> **Подсказка:** меняй второе число в `nn.LSTM(32, ЗДЕСЬ, ...)` и согласуй с `nn.Linear(ЗДЕСЬ, 2)`. Число эпох — в `range(...)`.

**🤔 Вопросы:**

1. Память 16 / 64 / 128 — где точность выше? Всегда ли «больше = лучше»?
2. Помогли ли лишние эпохи? Когда обучение перестаёт улучшать результат?
3. Большая память = больше параметров. Чем это может быть **плохо**? (вспомни переобучение)

---

### 2.2. LSTM против мешка слов (из 7.2)

> **Задача:** честно сравнить два подхода на одной задаче.

**Что нужно сделать:**
1. Запиши метрики LSTM (accuracy, и если посчитаешь — precision/recall).
2. Сравни с логистической регрессией на TF-IDF из урока 7.2 (~0.97).
3. Сделай вывод: сильно ли LSTM выигрывает **на спаме**?

> **Подсказка:** для precision/recall посчитай по предсказаниям, как в 7.2 (`precision_score`, `recall_score`).

**🤔 Вопросы:**

1. Сильно ли LSTM обошла мешок слов на спаме? Почему разница невелика? (подсказка: спам выдают **отдельные слова**)
2. На какой задаче LSTM выиграла бы у мешка слов **с большим отрывом**? (где важен порядок/отрицание)
3. Значит ли «LSTM сложнее», что её всегда надо предпочитать? Когда проще — лучше?

---

## Уровень 3. Со звёздочкой

### 3.1. Проверка на «порядок»

> **Задача:** проверить, чувствует ли наша LSTM порядок слов.

**Что нужно сделать:**
1. Прогони через обученную модель пары фраз с одними словами в разном порядке (придумай свои, по-английски).
2. Посмотри, даёт ли модель **разные** ответы.

> **Подсказка:** используй `predict(...)` из практики. Учти: наша модель обучена на **спаме**, а не на тональности — она может быть **не чувствительна** к таким парам. Это тоже результат!

**🤔 Вопросы:**

1. Различила ли модель фразы? Если нет — почему? (на чём её обучали?)
2. Чтобы модель **ловила порядок/отрицание**, на каких данных её надо учить?
3. Вывод: умение «читать по порядку» есть у архитектуры, но **проявится** только если задача и данные этого требуют. Согласен(на)?

---

### 3.2. Длина последовательности

> **Задача:** понять роль длины и «добивки» нулями (padding).

**Что нужно сделать:**
1. В `encode` поменяй `length` (например, 10, 30, 60). Переобучи, сравни точность.
2. Подумай, что теряется при `length=10` для длинных сообщений.

> **Подсказка:** при `length=10` длинные SMS **обрезаются** — теряется хвост. При большой длине много `<pad>` (нулей) — лишние вычисления.

**🤔 Вопросы:**

1. Как длина повлияла на точность? Есть ли «золотая середина»?
2. Что плохого в слишком **маленькой** длине? А в слишком **большой**?
3. Почему мы добиваем короткие сообщения нулями (`<pad>`)? (подсказка: батчу нужны строки одной длины)

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Понимает, что мешок слов теряет порядок | |
| Понимает идею RNN (читать по порядку + память) | |
| Понимает роль LSTM (умная память) | |
| Собрал/запустил LSTM, увидел результат | |
| **Уровень 2:** эксперимент (память/эпохи или сравнение) | |
| **Ответил на 🤔 вопросы размышления** | |

> **Минимум для «зачёта»:** запуск LSTM + сравнение с мешком слов (7.2) + одна задача Уровня 2 + **ответы на вопросы**.
>
> **Без ответов на вопросы — задача не считается решённой.**
> **Главное — понять: RNN/LSTM читают текст по порядку и помнят прочитанное.**

---

> **Ты познакомился с сетями, которые читают текст по порядку и помнят прочитанное!** RNN и LSTM годами были основой перевода и распознавания речи. Но у них есть слабость: они читают **строго по очереди**, медленно, и всё равно с трудом ловят **очень** далёкие связи. В 2017 году придумали кое-что получше — **трансформеры**: они смотрят на **весь** текст сразу и сами решают, какие слова **важны** для каждого слова (механизм **attention**). Это то, на чём работают ChatGPT и современные переводчики. О них — следующий урок! 🚀
