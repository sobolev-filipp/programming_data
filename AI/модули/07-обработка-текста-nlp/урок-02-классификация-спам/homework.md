# Урок 7.2. Самостоятельная работа на уроке

> **Как всегда:** в подсказках **мало готового кода**. Я даю **направление** — думать твоя задача.
>
> После каждого задания — **🤔 вопросы на размышление**. Записывай ответы в Markdown-ячейку.
>
> Тетрадка: продолжай `lesson-7-2-spam.ipynb`. Заголовок `## Самостоятельная работа`.

> **Главное правило урока:** конвейер «текст → TF-IDF → классификатор → метрики». Словарь учим на train (`fit_transform`), тест только `transform`. Accuracy на несбалансированных данных обманчива — смотри precision/recall.

---

## Шпаргалка (из практики)

```python
from sklearn.feature_extraction.text import TfidfVectorizer
from sklearn.linear_model import LogisticRegression
from sklearn.metrics import accuracy_score, precision_score, recall_score, confusion_matrix

tfidf = TfidfVectorizer()
Xtr = tfidf.fit_transform(X_train)     # train: учим словарь + преобразуем
Xte = tfidf.transform(X_test)          # test: только преобразуем

model = LogisticRegression(max_iter=1000).fit(Xtr, y_train)
pred = model.predict(Xte)
# accuracy_score / precision_score / recall_score / confusion_matrix
```

---

## Уровень 1. Понять конвейер

### 1.1. Теория своими словами (без кода!)

> **Задача:** проверить понимание. **Ответы текстом.**

1. Из каких 5 шагов состоит наш конвейер классификации текста?
2. Почему словарь TF-IDF учат **только на train**? Что плохого выучить его на тесте?
3. Почему **accuracy** на наших данных обманчива? Какую точность даст «ленивый» фильтр?
4. Что такое **precision** и **recall** на языке спама?
5. Что для спам-фильтра хуже — низкий precision или низкий recall? Почему?

**🤔 Вопросы на размышление:**

1. Наш фильтр: precision = 1.00, recall = 0.79. Объясни **словами**, что это значит для пользователя почты.
2. В матрице ошибок верхний-правый угол (ham → spam) = 0. Почему это особенно хорошо именно для **спам**-фильтра?
3. Назови **3 задачи**, кроме спама, где «текст → числа → классификатор» решает реальную проблему.

---

### 1.2. Прочитай матрицу (по числам, без кода)

> **Задача:** понять метрики по готовой матрице. Дана матрица ошибок другого фильтра:

```
           pred_ham  pred_spam
true_ham       1180        26
true_spam        20       167
```

Посчитай и ответь текстом:
1. Сколько нормальных писем по ошибке улетело в спам (ложные тревоги)?
2. Сколько спама просочилось в почту?
3. Этот фильтр лучше или хуже нашего (у нас было 0 ложных тревог, 39 пропусков)? Для **спам**-фильтра — чем именно?

**🤔 Вопросы:**

1. У этого фильтра recall **выше** нашего (поймал больше спама), но появились **ложные тревоги**. Это улучшение или ухудшение для почты? Обоснуй.
2. Как одним числом «штрафовать» и за пропуски, и за ложные тревоги сразу? (вспомни метрику из 3.4)

---

## Уровень 2. Свои эксперименты

### 2.1. Наивный Байес vs логистическая регрессия

> **Задача:** сравнить два классических алгоритма на спаме.

**Что нужно сделать:**
1. Обучи на тех же данных `MultinomialNB` (наивный Байес).
2. Посчитай accuracy, precision, recall, матрицу для него.
3. Сравни с логистической регрессией.

> **Подсказка:** `from sklearn.naive_bayes import MultinomialNB`; `model = MultinomialNB().fit(Xtr, y_train)`. Векторизатор и данные — те же, меняешь только модель.

**🤔 Вопросы:**

1. У какой модели **recall** выше (ловит больше спама)? У какой precision?
2. Если для тебя важнее **не потерять письма**, какую модель выберешь?
3. Обе дали высокую accuracy. Почему её одной недостаточно для выбора?

---

### 2.2. Какие слова выдают спам

> **Задача:** заглянуть «внутрь» модели — на какие слова она опирается.

**Что нужно сделать:**
1. У обученной логистической регрессии возьми веса (`model.coef_`).
2. Найди **10 слов с самым большим весом** (сильнее всего тянут к «спам») и 10 с самым маленьким (к «ham»).
3. Совпадают ли они с твоей интуицией о спаме?

> **Подсказка:** слова — в `tfidf.get_feature_names_out()`, веса — `model.coef_[0]`. Соедини `zip`, отсортируй по весу. Большой вес → спам, маленький (отрицательный) → ham.

**🤔 Вопросы:**

1. Какие слова сильнее всего тянут к «спам»? Логично ли это?
2. Какие — к «ham»? (обычные бытовые слова?)
3. Может ли модель **ошибиться** из-за слова не по делу? Приведи пример (например, нормальное письмо со словом «free»).

---

### 2.3. Влияние стоп-слов и порога

> **Задача:** посмотреть, как настройки меняют метрики.

**Что нужно сделать:**
1. Обучи фильтр с `TfidfVectorizer(stop_words='english')` (убрать частые англ. слова) — сравни метрики.
2. (Со звёздочкой) Вместо `predict` используй `predict_proba` и поменяй **порог** решения (например, 0.3 вместо 0.5). Как меняются precision/recall?

> **Подсказка:** `predict_proba(Xte)[:,1]` — вероятность спама. Порог: `(proba > 0.3).astype(int)`. Ниже порог → ловим больше спама (recall ↑), но рискуем ложными тревогами (precision ↓).

**🤔 Вопросы:**

1. Стоп-слова улучшили или ухудшили метрики? Почему?
2. Когда снижаешь порог, что растёт — recall или precision? А что падает?
3. Как порогом настроить фильтр «осторожный» (не теряет письма) против «строгого» (ловит весь спам)?

---

## Уровень 3. Со звёздочкой

### 3.1. Разбери ошибки фильтра

> **Задача:** посмотреть, на чём именно модель ошибается.

**Что нужно сделать:**
1. Найди сообщения, где предсказание ≠ правда (особенно пропущенный спам).
2. Прочитай их. Почему модель ошиблась? (короткие? необычные слова?)

> **Подсказка:** сравни `pred` и `y_test`, отбери несовпадения, выведи соответствующие тексты из `X_test`.

**🤔 Вопросы:**

1. Что общего у спама, который фильтр **пропустил**? (мало слов-маркеров? хитрая маскировка?)
2. Как спамеры **обманывают** фильтры? (например, «Fr33» вместо «Free»)
3. Что бы ты добавил(а), чтобы ловить таких хитрецов?

---

### 3.2. Свой мини-датасет

> **Задача:** собрать свои примеры и проверить фильтр.

**Что нужно сделать:**
1. Напиши 5 «спам» и 5 «нормальных» сообщений (на английском, как датасет).
2. Прогони через фильтр, посчитай, сколько угадал.
3. Попробуй **обмануть** фильтр — написать спам, который он не поймает.

**🤔 Вопросы:**

1. Сколько твоих примеров фильтр угадал?
2. Удалось ли обмануть? Каким приёмом?
3. Почему модель, обученная на SMS, может плохо работать на твоих примерах? (другой стиль текста — связь с train/test распределением)

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Собрал конвейер: текст → TF-IDF → классификатор | |
| Понимает `fit_transform` на train / `transform` на test | |
| Понимает, почему accuracy обманчива (несбаланс) | |
| Различает precision и recall (для спама) | |
| Прочитал матрицу ошибок | |
| **Уровень 2:** Naive Bayes / важные слова / порог | |
| **Ответил на 🤔 вопросы размышления** | |

> **Минимум для «зачёта»:** рабочий фильтр + precision/recall/матрица + одна задача Уровня 2 + **ответы на вопросы**.
>
> **Без ответов на вопросы — задача не считается решённой.**
> **Главное — понять: accuracy врёт на несбалансированных данных; правду говорят precision/recall и матрица ошибок.**

---

> **Ты построил настоящий спам-фильтр и научился честно его оценивать!** Теперь ты умеешь решать задачу классификации текста от начала до конца — это реальный навык. Но у нашего подхода есть слабое место: для модели слова — это просто **отдельные столбцы**, никак не связанные. Она не знает, что «good» и «great» близки по смыслу, а «good» и «table» — нет. На следующем уроке (7.3) исправим это: познакомимся с **embeddings** — способом представить слова так, что **похожие по смыслу слова оказываются рядом**. Это шаг к настоящему «пониманию» языка. 🚀
