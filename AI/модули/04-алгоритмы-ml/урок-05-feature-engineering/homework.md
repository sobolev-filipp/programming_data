# Урок 4.5. Самостоятельная работа на уроке

> **Как всегда:** в подсказках **мало готового кода**. Я даю **направление** — думать и придумывать признаки твоя задача.
>
> После каждого задания — **🤔 вопросы на размышление**. Записывай ответы в Markdown-ячейку.
>
> Тетрадка: продолжай `lesson-4-5-features.ipynb`. Заголовок `## Самостоятельная работа`.

> **Главное правило урока:** хорошие признаки часто важнее модели. Думай: **«какой новый признак помог бы модели понять задачу?»**

---

## Шпаргалка (минимум команд)

```python
# Комбинация столбцов
df['new'] = df['a'] + df['b']
df['ratio'] = df['a'] / df['b']

# Флаг (да/нет)
df['flag'] = (df['x'] > 100).astype(int)

# Извлечь из текста
df['title'] = df['Name'].str.extract(r' ([A-Za-z]+)\.')

# Бины (число → группа)
df['group'] = pd.cut(df['Age'], bins=[0,12,18,60,100], labels=['a','b','c','d'])

# One-hot кодирование
df = pd.get_dummies(df, columns=['title', 'group'])

# Проверить силу признака
df.groupby('new_feature')['target'].mean()

# Честная оценка
cross_val_score(model, X, y, cv=5).mean()
```

> Правило без утечки: статистики (медиану, среднее) для заполнения — считать **на train**, не на всех данных.

---

## Уровень 1. Понять идею

### 1.1. Придумай признаки (без кода!)

> **Задача:** потренировать «чутьё дата-сайентиста». **Ответы текстом в Markdown.**

Для каждой задачи придумай **2 новых признака**, которые помогли бы модели (используй здравый смысл / доменное знание):

1. **Предсказать цену квартиры.** Дано: площадь, число комнат, этаж, всего этажей, год постройки.
   *(Подсказка-пример: «площадь на комнату», «не первый/не последний этаж»...)*
2. **Предсказать, уйдёт ли клиент от оператора связи.** Дано: дата регистрации, сумма платежей за всё время, число звонков в поддержку.
3. **Предсказать продажи в магазине по дате.** Дано: дата (год-месяц-день).

**🤔 Вопросы на размышление:**

1. Почему «площадь на комнату» может быть **информативнее**, чем площадь и число комнат по отдельности?
2. Что важнее для хороших признаков — знание **математики** или знание **самой задачи** (предметной области)? Почему?
3. Приведи пример, когда **точное число** менее полезно, чем **группа** (бин), в которую оно попадает.

---

### 1.2. Поймай утечку (без кода!)

> **Задача:** научиться видеть «подглядывание в тест». **Ответы текстом.**

Для каждого случая скажи: есть ли **утечка** (подглядывание в тест), и почему:

1. `df['Age'] = df['Age'].fillna(df['Age'].median())` — где `df` это train+test вместе.
2. `df['FamilySize'] = df['SibSp'] + df['Parch'] + 1` — на всех данных.
3. Заполнили пропуски средним, посчитанным **только** на train, и применили к test.
4. Создали признак «средняя цена по классу каюты», посчитав среднее по train+test.

**🤔 Вопросы:**

1. Почему простая сумма (`SibSp + Parch`) **не** даёт утечки, а заполнение медианой по всем данным — даёт?
2. Как правильно заполнять пропуски, чтобы утечки не было?

---

## Уровень 2. Свои признаки

### 2.1. Добавь свой признак к Titanic

> **Задача:** придумать и проверить **свой** новый признак (которого не было в практике).

**Что нужно сделать:**
1. Возьми Titanic с признаками из практики (Задание 2 или 3).
2. Придумай и добавь **1 свой** новый признак. Идеи:
   - `Embarked` (порт посадки) → one-hot.
   - Есть ли запись о каюте: `HasCabin = df['Cabin'].notna().astype(int)`.
   - Длина имени (`df['Name'].str.len()`) — длинные имена у аристократов.
   - Своя идея!
3. Оцени модель **с** твоим признаком и **без** — сравни CV-точность.
4. Проверь осмысленность через `groupby(...)['Survived'].mean()`.

> **Подсказка:** добавляй признак в свою функцию `prepare_...`, потом `cross_val_score`. Сравни число с базовым.

**🤔 Вопросы:**

1. Твой признак помог, навредил или не изменил точность? Как думаешь, почему?
2. Что показал `groupby` — есть ли связь признака с выживанием?
3. Если признак не помог — значит ли это, что он «плохой»? (подумай: может, его информация уже есть в других признаках)

---

### 2.2. Какой признак самый важный

> **Задача:** узнать, какие из твоих признаков модель ценит больше всего.

**Что нужно сделать:**
1. Возьми полный набор признаков из практики (Задание 3).
2. Обучи случайный лес.
3. Выведи `feature_importances_` и отсортируй по убыванию.
4. Построй горизонтальный bar-график важности.

> **Подсказка:** удобно через `pd.Series(model.feature_importances_, index=X.columns).sort_values()`. Потом `.plot(kind='barh')`.

**🤔 Вопросы:**

1. Какие **3 признака** самые важные? Есть ли среди них **созданные нами** (Title, FamilySize...)?
2. Оказались ли наши новые признаки важнее некоторых «сырых»? Что это говорит о пользе feature engineering?
3. Какой «сырой» признак почти не используется? Можно ли его выбросить?

---

## Уровень 3. Со звёздочкой

### 3.1. Полиномиальные признаки

> **Задача:** автоматически создать признаки-произведения и проверить, помогут ли.

**Что нужно сделать:**
1. Возьми числовой датасет (Boston Housing или diabetes из sklearn).
2. Используй `PolynomialFeatures` для создания произведений признаков:
   ```python
   from sklearn.preprocessing import PolynomialFeatures
   poly = PolynomialFeatures(degree=2, include_bias=False)
   X_poly = poly.fit_transform(X)
   ```
3. Сравни модель (линейную регрессию) на обычных и на полиномиальных признаках по CV.

> **Подсказка:** `degree=2` создаёт квадраты и произведения пар признаков (x1², x1·x2, x2²...). Число признаков сильно растёт — будь готов.

**🤔 Вопросы:**

1. Помогли ли полиномиальные признаки? Насколько выросло их число?
2. В чём **риск** создавать слишком много признаков? (вспомни переобучение и «проклятие размерности» из 4.3)
3. Когда полиномиальные признаки оправданы, а когда лучше взять модель, которая и так ловит нелинейность (дерево/лес)?

---

### 3.2. Feature engineering решает больше, чем модель

> **Задача:** доказать тезис урока экспериментом.

**Что нужно сделать:**
1. Возьми Titanic.
2. **Эксперимент А:** простая модель (логистическая регрессия) на **хороших** признаках (полный набор из практики 3).
3. **Эксперимент Б:** сложная модель (лес + GridSearch) на **сырых** 4 признаках.
4. Сравни CV-точность А и Б.

> **Подсказка:** логрег требует стандартизации (Pipeline). Для Б возьми небольшой GridSearch по лесу. Сравни итоговые числа.

**🤔 Вопросы:**

1. Что победило — **простая модель с хорошими признаками** или **сложная модель с плохими**?
2. Как этот результат подтверждает (или опровергает) главный тезис урока?
3. Если бы у тебя был всего час на проект — на что бы ты потратил больше времени: на признаки или на подбор модели? Почему?

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Понимает, что признаки часто важнее модели | |
| Умеет создавать признаки (комбинации, бины, из текста) | |
| Видит **утечку** при создании признаков | |
| **Уровень 2:** добавил свой признак, оценил пользу | |
| **Ответил на 🤔 вопросы размышления** | |
| **Уровень 3:** полиномы ИЛИ «признаки vs модель» | |

> **Минимум для «зачёта»:** Уровень 1 (обе задачи) + одна задача Уровня 2 + **ответы на вопросы**.
>
> **Без ответов на вопросы — задача не считается решённой.**
> **Главное — понять, что умные признаки бьют сложную модель, и научиться их придумывать.**

---

> **Не бойся экспериментировать** с признаками — это нормально, что не каждый помогает! Дата-сайентисты пробуют десятки идей, и часть отбрасывают. Главный навык — **придумывать** и **проверять**. И помни: лучшие признаки рождаются из **понимания задачи**, а не из формул.
