# Урок 4.7. Самостоятельная работа на уроке

> **Сегодня работа особая:** ты **улучшаешь своё боевое решение** Titanic. Меньше «заданий по шаблону» — больше **свободного творчества** дата-сайентиста.
>
> После каждого задания — **🤔 вопросы на размышление**. Записывай ответы в Markdown-ячейку.
>
> Тетрадка: продолжай `lesson-4-7-kaggle.ipynb`. Заголовок `## Самостоятельная работа`.

> **Главное правило урока:** улучшения проверяй по **кросс-валидации**, не по догадкам. Вырос CV → улучшилось решение.

---

## Шпаргалка (минимум команд)

```python
# Создать признак для train И test одинаково
def add_features(df):
    df = df.copy()
    df['NewFeature'] = ...
    return df

# Оценить себя ДО отправки
cross_val_score(model, X, y, cv=5).mean()

# Сделать submission
submission = pd.DataFrame({'PassengerId': test_ids, 'Survived': preds})
submission.to_csv('submission.csv', index=False)   # index=False обязательно!
```

> Помни: признаки создавай **симметрично** (и для train, и для test), иначе предсказание сломается.

---

## Уровень 1. Понять соревнование

### 1.1. Как устроено соревнование (без кода!)

> **Задача:** проверить понимание правил Kaggle. **Ответы текстом в Markdown.**

**Что нужно сделать.** Ответь на вопросы (1–3 предложения):

1. Чем отличаются `train.csv` и `test.csv` в соревновании?
2. Почему **нельзя** посчитать accuracy на test локально, у себя в ноутбуке?
3. Как тогда понять, хороша ли модель, **до** отправки на Kaggle?
4. Что такое `submission.csv` и какие в нём колонки?

**🤔 Вопросы на размышление:**

1. Зачем Kaggle **скрывает** ответы тестовой выборки? Что было бы, если бы их можно было увидеть?
2. Почему этой «честной» проверке на скрытом тесте можно **доверять** больше, чем нашему учебному `train_test_split`?
3. Как ситуация «предсказать без знания ответа» связана с **реальной** работой ML (например, предсказать, уйдёт ли клиент **завтра**)?

---

### 1.2. Лидерборд-ловушка (без кода!)

> **Задача:** понять, почему нельзя слепо верить публичному лидерборду. **Ответы текстом.**

**Сценарий.** Два ученика решают Titanic:
- **Аня:** доверяет своей кросс-валидации (CV=0.82), отправила 2 раза. Public score 0.79.
- **Боря:** отправлял по 20 раз в день, подбирая то, что даёт максимум на public. Public score 0.85.

В конце открыли **private** лидерборд: у Ани 0.81, у Бори 0.74.

**Объясни текстом:**
1. Почему у Бори public был выше, а private — резко ниже?
2. Кто поступил **правильно** и почему?

**🤔 Вопросы:**

1. Как «подгонка под public лидерборд» связана с **переобучением** модели? Что общего?
2. Какому числу стоит верить больше — публичному score (по 50% теста) или своей CV (по всему train)? Почему?

---

## Уровень 2. Улучши своё решение

### 2.1. Добавь 2 новых признака

> **Задача:** улучшить CV-score, добавив свои признаки.

**Что нужно сделать:**
1. Возьми своё решение из практики.
2. Добавь в `add_features` **минимум 2 новых** признака. Идеи:
   - `FarePerPerson = Fare / FamilySize`
   - `HasCabin = Cabin.notna().astype(int)`
   - бины возраста (`pd.cut`)
   - размер группы по номеру билета (`Ticket`)
3. Пересобери X (с новыми признаками в нужных списках num/cat).
4. Сравни CV **до** и **после**.

> **Подсказка:** не забудь добавить новые признаки в `features_num` или `features_cat`! Иначе ColumnTransformer их не возьмёт. Применяй `add_features` и к train, и к test.

**🤔 Вопросы:**

1. Какие признаки ты добавил? CV вырос, упал или не изменился?
2. Если признак не помог — почему? (вспомни 4.5: может, информация дублируется)
3. Какой из твоих признаков оказался полезнее? Как это проверить? (подсказка: `feature_importances_`)

---

### 2.2. Сделай улучшенный submission

> **Задача:** оформить улучшенное решение в новый `submission.csv`.

**Что нужно сделать:**
1. Возьми лучшую версию модели (с новыми признаками, можно + GridSearch).
2. Обучи на **всём** train.
3. Предскажи test, сделай `submission_v2.csv`.
4. Проверь формат: 418 строк, колонки `PassengerId, Survived`, `index=False`.
5. (Если есть Kaggle) отправь и сравни score с первой версией.

> **Подсказка:** перед отправкой всегда проверяй `len(submission)` (должно быть 418) и `submission.columns`. Кривой формат Kaggle отвергнет.

**🤔 Вопросы:**

1. Стало ли решение лучше по CV? На сколько?
2. Если отправил на Kaggle — public score совпал с твоей CV? Насколько близко?
3. Что бы ты попробовал **следующим**, будь у тебя ещё час?

---

## Уровень 3. Со звёздочкой

### 3.1. Сравни 3 модели честно

> **Задача:** выбрать лучшую модель для своего сабмишена по кросс-валидации.

**Что нужно сделать:**
1. Возьми свой набор признаков.
2. Сравни по CV (`cv=5`) три модели в одном Pipeline (меняя только последний шаг):
   - `RandomForestClassifier`
   - `GradientBoostingClassifier`
   - `LogisticRegression` (помни про масштабирование — оно уже в Pipeline)
3. Выбери лучшую и сделай ею submission.

**🤔 Вопросы:**

1. Какая модель дала лучший CV? Большая ли разница между ними?
2. Подтверждает ли это правило «признаки важнее модели» (разница между моделями мала)?
3. Почему важно сравнивать модели именно по **CV**, а не по одной отправке на Kaggle?

---

### 3.2. Лидерборд класса

> **Задача (командная):** устроить мини-соревнование внутри класса.

**Что нужно сделать:**
1. Каждый записывает свой **CV-score** и (если есть) **Kaggle public score** в общую таблицу класса.
2. Обсудите: у кого выше и **почему** (какие признаки/модели помогли).
3. Поделитесь идеями признаков — и попробуйте лучшие у себя.

**🤔 Вопросы:**

1. Что общего у решений с **высоким** score? (наверняка — хорошие признаки!)
2. Совпал ли порядок по CV с порядком по Kaggle public score? Где разошёлся?
3. Какую **одну** идею у одноклассника ты возьмёшь себе?

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Понимает устройство соревнования (train/test/submission) | |
| Понимает, что на test **нет ответов**, и доверяет CV | |
| Сделал корректный `submission.csv` (418 строк, формат) | |
| **Уровень 2:** улучшил решение новыми признаками | |
| **Ответил на 🤔 вопросы размышления** | |
| (Бонус) отправил на Kaggle, получил score | |

> **Минимум для «зачёта»:** Уровень 1 (обе задачи) + рабочий `submission.csv` из Уровня 2 + **ответы на вопросы**.
>
> **Без ответов на вопросы — задача не считается решённой.**
> **Главное — пройти весь путь от данных до сабмишена и понять роль честной валидации.**

---

> **Поздравляю — ты участник настоящего соревнования по ML!** Не расстраивайся из-за места: даже score 0.77 означает, что ты **сам** прошёл весь путь дата-сайентиста. Это огромное достижение. И помни: **верь своей кросс-валидации**, а не сиюминутному лидерборду.
