# Урок 4.6. Самостоятельная работа на уроке

> **Как всегда:** в подсказках **мало готового кода**. Я даю **направление** — собирать конвейер твоя задача.
>
> После каждого задания — **🤔 вопросы на размышление**. Записывай ответы в Markdown-ячейку.
>
> Тетрадка: продолжай `lesson-4-6-pipeline.ipynb`. Заголовок `## Самостоятельная работа`.

> **Главное правило урока:** весь препроцессинг — **внутри Pipeline**. Тогда нет утечек, код чистый, модель готова к продакшену.

---

## Шпаргалка (минимум команд)

```python
from sklearn.pipeline import Pipeline
from sklearn.compose import ColumnTransformer
from sklearn.preprocessing import StandardScaler, OneHotEncoder
from sklearn.impute import SimpleImputer
import joblib

# Числовые и категориальные обработчики
num_tf = Pipeline([('imp', SimpleImputer(strategy='median')), ('sc', StandardScaler())])
cat_tf = Pipeline([('imp', SimpleImputer(strategy='most_frequent')),
                   ('oh', OneHotEncoder(handle_unknown='ignore'))])

# ColumnTransformer
pre = ColumnTransformer([('num', num_tf, num_cols), ('cat', cat_tf, cat_cols)])

# Полный pipeline
pipe = Pipeline([('pre', pre), ('model', RandomForestClassifier())])
pipe.fit(X_train, y_train)

# GridSearch: путь через шаги
{'model__n_estimators': [100, 200], 'pre__num__imp__strategy': ['mean','median']}

# Сохранение
joblib.dump(pipe, 'model.joblib'); joblib.load('model.joblib')
```

---

## Уровень 1. Понять Pipeline

### 1.1. Зачем и как (без кода!)

> **Задача:** проверить понимание идеи. **Ответы текстом в Markdown.**

**Что нужно сделать.** Ответь на вопросы (1–3 предложения):

1. Назови **3 проблемы** «обработки по кускам» (россыпью команд), которые решает Pipeline.
2. Что происходит внутри Pipeline при вызове `pipe.fit(X_train, y_train)`? Опиши по шагам для цепочки `scaler → model`.
3. Зачем нужен `ColumnTransformer` — почему нельзя обработать все столбцы одинаково?

**🤔 Вопросы на размышление:**

1. Почему Pipeline **защищает от утечки** в кросс-валидации, а ручная стандартизация всех данных — нет?
2. Почему лучше сохранять (`joblib`) **весь Pipeline**, а не только обученную модель?
3. Аналогия конвейера: что здесь «сырьё», что «станции», что «готовая машина»?

---

### 1.2. Прочитай путь параметра (без кода!)

> **Задача:** научиться читать «адреса» параметров в Pipeline. **Ответы текстом.**

Дан Pipeline:
```python
pipe = Pipeline([
    ('pre', ColumnTransformer([
        ('num', Pipeline([('imp', SimpleImputer()), ('sc', StandardScaler())]), num_cols),
        ('cat', OneHotEncoder(), cat_cols)
    ])),
    ('clf', RandomForestClassifier())
])
```

**Объясни словами, что настраивает каждый путь:**
1. `clf__n_estimators`
2. `pre__num__imp__strategy`
3. `pre__num__sc__with_mean`

**🤔 Вопросы:**

1. Почему в путях используется именно **двойное** подчёркивание `__`, а не точка или одинарное?
2. Как бы выглядел путь к параметру `max_depth` модели в этом Pipeline?

---

## Уровень 2. Свой конвейер

### 2.1. Собери полный Pipeline для Titanic

> **Задача:** построить рабочий конвейер «с нуля» на сырых данных Titanic.

**Что нужно сделать:**
1. Загрузи Titanic. Возьми признаки: `Pclass, Sex, Age, Fare, Embarked, SibSp, Parch` (есть строки и пропуски!).
2. Определи, какие столбцы **числовые**, какие **категориальные**.
3. Собери `num_transformer` (impute median + scale) и `cat_transformer` (impute most_frequent + one-hot).
4. Собери `ColumnTransformer` и полный `Pipeline` с моделью (лес или логрег).
5. Оцени через `cross_val_score`.

> **Подсказка:** числовые — `Pclass, Age, Fare, SibSp, Parch`; категориальные — `Sex, Embarked`. Не кодируй и не заполняй ничего вручную — пусть всё делает Pipeline!

**🤔 Вопросы:**

1. Сколько строк кода ручной подготовки тебе **не пришлось** писать благодаря Pipeline?
2. Что будет, если в `Embarked` теста встретится значение, которого не было в train? Как `handle_unknown='ignore'` это решает?
3. Сравни CV-точность с тем, что было в Уроке 4.5. Похоже? (Pipeline не меняет точность — он делает код чище.)

---

### 2.2. Pipeline + GridSearch + сохранение

> **Задача:** настроить конвейер из 2.1 и сохранить лучшую модель.

**Что нужно сделать:**
1. Возьми Pipeline из 2.1.
2. Сделай `GridSearchCV` по параметрам модели (например, `model__n_estimators`, `model__max_depth`).
3. Добавь в сетку **параметр препроцессинга** (например, `pre__num__imp__strategy: ['mean','median']`).
4. Найди лучшие параметры, оцени на тесте.
5. Сохрани лучшую модель через `joblib`, загрузи обратно, предскажи на 3 строках.

> **Подсказка:** `grid.best_estimator_` — готовая лучшая модель для сохранения. После загрузки подавай сырые `X_test` — препроцессинг внутри.

**🤔 Вопросы:**

1. Какие параметры выбрал GridSearch? Какую стратегию заполнения — mean или median?
2. Загруженная модель дала те же предсказания, что исходная? (должна!)
3. Зачем в реальном проекте **сохранять** модель, а не обучать её каждый раз заново?

---

## Уровень 3. Со звёздочкой

### 3.1. Сравни «с Pipeline» и «без Pipeline» на утечку

> **Задача:** показать, что неправильная стандартизация (вне Pipeline) **завышает** оценку.

**Что нужно сделать:**
1. Возьми числовой датасет (breast cancer).
2. **Способ А (с утечкой):** стандартизуй **все** X заранее (`StandardScaler().fit_transform(X)`), потом `cross_val_score`.
3. **Способ Б (правильный):** положи scaler в Pipeline, `cross_val_score(pipe, X, y)`.
4. Сравни средние оценки.

> **Подсказка:** на «хороших» данных разница мала, но она есть. Чтобы усилить эффект, можно взять очень маленькую подвыборку (например, 30 объектов) — там утечка заметнее.

**🤔 Вопросы:**

1. Какой способ дал **выше** оценку? Почему завышенная оценка — это **плохо**?
2. Почему именно Pipeline-способ даёт **честную** оценку?
3. Где ещё в ML важно «препроцессинг учить только на train»? (вспомни весь Модуль 3-4)

---

### 3.2. Свой трансформер (продвинутое)

> **Задача:** добавить в Pipeline **собственный** шаг создания признаков.

**Что нужно сделать:**
1. Изучи `FunctionTransformer` из `sklearn.preprocessing`.
2. Напиши функцию, которая добавляет признак `FamilySize = SibSp + Parch + 1`.
3. Оберни её в `FunctionTransformer` и вставь в Pipeline **до** препроцессинга.
4. Проверь, что Pipeline работает.

> **Подсказка:** `FunctionTransformer(func)` превращает обычную функцию в шаг Pipeline. Функция получает данные и возвращает преобразованные. Это задание «на вырост» — если не выйдет, ничего страшного.

**🤔 Вопросы:**

1. Зачем встраивать создание признаков **в** Pipeline, а не делать его заранее? (подумай про продакшен и повторяемость)
2. Что удобнее для **новых** данных: Pipeline с FamilySize внутри или ручное создание признака каждый раз?
3. Как это связано с feature engineering из Урока 4.5?

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Понимает, зачем нужен Pipeline | |
| Умеет собрать Pipeline (scaler + модель) | |
| Применяет **ColumnTransformer** (числа + категории) | |
| Понимает, что Pipeline **защищает от утечек** | |
| **Уровень 2:** собрал полный конвейер, настроил, сохранил | |
| **Ответил на 🤔 вопросы размышления** | |

> **Минимум для «зачёта»:** Уровень 1 (обе задачи) + одна задача Уровня 2 + **ответы на вопросы**.
>
> **Без ответов на вопросы — задача не считается решённой.**
> **Главное — понять, что Pipeline делает код чистым, повторяемым и без утечек.**

---

> **Не пугайся** длинных путей вроде `pre__num__imp__strategy` — читай их слева направо как «адрес»: шаг → подшаг → параметр. А главную идею запомни: **весь препроцессинг — в Pipeline**. Так пишут профессионалы. На следующем уроке этот конвейер пригодится на настоящем соревновании Kaggle!
