# Урок 1.7. Самостоятельная работа на уроке

> **В этом задании ты применяешь готовые `derivative` и `numerical_gradient` с урока.** Я даю **постановку**, ты пишешь код. После каждой задачи — **вопросы на размышление**.
>
> Тетрадка: продолжай — `lesson-1-7-derivatives.ipynb`. Раздели: `## Самостоятельная работа`.

> **Главное правило:** в градиентном спуске **МИНУС** в шаге! `point = point - lr * grad`. С плюсом — пойдёшь **вверх по холму**.

---

## Шпаргалка (минимум команд)

```python
import numpy as np

# Численная производная (1D)
def derivative(f, x, h=1e-4):
    return (f(x + h) - f(x - h)) / (2 * h)

# Численный градиент (multi-D)
def numerical_gradient(f, point, h=1e-4):
    grad = np.zeros_like(point)
    for i in range(len(point)):
        p_plus = point.copy()
        p_minus = point.copy()
        p_plus[i] += h
        p_minus[i] -= h
        grad[i] = (f(p_plus) - f(p_minus)) / (2 * h)
    return grad

# Градиентный спуск
point = np.array([5.0, 7.0])
lr = 0.1
for _ in range(50):
    grad = numerical_gradient(f, point)
    point = point - lr * grad    # МИНУС!
```

---

## Уровень 1. Производная

### 1.1. Производные в разных точках

> **Задача:** для функции `f(x) = 2x³ - 5x + 1` посчитать производные и сделать вывод о монотонности.

**Что нужно:**

1. **Определи** функцию `f(x)`.
2. **Посчитай** производную в точках `x = 0, 1, 2, -1`.
3. **Запиши в Markdown:** где функция растёт, где убывает.

**🤔 Вопросы:**

1. По знакам производных: где функция **меняет монотонность**? (рост → убывание или наоборот)
2. Между какими двумя соседними точками знак производной **меняется**? Что это означает геометрически?
3. **Сначала предскажи**, потом проверь: какой будет производная в точке `x = 10`? Будет ли положительной или отрицательной?

---

### 1.2. Найти максимум

> **Задача:** для `f(x) = -(x - 3)² + 9` найти точку максимума.

**Что нужно:**

1. Определи функцию.
2. Посчитай производную в `x = 0, 2, 3, 4, 6`.
3. **Подумай:** в какой точке производная **меняет знак**? Это и есть максимум.
4. Построй график для проверки.

**🤔 Вопросы:**

1. В точке максимума производная равна **нулю**. Почему? Что это означает геометрически?
2. Это **максимум** или **минимум**? Как ты определил по производным?
3. Если бы функция была `+(x - 3)² + 9` (со знаком плюс перед квадратом) — была бы это точка максимума или **минимума**?

---

## Уровень 2. Градиент

### 2.1. Градиент функции двух переменных

> **Задача:** найти градиент `f(p) = p[0]² + 3·p[1]²` в нескольких точках.

**Точки:**
- `[1, 1]`
- `[2, 0]`
- `[0, 2]`
- `[3, 4]`

**Что нужно:**

1. Для каждой посчитай **градиент** и **его длину** (`np.linalg.norm`).
2. Определи, в какой точке градиент самый длинный.
3. **Проверь:** что вернёт градиент в точке `(0, 0)`?

**🤔 Вопросы:**

1. Аналитически градиент: `∇f = [2x, 6y]` (производная по x — `2x`, по y — `6y`). **Сравни** с численным результатом. Совпадает?
2. Почему в точке `(0, 0)` градиент почти **нулевой**? Что значит «нулевой градиент»?
3. **Сначала предскажи**, потом проверь: в какой точке `(10, 0)` или `(0, 10)` градиент длиннее? Почему?

---

### 2.2. Градиентный спуск со старта

> **Задача:** запустить градиентный спуск для функции из 2.1, начиная с (10, 10).

**Что нужно:**

1. Реализуй цикл: 50 шагов, `lr = 0.1`.
2. Печатай состояние каждые 10 шагов.
3. **Бонус:** добавь условие выхода, когда `f(point) < 0.01`.

**🤔 Вопросы:**

1. Куда сошёлся спуск? Это **минимум** функции?
2. На каком шаге значение функции стало `< 0.01`? Если не успело за 50 — сколько шагов нужно?
3. **Что произойдёт, если поставить плюс вместо минуса** (`point = point + lr * grad`)? **Сначала предскажи**, потом попробуй. Куда «убежит» точка?

---

## Уровень 3. Со звёздочкой

### 3.1. Минимум функции с «трещиной»

> **Задача:** показать, что **градиентный спуск находит ЛОКАЛЬНЫЙ минимум**, не глобальный.

```python
def f(x):
    return x**4 - 4*x**2 + 2
```

**Это двугорбая функция** — два минимума и максимум в нуле.

**Что нужно:**

1. Построй график на `x ∈ [-3, 3]`.
2. Реализуй `gd_1d(f, start, lr=0.1, n_steps=100)`.
3. **Запусти с трёх стартов:** 2.0, -2.0, 0.01.

**🤔 Главные вопросы:**

1. С каких стартов в какой минимум попал? Запиши таблицу.
2. **Это «недостаток»** градиентного спуска? Или **особенность**?
3. В нейросетях с **миллионами параметров** — есть ли «локальные минимумы»? Как с ними бороться?

---

### 3.2. Подбор learning rate

> **Задача:** подобрать lr для разных режимов сходимости.

```python
def f(p):
    return p[0]**2 + p[1]**2
```

**Что нужно найти:**

1. **Хороший lr** — сходится **за 10 шагов** до `f < 0.1` (старт `[5, 5]`).
2. **Слишком большой lr** — алгоритм **расходится**.
3. **Слишком маленький lr** — за 1000 шагов не дошёл до `< 0.1`.

**Подсказка для перебора:**
```python
for lr in [1.5, 1.0, 0.5, 0.3, 0.1, 0.05, 0.01, 0.001]:
    # запусти спуск и распечатай
```

**🤔 Вопросы:**

1. **Какой lr** дал лучшую сходимость? Запиши.
2. **Что происходит** при `lr = 1.5`? Точка скачет вверх и вниз — это нормально?
3. **Что произойдёт при lr = 1.0** ровно? Это «граничный» случай — попробуй и опиши.

---

### 3.3. Найти минимум функции потерь (мини-ML!)

> **Задача:** **впервые обучить модель!** Найти `w`, при котором `y = w·x` лучше всего предсказывает данные.

```python
def predict(w, x):
    return w * x

x_data = np.array([1, 2, 3, 4, 5])
y_data = np.array([2.1, 3.9, 6.0, 7.8, 10.2])
```

> Данные похожи на `y ≈ 2x` (с шумом).

**Функция потерь:**
```python
def loss(w):
    predictions = predict(w, x_data)
    return ((predictions - y_data) ** 2).sum()
```

**Что нужно:**

1. **Посчитай loss** для w = 0, 1, 2, 3, 4. Какой минимальный?
2. **Реализуй градиентный спуск** через `derivative(loss, w)`, чтобы найти лучшее `w`.
3. Старт: w = 0.0, lr = 0.01, 100 шагов.

**🤔 Главные вопросы:**

1. **Какой `w` нашёл** градиентный спуск? Близко к 2.0?
2. Это **первая ML-модель**, которую ты обучил. Что это за модель? (подсказка: «линейная регрессия»)
3. **Сравни** свой код с тем, что делает sklearn:
   ```python
   from sklearn.linear_model import LinearRegression
   model = LinearRegression(fit_intercept=False).fit(x_data.reshape(-1, 1), y_data)
   print(model.coef_)
   ```
   Совпадают ли результаты?

> **Поздравь себя.** Ты только что **с нуля реализовал базу всего ML** — оптимизацию параметров градиентным спуском. Это **серьёзно**.

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Использует `derivative` правильно | |
| Понимает: производная > 0 → растёт; < 0 → убывает | |
| Может посчитать градиент | |
| Запускает градиентный спуск | |
| **Уровень 3.1:** понимает локальные минимумы | |
| **Уровень 3.3:** связал с ML | |
| **Ответил на вопросы размышления** | |

**Минимум:** Уровень 1 целиком + **ответы на вопросы**.

> **Если справился с Уровнем 3.3** — ты уже **понимаешь, как обучается ИИ**. На самом деле — да. Серьёзно.
