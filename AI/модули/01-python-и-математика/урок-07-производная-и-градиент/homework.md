# 📝 Самостоятельная работа · Урок 1.7 «Производная и градиент»

**Темы:** численная производная, знак производной (рост/убывание), градиент функции нескольких переменных, градиентный спуск (`point = point - lr * grad`), learning rate, первая ML-модель.

> 🎯 **Как работать:** решай по порядку и **запускай после каждой задачи**. Уровни: 🟢 базовые (обязательно) · 🟡 средние · 🔴 со звёздочкой. В каждой задаче сказано, **что сделать и что должно получиться**. Подсказка 💡 — наводка на идею, **не готовый код**, и есть не у каждой задачи.
>
> 📌 Пользуйся готовыми `derivative` и `numerical_gradient` из шпаргалки — их надо **применять**, а не писать заново. Главное правило спуска: **МИНУС** в шаге! С плюсом полезешь вверх по холму. Тетрадка та же — `lesson-1-7-derivatives.ipynb`, раздел `## Самостоятельная работа`.

---

## Шпаргалка (готовые инструменты — просто используй)

```python
import numpy as np

def derivative(f, x, h=1e-4):
    return (f(x + h) - f(x - h)) / (2 * h)

def numerical_gradient(f, point, h=1e-4):
    grad = np.zeros_like(point)
    for i in range(len(point)):
        p_plus = point.copy(); p_minus = point.copy()
        p_plus[i] += h; p_minus[i] -= h
        grad[i] = (f(p_plus) - f(p_minus)) / (2 * h)
    return grad

point = np.array([5.0, 7.0]); lr = 0.1
for _ in range(50):
    grad = numerical_gradient(f, point)
    point = point - lr * grad    # МИНУС!
```

---

## 🟢 Уровень 1 — базовый (производная)

**1. Производная простой функции.** Определи `f(x) = x**2` и посчитай её производную в точках `x = 3`, `x = 0`, `x = -5`. Должно получиться примерно `6`, `0`, `-10` (это `2x`).

**2. Знак говорит о направлении.** Для `f(x) = 2*x**3 - 5*x + 1` посчитай производную в точках `x = 0, 1, 2, -1`. По знаку каждой в Markdown отметь, растёт функция в этой точке или убывает.
> 💡 Производная больше 0 — функция растёт; меньше 0 — убывает.

**3. Где меняется направление.** По результатам задачи 2 найди, между какими двумя соседними точками знак производной **меняется**. Это значит, что там у функции «поворот» (вершина или впадина).

**4. Вершина холма.** Функция `f(x) = -(x - 3)**2 + 9` — это холм. Посчитай производную в `x = 0, 2, 3, 4, 6` и найди точку, где она равна нулю. Построй график и убедись, что это вершина.
> 💡 В самой вершине касательная горизонтальна, поэтому производная там равна 0.

**5. Проверка формулы.** *(на подумать)* Для `f(x) = x**3` посчитай численную производную в `x = 2` и сравни с формулой `3x²` (= 12). Близко?

---

## 🟡 Уровень 2 — средний (градиент и спуск)

**6. Градиент в точке.** Определи `f(p) = p[0]**2 + 3*p[1]**2`. Посчитай градиент в точках `[1, 1]`, `[2, 0]`, `[0, 2]`. Для каждой выведи и сам градиент, и его длину (`np.linalg.norm`).

**7. Где градиент длиннее.** По задаче 6 определи, в какой из точек градиент самый длинный. И проверь, что вернёт градиент в точке `(0, 0)` — он почти нулевой (это дно «чаши»).

**8. Скатываем шарик.** Запусти градиентный спуск для функции из задачи 6 со старта `(10, 10)`: 50 шагов, `lr = 0.1`. Печатай состояние каждые 10 шагов. Куда сойдётся точка? (к минимуму — около `(0, 0)`).

**9. Плюс вместо минуса.** *(на подумать)* Возьми спуск из задачи 8 и поставь в шаге **плюс** (`point = point + lr * grad`). Сначала предскажи, потом запусти: куда убежит точка и почему?

**10. Слишком большой шаг.** Для `f(p) = p[0]**2 + p[1]**2` со старта `[5, 5]` запусти спуск с `lr = 1.5`. Что происходит с точкой — приходит в минимум или «разлетается»?

**11. Слишком маленький шаг.** Тот же спуск, но с `lr = 0.001`, 50 шагов. Дошёл ли до минимума? В Markdown коротко: какой из трёх lr (0.001 / 0.1 / 1.5) оказался лучшим?

---

## 🔴 Уровень 3 — со звёздочкой

**12. Два минимума.** Дана «двугорбая» функция:
```python
def f(x):
    return x**4 - 4*x**2 + 2
```
Построй её график на `x ∈ [-3, 3]`, напиши функцию одномерного спуска и запусти её с трёх стартов: `2.0`, `-2.0`, `0.01`. Запиши, кто куда скатился.
> 💡 У функции два минимума по краям; с разных стартов спуск попадает в разные — это важная особенность.

**13. ⭐ Обучи свою первую модель.** Найдём `w`, при котором `y = w·x` лучше всего описывает данные:
```python
def predict(w, x):
    return w * x

x_data = np.array([1, 2, 3, 4, 5])
y_data = np.array([2.1, 3.9, 6.0, 7.8, 10.2])   # похоже на y ≈ 2x

def loss(w):
    return ((predict(w, x_data) - y_data) ** 2).sum()
```
Посчитай `loss` для `w = 0, 1, 2, 3, 4` (при каком минимальна?), а потом запусти спуск по `w` (старт `0.0`, `lr = 0.01`, 100 шагов, используя `derivative(loss, w)`). Какой `w` нашёлся — близко к 2?

**14. Сверка со sklearn.** Сравни свой `w` из задачи 13 с готовой моделью:
```python
from sklearn.linear_model import LinearRegression
model = LinearRegression(fit_intercept=False).fit(x_data.reshape(-1, 1), y_data)
print(model.coef_)
```
Совпали результаты?

**15. Что ты только что сделал.** *(коротко в Markdown-ячейке)* Одной фразой объясни, что общего между твоим циклом спуска из задачи 13 и тем, как обучается любая нейросеть.

> 🎉 Дошёл до 13–14 и получил `w ≈ 2` — ты **с нуля** реализовал базу всего машинного обучения. Это не преувеличение.

---

## Что проверит преподаватель

| Что | Зачёт ✅ |
|-----|----------|
| Применяет `derivative` правильно | |
| Понимает знак производной (рост/убывание) | |
| Считает градиент, запускает спуск (с минусом!) | |
| Понимает влияние learning rate | |
| **🔴:** связал спуск с обучением модели (задачи 13–15) | |

> ✅ **Сделал 🟢 — читаешь функцию по производной. Осилил 🟡 — управляешь градиентным спуском. Взял 🔴 — понимаешь, как обучается ИИ.**
>
> **Минимум для зачёта:** 🟢 целиком + задачи 6–8 из 🟡.
