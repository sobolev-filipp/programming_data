# 📝 Самостоятельная работа · Урок 1.6 «Статистика»

**Темы:** среднее, медиана, мода, разброс (`std`, `var`), стандартизация, распределения (равномерное и нормальное), правило 68-95-99.7, корреляция.

> 🎯 **Как работать:** решай по порядку и **запускай после каждой задачи**. Уровни: 🟢 базовые (обязательно) · 🟡 средние · 🔴 со звёздочкой. В каждой задаче сказано, **что сделать и что должно получиться**. Подсказка 💡 — наводка на идею, **не готовый код**, и есть не у каждой задачи.
>
> 📌 Статистика оживает, только когда видишь числа, — поэтому считай и **сразу смотри на результат**. Тетрадка та же — `lesson-1-6-stats.ipynb`, раздел `## Самостоятельная работа`.

---

## Шпаргалка

```python
import numpy as np
import matplotlib.pyplot as plt
a.mean(), np.median(a), a.std(), a.var()
z = (a - a.mean()) / a.std()          # стандартизация
np.random.uniform(low, high, n)       # равномерное
np.random.normal(mean, std, n)        # нормальное (колокол)
np.random.randint(low, high, n)       # целые (high не входит!)
plt.hist(data, bins=30)               # гистограмма
np.corrcoef(a, b)[0, 1]                # корреляция
```

---

## 🟢 Уровень 1 — базовый (меры центра и разброса)

**1. Портрет класса.** Дан массив баллов ЕГЭ `scores = np.array([72, 88, 95, 65, 78, 82, 60, 91, 85, 76, 100, 55, 70, 84, 89])`. Посчитай и выведи среднее (до 2 знаков), медиану, минимум, максимум.

**2. Разброс.** Для тех же баллов посчитай размах (максимум − минимум), стандартное отклонение и дисперсию. Проверь, что `std² ≈ var`.

**3. Среднее против медианы.** Дан массив зарплат `salaries = np.array([35, 40, 42, 45, 50, 55, 60, 800])` (7 сотрудников и директор). Посчитай среднее и медиану. В Markdown коротко: какое число честнее описывает «типичную» зарплату?

**4. Убираем выброс.** Убери директора (`salaries[:-1]`) и посчитай среднее и медиану заново. Что сильнее «дёрнулось» — среднее или медиана?
> 💡 Один огромный выброс тянет среднее сильно, а медиану почти не трогает.

**5. Стандартизация.** Дан `ages = np.array([12, 13, 14, 15, 15, 16, 16, 17, 17, 18])`. Стандартизуй по формуле `z = (x - mean) / std`, выведи новый массив и проверь, что его среднее ≈ 0, а std ≈ 1.

**6. Смысл z-значения.** *(на подумать)* Для стандартизованного массива из задачи 5 найди, какому исходному возрасту соответствует самое большое z-значение, а какому — самое маленькое. В Markdown одной фразой: что значит `z = +1.5`?

---

## 🟡 Уровень 2 — средний (распределения и гистограммы)

**7. Бросаем кубик.** Сгенерируй 1000 бросков кубика: `np.random.seed(0); rolls = np.random.randint(?, ?, 1000)` — подставь правильные границы для чисел 1–6. Посчитай среднее (должно быть около 3.5).
> 💡 В `randint` верхняя граница **не входит**, так что для «1–6» её надо взять на 1 больше.

**8. Гистограмма кубика.** Построй гистограмму бросков с 6 столбиками. Столбики примерно одной высоты — почему? (каждое число выпадает одинаково часто).

**9. Рост людей.** Сгенерируй `np.random.seed(0); heights = np.random.normal(170, 8, 1000)`. Построй гистограмму с 40 столбиками и добавь красную вертикальную линию на среднем (170). Убедись, что получился «колокол».

**10. Правило 68%.** Посчитай долю людей в пределах ±1 std от среднего (162…178). Должно выйти около 68%.
> 💡 Собери маску «в диапазоне» и посчитай, какая доля от всех в неё попала (число True делить на общее).

**11. Правило 95%.** *(на подумать)* По тем же `heights` посчитай долю в пределах ±2 std (154…186). Сначала предскажи (это часть правила 68-95-99.7), потом проверь.

**12. Два класса.** Сгенерируй `np.random.seed(0); class_a = np.random.normal(4.0, 0.3, 30)` и `class_b = np.random.normal(3.7, 0.7, 30)`. Выведи среднее и std каждого. Построй две гистограммы на одном графике с `alpha=0.5`. В Markdown коротко: какой класс назвал бы «сильнее» и почему?

---

## 🔴 Уровень 3 — со звёздочкой (корреляция)

**13. Часы учёбы и оценки.** Даны `hours = np.array([2,3,5,4,7,6,8,10,9,12,11,14])` и `grades = np.array([3.2,3.5,4.0,3.8,4.2,4.0,4.5,4.7,4.6,4.9,4.8,5.0])`. Посчитай корреляцию (`np.corrcoef(...)[0,1]`) и построй scatter plot.

**14. Портим одну точку.** *(на подумать)* Возьми данные из задачи 13, замени одну оценку на дикую (например, при 100 часах поставь 1.0) и посчитай корреляцию заново. Насколько сильно её сдвинула одна точка?

**15. Корреляционная матрица.** Собери данные:
```python
np.random.seed(42); n = 100
math = np.random.normal(4.0, 0.5, n)
physics = math * 0.8 + np.random.normal(0, 0.3, n)
history = np.random.normal(4.0, 0.5, n)
chemistry = math * 0.7 + np.random.normal(0, 0.4, n)
data = np.array([math, physics, history, chemistry])
```
Получи матрицу 4×4 через `np.corrcoef(data)`, округли до 2 знаков и определи, какие предметы связаны сильнее всего.

**16. Ловушка корреляции.** Собери `np.random.seed(0); temperature = np.random.uniform(0, 35, 100); ice_cream = 5*temperature + np.random.normal(0, 10, 100); swimming = 4*temperature + np.random.normal(0, 15, 100)`. Посчитай корреляцию `ice_cream` и `swimming`.

**17. Почему они связаны?** *(коротко в Markdown-ячейке)* Корреляция мороженого и купаний высокая, хотя одно не вызывает другое. Назови одной фразой скрытую переменную, которая тайно управляет обоими.

---

## Что проверит преподаватель

| Что | Зачёт ✅ |
|-----|----------|
| Считает mean / median / std правильно | |
| Понимает, когда среднее обманывает | |
| Строит гистограмму, знает правило 68-95-99.7 | |
| Считает корреляцию | |
| **🔴:** понимает «корреляция ≠ причина» (задача 17) | |

> ✅ **Сделал 🟢 — видишь данные через числа. Осилил 🟡 — читаешь распределения по форме. Взял 🔴 — не попадёшься в ловушку корреляции.**
>
> **Минимум для зачёта:** 🟢 целиком + задачи 7–10 из 🟡.
