# Урок 1.6. Самостоятельная работа на уроке

> **В этом задании ты сам считаешь статистики, строишь графики, ищешь корреляции.** Я даю **постановку**, ты пишешь код. После каждой задачи — **вопросы на размышление**.
>
> Тетрадка: продолжай — `lesson-1-6-stats.ipynb`. Раздели: `## Самостоятельная работа`.

---

## Шпаргалка (минимум команд)

```python
import numpy as np
import matplotlib.pyplot as plt

# Меры центра и разброса
a.mean(), np.median(a), a.std(), a.var()

# Стандартизация
z = (a - a.mean()) / a.std()

# Распределения
np.random.uniform(low, high, n)
np.random.normal(mean, std, n)
np.random.randint(low, high, n)

# Гистограмма
plt.hist(data, bins=30)

# Корреляция
np.corrcoef(a, b)[0, 1]
```

---

## Уровень 1. Обязательная база

### 1.1. Описать выборку

> **Задача:** для массива баллов ЕГЭ посчитать **6 статистик**.

```python
scores = np.array([72, 88, 95, 65, 78, 82, 60, 91, 85, 76, 100, 55, 70, 84, 89])
```

**Что нужно:**

- Среднее (округлить до 2 знаков).
- Медиана.
- Минимум, максимум.
- Размах (макс − мин).
- Стандартное отклонение.
- Дисперсия.

**Подсказка по форматированию:** `f"{val:.2f}"`.

**🤔 Вопросы:**

1. Сравни **среднее** и **медиану**. Какое из них больше? О чём это говорит про распределение?
2. Что показывает **размах**? Чем он отличается от std как меры разброса?
3. Чему равна **дисперсия** в твоём ответе? Как связаны `std` и `var` математически? Проверь: `std² == var`?

---

### 1.2. Где обманывает среднее

> **Задача:** на «зарплатах» показать, **когда среднее обманывает**.

```python
salaries = np.array([35, 40, 42, 45, 50, 55, 60, 800])
```

> Это 8 сотрудников фирмы. Семеро — 35-60, один (директор) — 800.

**Что сделать:**

1. Посчитай среднее и медиану.
2. **Напиши в Markdown:** какое число «честнее» представляет типичную зарплату?
3. **Удали** директора (`salaries[:-1]`) и посчитай заново. Сильно ли изменилось среднее? А медиана?

**🤔 Вопросы:**

1. Если бы ты пришёл на собеседование в эту фирму, какое число тебе **показали** бы (среднее или медиану), чтобы привлечь? А какое **честнее**?
2. Назови **3 ситуации из жизни**, где среднее обманывает (помимо зарплат).
3. Когда стоит использовать **среднее**, а когда — **медиану**? Сформулируй правило.

---

### 1.3. Стандартизация

> **Задача:** превратить массив так, чтобы среднее стало 0, std = 1.

```python
ages = np.array([12, 13, 14, 15, 15, 16, 16, 17, 17, 18])
```

**Что сделать:**

1. Стандартизуй через формулу `z = (x - mean) / std`.
2. Распечатай новый массив.
3. **Проверь:** среднее ≈ 0, std ≈ 1.

**🤔 Вопросы:**

1. Среднее `z` получится **точно 0** или близко? Если близко (например, `-1.11e-16`) — что это за число? Почему не ноль?
2. Что **физически** означает значение `z = -1.5` для конкретного возраста? А `z = +2`?
3. Можно ли стандартизировать **строку**? А `[1, 1, 1, 1]` (все одинаковые)? Что произойдёт?

---

## Уровень 2. Распределения и сравнения

### 2.1. Симуляция кубика

> **Задача:** «бросить» кубик 1000 раз и посмотреть распределение.

**Сгенерируй:**
```python
np.random.seed(0)
rolls = np.random.randint(?, ?, 1000)
```

**Подсказка:** `randint(low, high)` — `high` **не входит**! Какие границы для «1-6»?

**Что нужно:**

1. Распечатай среднее и медиану.
2. **Какое теоретическое среднее** у честного кубика?
3. Построй гистограмму с **6 столбиками**.

**🤔 Вопросы:**

1. Получилось ли среднее **точно 3.5**? Или близко? Сколько шагов до точного 3.5?
2. **Сначала предскажи**, потом проверь: как изменится среднее, если бросков будет **10 000 000**?
3. На гистограмме все столбики **одной высоты**? Если нет — почему? Это «ошибка»?

---

### 2.2. Симуляция роста

> **Задача:** сгенерировать рост 1000 человек и **проверить правило 68-95-99.7**.

```python
np.random.seed(0)
heights = np.random.normal(170, 8, 1000)
```

**Что нужно:**

1. Построй гистограмму с 40 столбиками. Добавь красную **вертикальную линию** на среднем (170).
2. Посчитай **долю** людей выше 180.
3. Посчитай **долю** людей в пределах **±1 std** от среднего (162..178).

**Подсказка для (3):** маска `(heights >= 162) & (heights <= 178)`, потом `.sum() / len(...)`.

**🤔 Вопросы:**

1. Доля в ±1 std получилась около **68%**? Это и есть **правило 68-95-99.7**. Что оно означает?
2. **Сначала предскажи**: какая доля людей в пределах ±2 std (154..186)? А ±3 std?
3. Это правило работает **для любого** распределения или только для нормального?

---

### 2.3. Сравнение двух классов

> **Задача:** сравнить два класса с **разным разбросом** оценок.

```python
np.random.seed(0)
class_a = np.random.normal(4.0, 0.3, 30)    # std маленький
class_b = np.random.normal(3.7, 0.7, 30)    # std большой
```

**Что нужно:**

1. Распечатай среднее и std каждого.
2. Построй **две гистограммы** на одном графике с `alpha=0.5`.
3. **Напиши в Markdown:** какой класс ты бы выбрал как «более сильный» и **почему**.

**🤔 Вопросы:**

1. У Класса А **выше** среднее. У Класса Б **больше** разброс. Какой ты выбрал и почему?
2. **Подумай**: в каком классе больше **отличников** (оценки ≥ 4.5)? Сначала предскажи, потом проверь.
3. **А в каком больше «двоечников»** (оценки < 3)? Что это говорит о пользе анализа `std`?

---

## Уровень 3. Со звёздочкой — корреляция

### 3.1. Реальные данные — часы учёбы и оценки

> **Задача:** найти корреляцию между двумя величинами.

```python
hours = np.array([2, 3, 5, 4, 7, 6, 8, 10, 9, 12, 11, 14])
grades = np.array([3.2, 3.5, 4.0, 3.8, 4.2, 4.0, 4.5, 4.7, 4.6, 4.9, 4.8, 5.0])
```

**Что сделать:**

1. Посчитай корреляцию через `np.corrcoef(...)[0, 1]`.
2. Построй scatter plot.
3. **Напиши вывод словами.**

**🤔 Вопросы:**

1. Корреляция близка к 1. Можно ли утверждать «**учёба вызывает** высокие оценки»? Что значит «корреляция ≠ причина»?
2. Какие **3 фактора**, кроме часов учёбы, могут влиять на оценку?
3. Что произойдёт, если **одну точку** сместить далеко (например, `(100, 1.0)`)? Как корреляция изменится? Проверь.

---

### 3.2. Корреляционная матрица

> **Задача:** посчитать корреляции **между 4 предметами**.

```python
np.random.seed(42)
n = 100
math = np.random.normal(4.0, 0.5, n)
physics = math * 0.8 + np.random.normal(0, 0.3, n)
history = np.random.normal(4.0, 0.5, n)
chemistry = math * 0.7 + np.random.normal(0, 0.4, n)

data = np.array([math, physics, history, chemistry])
```

**Что нужно:**

1. Получить **матрицу 4×4** через `np.corrcoef(data)`.
2. Округлить до 2 знаков.
3. Найти, **какие предметы связаны сильнее всего**.

**🤔 Вопросы:**

1. Почему **диагональ** всегда равна 1?
2. Почему `physics` и `chemistry` коррелируют? Они же **отдельно** были созданы — посмотри на код.
3. `history` ≈ 0 со всеми остальными. Что это **физически** означает?

---

### 3.3. Ловушка корреляции

> **Задача:** показать **классическую ловушку** — высокая корреляция без причинной связи.

```python
np.random.seed(0)
temperature = np.random.uniform(0, 35, 100)
ice_cream = 5 * temperature + np.random.normal(0, 10, 100)
swimming = 4 * temperature + np.random.normal(0, 15, 100)
```

**Что нужно:**

1. Посчитай корреляцию `ice_cream` и `swimming`.
2. **Напиши в Markdown ответ на вопрос:** «Почему она высокая, хотя одно НЕ вызывает другое?»

**🤔 Вопросы:**

1. Это пример **скрытой переменной**. Что это за переменная в нашем случае?
2. Назови **3 примера из жизни**, где есть высокая корреляция, но **нет** причинной связи (вспомни мороженое и утопающих с урока).
3. Как **проверить**, действительно ли один фактор влияет на другой, а не корреляция «случайная»? Подсказка: эксперимент.

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Считает mean / median / std правильно | |
| Понимает, когда среднее обманывает | |
| Может построить гистограмму | |
| Знает правило 68-95-99.7 | |
| **Ответил на вопросы размышления** | |
| **Уровень 3:** понимает «корреляция ≠ причина» | |

**Минимум:** все 3 задачи Уровня 1 + **ответы на вопросы**.

> **Главное сегодня — научиться смотреть НА ДАННЫЕ через числа.** Среднее, std, корреляция — это «очки», через которые мы видим, что в данных происходит.
