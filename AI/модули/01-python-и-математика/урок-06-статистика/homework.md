# Урок 1.6. Самостоятельная работа: статистика

> **Сегодня учимся «видеть» данные через числа.** Среднее, разброс, корреляция — это очки, через которые дата-сайентист смотрит на любой набор данных. Задачи все жизненные: баллы ЕГЭ, зарплаты, кубик, рост людей.
>
> **Как работать:**
> - Считай и **сразу смотри на результат** — статистика оживает, только когда видишь числа.
> - Где просят «напиши в Markdown» — это короткий **вывод по данным** одной-двумя фразами, не сочинение.
> - Короткий **🤔 вопрос** после задачи — ответь парой фраз.
> - Тетрадка та же — `lesson-1-6-stats.ipynb`, раздел `## Самостоятельная работа`.

---

## Шпаргалка (подглядывать можно)

```python
import numpy as np
import matplotlib.pyplot as plt

a.mean(), np.median(a), a.std(), a.var()   # центр и разброс
z = (a - a.mean()) / a.std()               # стандартизация
np.random.uniform(low, high, n)            # равномерное
np.random.normal(mean, std, n)             # нормальное (колокол)
np.random.randint(low, high, n)            # целые (high не входит!)
plt.hist(data, bins=30)                    # гистограмма
np.corrcoef(a, b)[0, 1]                     # корреляция
```

---

## Уровень 1. Обязательная база

### 1.1. Портрет класса по баллам ЕГЭ

Представь, что ты классный руководитель и хочешь одним взглядом понять, как сдал класс.

```python
scores = np.array([72, 88, 95, 65, 78, 82, 60, 91, 85, 76, 100, 55, 70, 84, 89])
```

Посчитай и красиво выведи: среднее (до 2 знаков), медиану, минимум, максимум, размах (макс − мин), стандартное отклонение и дисперсию.

**🤔 Вопрос:** сравни среднее и медиану — какое больше? О чём это говорит: есть ли в классе «выбивающиеся» результаты?

---

### 1.2. Где среднее обманывает

Классическая ловушка: одно огромное число «утягивает» среднее и создаёт ложную картину.

```python
salaries = np.array([35, 40, 42, 45, 50, 55, 60, 800])   # 7 сотрудников и директор (800)
```

**Сделай:**
1. Посчитай среднее и медиану всей восьмёрки.
2. Убери директора (`salaries[:-1]`) и посчитай заново — что сильнее «дёрнулось»: среднее или медиана?
3. В Markdown коротко: какое число честнее описывает «типичную» зарплату?

**🤔 Вопрос:** какое из двух чисел показали бы тебе на собеседовании, чтобы фирма выглядела привлекательнее, — и почему это нечестно?

---

### 1.3. Стандартизация

Приём, без которого не живёт половина ML: сдвинуть данные так, чтобы среднее стало 0, а разброс — 1.

```python
ages = np.array([12, 13, 14, 15, 15, 16, 16, 17, 17, 18])
```

**Сделай:** стандартизуй по формуле `z = (x - mean) / std`, выведи новый массив и **проверь**, что его среднее ≈ 0, а std ≈ 1.

**🤔 Вопрос:** что физически означает `z = -1.5` для конкретного возраста, а что — `z = +2`? (Подсказка: на сколько «шагов-std» человек от среднего и в какую сторону.)

---

## Уровень 2. Распределения и сравнения

> Подсказок меньше — форму распределений ты уже видел на уроке.

### 2.1. Бросаем кубик 1000 раз

Проверим на симуляции, правда ли «в среднем на кубике 3.5».

```python
np.random.seed(0)
rolls = np.random.randint(?, ?, 1000)   # подставь границы для чисел 1–6
```

**Сделай:** подбери правильные границы (помни: верхняя в `randint` не входит!), посчитай среднее и медиану, построй гистограмму с 6 столбиками.

**🤔 Вопрос:** среднее вышло ровно 3.5 или чуть рядом? Сначала **предскажи**, а потом мысленно ответь: что будет со средним, если бросков станет 10 миллионов?

---

### 2.2. Рост 1000 человек и правило 68-95-99.7

```python
np.random.seed(0)
heights = np.random.normal(170, 8, 1000)
```

**Сделай:**
1. Построй гистограмму (40 столбиков) и добавь красную вертикальную линию на среднем (170).
2. Посчитай долю людей выше 180.
3. Посчитай долю людей в пределах ±1 std от среднего (162…178).

> **Направление для пункта 3:** собери маску «в диапазоне» и посчитай, какая доля от всех в неё попала (сколько True делить на общее число).

**🤔 Вопрос:** доля в ±1 std получилась около 68%? Сначала **предскажи**, какой будет доля в ±2 std, а потом проверь.

---

### 2.3. Два класса с разным разбросом

```python
np.random.seed(0)
class_a = np.random.normal(4.0, 0.3, 30)    # разброс маленький
class_b = np.random.normal(3.7, 0.7, 30)    # разброс большой
```

**Сделай:** выведи среднее и std каждого класса, построй две гистограммы на одном графике (`alpha=0.5`) и в Markdown коротко напиши, какой класс назвал бы «сильнее» и почему.

**🤔 Вопрос:** у класса А выше среднее, у класса Б — больше разброс. Как думаешь, в каком из них больше и отличников, и двоечников одновременно? Проверь догадку.

---

## Уровень 3. Со звёздочкой — корреляция

> Только направление — остальное сам.

### 3.1. Часы учёбы и оценки

```python
hours = np.array([2, 3, 5, 4, 7, 6, 8, 10, 9, 12, 11, 14])
grades = np.array([3.2, 3.5, 4.0, 3.8, 4.2, 4.0, 4.5, 4.7, 4.6, 4.9, 4.8, 5.0])
```

**Сделай:** посчитай корреляцию, построй scatter plot и в Markdown напиши вывод одной фразой.

**🤔 Вопрос:** корреляция близка к 1. Можно ли из этого делать вывод «учёба **вызывает** высокие оценки»? Что означает «корреляция ≠ причина»?

---

### 3.2. Корреляционная матрица предметов

```python
np.random.seed(42)
n = 100
math = np.random.normal(4.0, 0.5, n)
physics = math * 0.8 + np.random.normal(0, 0.3, n)
history = np.random.normal(4.0, 0.5, n)
chemistry = math * 0.7 + np.random.normal(0, 0.4, n)

data = np.array([math, physics, history, chemistry])
```

**Сделай:** получи матрицу 4×4 через `np.corrcoef(data)`, округли до 2 знаков и определи, какие предметы связаны сильнее всего.

**🤔 Вопрос:** `physics` и `chemistry` создавались **отдельными** строчками кода — почему же они всё равно коррелируют друг с другом? (Посмотри внимательно, из чего собран каждый.)

---

### 3.3. Ловушка корреляции

```python
np.random.seed(0)
temperature = np.random.uniform(0, 35, 100)
ice_cream = 5 * temperature + np.random.normal(0, 10, 100)
swimming = 4 * temperature + np.random.normal(0, 15, 100)
```

**Сделай:** посчитай корреляцию `ice_cream` и `swimming` и в Markdown ответь: почему она высокая, хотя одно точно не вызывает другое?

**🤔 Вопрос:** какая **скрытая переменная** здесь тайно управляет обоими показателями? Назови ещё пару похожих примеров из жизни.

---

## Что проверит преподаватель

| Что | Зачёт ✅ |
|-----|----------|
| Считает mean / median / std правильно | |
| Понимает, когда среднее обманывает | |
| Умеет построить гистограмму | |
| Знает правило 68-95-99.7 | |
| **Уровень 3:** понимает «корреляция ≠ причина» | |

**Минимум для зачёта:** все 3 задачи Уровня 1 (написаны и запущены).

> Главное сегодня — привыкнуть **смотреть на данные через числа**. Среднее, std и корреляция — это «очки», через которые видно, что на самом деле происходит внутри данных.
