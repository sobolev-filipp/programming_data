# Урок 1.3. Самостоятельная работа на уроке

> **В этом задании ты применяешь `zip`, `enumerate`, `sorted`, агрегации, методы строк.** Я даю **постановку**, ты пишешь код. После каждой задачи — **вопросы на размышление**.
>
> Тетрадка: продолжай — `lesson-1-3-utilities.ipynb`. Раздели: `## Самостоятельная работа`.

---

## Шпаргалка (минимум команд)

```python
zip(a, b)                       # параллельный проход
enumerate(coll, start=1)        # индекс + значение
sorted(coll, key=..., reverse=) # сортировка
sum(coll), min(coll, key=)      # агрегации
all(...), any(...)              # все/хоть один
"a,b".split(",")                # ['a', 'b']
",".join(["a","b"])             # "a,b"
"hello".startswith("h")         # True
```

---

## Уровень 1. Обязательная база

### 1.1. Объединить в словарь

> **Задача:** объединить два списка в словарь **в одну строку**.

```python
fruits = ["яблоко", "банан", "вишня", "груша"]
prices = [80, 60, 150, 90]
```

**Подсказки:**
- `zip(...)` даст пары `('яблоко', 80)`, `('банан', 60)`...
- `dict(...)` превращает пары в словарь.

**🤔 Вопросы:**

1. Что произойдёт, если списки **разной длины** (например, к `fruits` добавить `"слива"`)? Какой будет результат?
2. Сравни два подхода: `dict(zip(a, b))` и `{f: p for f, p in zip(a, b)}`. Какой короче? Какой гибче? Когда лучше использовать какой?
3. Можно ли через `zip` объединить **три** списка в один словарь? Если нет — почему?

---

### 1.2. Пронумерованный список

> **Задача:** распечатать список дел с номерами 1, 2, 3, 4.

```python
todo = ["учить Python", "сделать ДЗ", "посмотреть фильм", "позвонить маме"]
```

**Что должно вывестись:**
```
1. учить Python
2. сделать ДЗ
3. посмотреть фильм
4. позвонить маме
```

**Подсказки:**
- `enumerate(...)` даёт пары `(индекс, значение)`.
- Распакуй пару в две переменные внутри `for`.
- Параметр `start=1` чтобы нумерация шла с 1.

**🤔 Вопросы:**

1. Что произойдёт, если убрать `start=1`? Откуда начнётся нумерация?
2. Можно ли сделать **то же самое** через `for i in range(len(todo))`? Сравни два подхода — какой читаемее?
3. Что вернёт `list(enumerate(todo, start=10))`? Опиши формат.

---

### 1.3. Сортировка слов

> **Задача:** отсортировать список слов **тремя разными способами**.

```python
words = ["машина", "ai", "python", "ml", "нейросеть", "kaggle"]
```

**Способы:**

1. **По длине, от коротких к длинным.**
   - Подсказка: `sorted(words, key=...)`. Что подставить в `key`?

2. **В алфавитном порядке.**
   - Подсказка: `sorted` без `key`.

3. **По длине, от длинных к коротким.**
   - Подсказка: добавить `reverse=True`.

**🤔 Вопросы:**

1. Какие слова в алфавитном порядке окажутся в начале — кириллические или латинские? **Сначала предположи**, потом запусти и проверь.
2. Что произойдёт, если два слова имеют **одинаковую длину**? В каком порядке они окажутся в результате?
3. Можно ли сортировать **на месте** (изменяя `words`)? Какой метод нужен и чем он отличается от `sorted`?

---

### 1.4. Агрегации — погода за неделю

> **Задача:** посчитать статистику температур и найти самый тёплый день.

```python
days = ["пн", "вт", "ср", "чт", "пт", "сб", "вс"]
temps = [18, 22, 20, 23, 17, 19, 25]
```

**Что нужно:**

1. Среднюю температуру (округли до 1 знака).
2. Минимум и максимум.
3. День (название), когда было теплее всего.

**Подсказки для пункта 3:**
- **Способ 1:** найди индекс максимума через `.index(max(...))`, потом `days[индекс]`.
- **Способ 2:** через `max + lambda` на парах из `zip`.

**🤔 Вопросы:**

1. Что произойдёт, если **два дня** имеют одинаковую максимальную температуру? Какой выберется?
2. Что вернёт `temps.index(100)` — числа, которого нет в списке?
3. Какой из двух способов поиска самого тёплого дня тебе кажется **читаемее**? Почему?

---

## Уровень 2. Связка с предыдущими уроками

### 2.1. Топ-3 учеников по среднему

> **Задача:** найти **3 лучших** учеников по среднему баллу.

```python
students = [
    {"name": "Иван",  "grades": [4, 5, 5, 3, 4]},
    {"name": "Мария", "grades": [5, 5, 5, 4, 5]},
    {"name": "Пётр",  "grades": [3, 3, 4, 3, 4]},
    {"name": "Ольга", "grades": [4, 4, 5, 5, 4]},
    {"name": "Алина", "grades": [5, 4, 4, 5, 5]},
]
```

**Ожидаемый вывод:**
```
1. Мария — 4.80
2. Алина — 4.60
3. Ольга — 4.40
```

**План:**

1. Добавь к каждому словарю поле `"avg"` (через цикл).
2. Отсортируй по `"avg"` по убыванию.
3. Возьми первых 3.
4. Распечатай с нумерацией (через `enumerate`).

**Подсказка:** `{s['avg']:.2f}` — формат до 2 знаков.

**🤔 Вопросы:**

1. Можно ли решить **без** добавления `"avg"` в словари? Если да — как?
2. Какой алгоритм быстрее для **миллиона** учеников: «отсортировать всех» или «найти 3 максимума через цикл»?
3. Что произойдёт, если у двух учеников **одинаковый** средний балл? Кто окажется выше в топе?

---

### 2.2. Слова, начинающиеся с гласной

> **Задача:** из строки выбрать **слова**, начинающиеся с гласной.

```python
text = "искусственный интеллект учится сам и помогает человеку открывать новое"
vowels = "аеёиоуыэюя"
```

**Подсказки:**
- `text.split()` — разбивает на слова.
- `w[0]` — первая буква.
- `w[0] in vowels` — проверка.

**🤔 Вопросы:**

1. Сколько слов в ответе? Подсчитай.
2. Что произойдёт, если в тексте есть слова с **заглавной** буквы (например, «Искусственный»)? Будут ли они в ответе?
3. Как изменить код, чтобы регистр **не имел значения**?

---

### 2.3. Класс `Product` + утилиты

> **Задача:** соединить **ООП из 1.1** и **утилиты из 1.3**.

**Что сделать:**

1. **Напиши класс `Product`** с атрибутами `name, price, quantity` и методом `total_value()` (возвращает `price × quantity`).
2. **Создай список** из 5 продуктов с разными значениями.
3. **Отсортируй** по убыванию `total_value` — через `sorted + lambda`.
4. **Найди** самый дорогой за единицу — через `max + lambda`.
5. **Проверь** через `all`, все ли в наличии (`quantity > 0`).

**🤔 Вопросы:**

1. Почему в `key=lambda p: p.total_value()` обязательны **скобки** у `total_value`?
2. Что произойдёт, если ты напишешь `key=lambda p: p.total_value` (без скобок)? Будет ли ошибка?
3. Какой результат `all([])` (пустой список)? Удивительно? Объясни почему так.

---

## Уровень 3. Со звёздочкой

### 3.1. Палиндромы

> **Задача:** найти все слова, которые читаются **одинаково** в обе стороны (с учётом регистра).

```python
words = ["шалаш", "топот", "Анна", "потом", "тут", "молоко", "АРБА"]
```

**Подсказки:**
- `w[::-1]` — перевёрнутая строка.
- Регистр важен — используй `.lower()` для обоих сравнений.

**🤔 Вопросы:**

1. Сколько палиндромов в списке? Перечисли.
2. Является ли пустая строка `""` палиндромом? А одна буква `"а"`?
3. Как проверить, является ли **фраза** палиндромом, если убрать пробелы (например, «А роза упала на лапу Азора»)?

---

### 3.2. Гистограмма частот букв

> **Задача:** для каждой буквы текста посчитать, **сколько раз** она встречается, и вывести **топ-5**.

```python
text = "программирование на python это интересно"
```

**Подсказки:**
- Уникальные символы — `set(text)`.
- Подсчёт — `text.count(c)`.
- Исключи пробелы.
- Топ-5 — через `sorted` с ключом и срез `[:5]`.

**🤔 Вопросы:**

1. Какие буквы оказались самыми частыми? Совпадает ли это с общей статистикой русского языка (самые частые — «о», «е»)?
2. Что вернёт твой код для пустой строки?
3. Подумай: метод `text.count(c)` для каждой уникальной буквы — насколько это **эффективно**? Можно ли быстрее? Подсказка: `collections.Counter`.

---

### 3.3. Анализ файла

> **Задача:** **записать** текст в файл, потом **прочитать** его и сделать анализ.

**План:**

1. **Создай** файл `text.txt` с любым текстом из 5-10 строк (через `with open("text.txt", "w", encoding="utf-8")`).
2. **Прочитай** обратно через `with open("text.txt", "r", encoding="utf-8")` и `f.readlines()`.
3. **Посчитай:**
   - Количество строк.
   - Количество слов (всего).
   - Самое длинное слово.
   - Среднюю длину слова (округли до 1 знака).
4. **Сохрани** результат в новый файл `analysis.txt`.

**Подсказки:**
- `f.readlines()` возвращает список строк.
- Все слова из всех строк — через **вложенный** генератор: `[w for line in lines for w in line.split()]`.
- `max(words, key=len)` — длиннейшее.

**🤔 Вопросы:**

1. Зачем нужен `encoding="utf-8"`? Что произойдёт без него на Windows?
2. В чём разница между `f.read()` (без `s`) и `f.readlines()`? Когда что использовать?
3. Что если файл **не существует**? Какая будет ошибка? Как её обработать?

---

## Что преподаватель проверит

| Что | Зачёт ✅ |
|-----|----------|
| Использует `zip`/`enumerate`/`sorted` правильно | |
| Знает разницу `.sort()` vs `sorted()` | |
| Использует генераторы (не `for + append`) | |
| **Уровень 2:** соединяет ООП и утилиты | |
| **Ответил на вопросы размышления** | |

**Минимум для «зачёта»:** все 4 задачи Уровня 1 + 1 задача Уровня 2 + **ответы на вопросы**.

> **Без ответов на вопросы — задача не считается решённой.** Главное — **понять**, а не просто прогнать код.
