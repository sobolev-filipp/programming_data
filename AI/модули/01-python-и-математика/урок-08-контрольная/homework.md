# Контрольная работа по Модулю 1

> **Время:** 65 минут.
> **Тетрадка:** `lesson-1-8-control.ipynb` в твоём GitHub-репозитории.
> **Можно:** документация NumPy, твои `summary.md`.
> **Нельзя:** ChatGPT/Claude, помощь одноклассников.

> **Совет:** делай задачу за задачей. Не сидишь на одной дольше 8 минут — переходи к следующей.

---

## Задача 1. ООП (10 баллов)

Реализуй класс `Movie`:

- Конструктор: `title` (строка), `year` (число), `rating` (от 1.0 до 10.0).
- Атрибут `is_classic` — `True`, если фильм вышел до 2000 года И rating ≥ 8.0.
- Метод `description()` — возвращает строку вида: `«Матрица» (1999) — рейтинг 8.7`.
- Перегрузи `__repr__` — пусть `print(m)` показывает: `Movie('Матрица', 1999, 8.7)`.

Создай 2 объекта и проверь.

---

## Задача 2. Генераторы списка + lambda (10 баллов)

Дан список чисел:
```python
nums = [12, -5, 8, 0, -3, 7, 15, -1, 9, 4]
```

Сделай **каждую задачу одной строкой**:

**А)** Получи список **кубов** всех **положительных** чисел.
**Б)** Получи **словарь** `{число: его_квадрат}` для всех чисел `> 5`.
**В)** Отсортируй список по **модулю** (от 0 к большим), используя `sorted` и `lambda`.

---

## Задача 3. Утилиты Python (10 баллов)

Даны два списка:
```python
students = ["Иван", "Мария", "Пётр", "Ольга", "Алексей"]
scores = [85, 92, 78, 88, 95]
```

**А)** Распечатай:
```
1. Иван: 85
2. Мария: 92
3. Пётр: 78
4. Ольга: 88
5. Алексей: 95
```

**Б)** Создай словарь `{имя: балл}` одной строкой.

**В)** Распечатай учеников в порядке **возрастания** баллов:
```
Пётр: 78
Иван: 85
Ольга: 88
Мария: 92
Алексей: 95
```

---

## Задача 4. NumPy (15 баллов)

**А)** Создай матрицу 4×4 со случайными целыми числами от 1 до 20 (`np.random.randint`). Зафиксируй seed = 0.

**Б)** Распечатай:
- Среднее всей матрицы.
- Сумму по строкам.
- Сумму по столбцам.
- Максимальный элемент в каждой строке.

**В)** Замени все числа `< 10` на 0 (через булеву маску).

**Г)** Стандартизуй матрицу: `(m - m.mean()) / m.std()`. Проверь, что среднее новой матрицы ≈ 0, std ≈ 1.

---

## Задача 5. Линейная алгебра (15 баллов)

**А)** Даны два вектора:
```python
a = np.array([3, -2, 4, 1])
b = np.array([1, 5, -3, 2])
```

Посчитай **тремя способами**:
- Через `np.dot`.
- Через `@`.
- Через `(a * b).sum()`.

Все три должны дать **одно и то же** число.

**Б)** Длина вектора `a` (через `np.linalg.norm`).

**В)** Косинусная похожесть `a` и `b`. Формула:
```
sim = (a @ b) / (|a| * |b|)
```

**Г)** Даны матрицы:
```python
W = np.array([[0.2, 0.5, -0.1],
              [0.4, -0.3, 0.6]])
x = np.array([1.0, 2.0, 3.0])
```

Посчитай **«выход слоя нейросети»**: `W @ x`. Какая форма получится? Какие 2 числа?

---

## Задача 6. Статистика (15 баллов)

Дан массив температур за месяц (30 значений):
```python
np.random.seed(42)
temps = np.random.normal(20, 5, 30)
```

**А)** Распечатай:
- Среднюю температуру (округли до 1 знака).
- Медиану.
- Стандартное отклонение.
- Минимум и максимум.

**Б)** Сколько дней было «теплее обычного» (выше среднего)?

**В)** Сгенерируй второй массив:
```python
ice_cream_sales = temps * 5 + np.random.normal(0, 2, 30)
```
Это «продажи мороженого». Посчитай **корреляцию** между `temps` и `ice_cream_sales`. Округли до 2 знаков.

**Г)** Дополнительно — **в комментарии**: можно ли по корреляции сделать вывод, что **температура вызывает рост продаж мороженого**? Объясни.

---

## Задача 7. Производная и градиентный спуск (25 баллов)

**А)** Реализуй функцию численной производной (если ещё не было в тетрадке):
```python
def derivative(f, x, h=1e-4):
    return (f(x + h) - f(x - h)) / (2 * h)
```

**Б)** Дана функция `f(x) = (x - 4)**2 + 2`. Посчитай производную в точках `x = 0, 2, 4, 6, 8`. Где функция растёт, где убывает, где минимум?

**В)** Найди минимум этой функции через градиентный спуск:
- старт: `x = 0.0`
- `lr = 0.1`
- 100 шагов

Распечатай итоговое `x` и `f(x)`. Какое ожидаемое значение? (`x = 4`, `f(x) = 2`)

**Г)** Сделай **тоже самое для функции двух переменных** `g(p) = (p[0] - 2)**2 + (p[1] + 3)**2`:
- старт: `[0.0, 0.0]`
- `lr = 0.1`
- 100 шагов

Какой ожидаемый минимум? (`[2, -3]`, `g = 0`)

---

## ★ Бонус (20 баллов)

> **Решать ТОЛЬКО** если задачи 1–7 готовы.

Тебе дано:
```python
np.random.seed(0)
x_data = np.linspace(0, 10, 30)
y_data = 2 * x_data + 5 + np.random.normal(0, 1, 30)
```

Это «зашумлённые» данные, на которые ты должен построить линию `y = w * x + b`. Цель: найти такие `w` и `b`, чтобы линия проходила как можно ближе ко всем точкам.

**Шаги:**

1. Определи **функцию потерь** (loss): сумма квадратов разностей между `y_data` и `predict(w, b, x_data)`:
```python
def predict(w, b, x):
    return w * x + b

def loss(params):
    w, b = params[0], params[1]
    return ((predict(w, b, x_data) - y_data) ** 2).sum()
```

2. Реализуй численный градиент **функции двух переменных** (как было на уроке 1.7).

3. Запусти градиентный спуск:
- старт: `[0.0, 0.0]`
- `lr = 0.001`
- 1000 шагов

4. Распечатай итоговые `w` и `b`. Сравни с ожидаемыми (`w ≈ 2`, `b ≈ 5`).

5. **Бонус-бонус**: построй scatter plot `x_data` vs `y_data` + линию `predict` с найденными `w, b`.

---

## Сдача

1. Сохрани `lesson-1-8-control.ipynb` в Colab.
2. Скачай файл к себе на компьютер (`File → Download .ipynb`).
3. Загрузи в свой GitHub-репозиторий в папку `homework/lesson-1-8/`.
4. Сделай коммит с сообщением `Контрольная по Модулю 1`.

**Покажи преподавателю**, что файл появился на GitHub. Только после этого работа считается сданной.

---

## Шкала оценок

| Баллов | Оценка |
|--------|--------|
| 90+ | Отлично |
| 70–89 | Хорошо |
| 50–69 | Удовлетворительно |
| <50 | Пересдача |

**Удачи!**
