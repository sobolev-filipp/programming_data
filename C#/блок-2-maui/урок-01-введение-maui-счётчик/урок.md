# Урок 1 (Блок 2). Введение в MAUI + приложение «Счётчик» 📱

**Блок 2 · Урок 1 | Первое приложение с окном и кнопками**

> 🎯 **Сегодня мы:**
> 1. поймём, что такое **MAUI** и чем приложение отличается от консоли;
> 2. **настроим среду** (Visual Studio на Windows / VS Code на Mac);
> 3. **создадим первый проект** и запустим его — на экране появится настоящее окно;
> 4. разберём, из чего состоит проект (`MainPage.xaml`, `MainPage.xaml.cs` и др.);
> 5. соберём своё приложение **«Счётчик»** с кнопками **+1**, **−1**, **Сброс**.
>
> ⏳ Урок большой и важный — идём **маленькими шагами**. После каждого шага **запускаем и смотрим результат**. Не спеши: сегодня главное — не написать много кода, а понять, **как устроено приложение**.

---

## 🌉 Часть 0. Из чего мы вышли (мостик из блока 1)

Весь блок 1 мы писали **консольные** программы. Они общались с человеком через **чёрное окно с текстом**:

```csharp
Console.WriteLine("Привет!");            // показать текст
string name = Console.ReadLine();        // прочитать, что ввёл человек
```

Это работало, но выглядит «по-программистски»: ни кнопок, ни картинок, только строки. А телефонные и компьютерные приложения, которыми ты пользуешься каждый день, — это **окна с кнопками, полями и списками**. Сегодня мы научимся делать **такие**.

| Было (консоль, блок 1) | Стало (MAUI, блок 2) |
|------------------------|----------------------|
| `Console.WriteLine("текст")` | текст на экране — элемент **`Label`** |
| `Console.ReadLine()` | поле ввода — элемент **`Entry`** |
| «нажми Enter» | нажатие на **`Button`** (кнопку) |
| одно чёрное окно | настоящее окно приложения |

> 💡 **Хорошая новость:** *логику* (переменные, `if`, методы, классы) мы уже знаем из блока 1 — она никуда не делась. Просто теперь у программы появляется **лицо** — экран, на который смотрит человек.

---

## 📱 Часть 1. Что такое MAUI (теория, 8 мин)

**MAUI** (читается «ма́уи», полностью — *.NET Multi-platform App UI*) — это инструмент от Microsoft, чтобы делать **приложения с окном** на языке C#.

Главная суперсила MAUI — **«напиши один раз — запусти везде»**:

- 🪟 **Windows** (компьютер),
- 🤖 **Android** (телефон/планшет),
- 🍎 **iOS** (айфон) и **macOS** (мак).

Ты пишешь **одну** программу, а MAUI сам превращает её в приложение для каждой из этих систем. Раньше пришлось бы писать четыре разные программы на четырёх языках!

### Из чего состоит приложение: две части

Любой экран приложения MAUI описывается **двумя файлами** — как у человека есть **внешность** и **характер**:

| Файл | Язык | Отвечает за | Аналогия |
|------|------|-------------|----------|
| `MainPage.xaml` | **XAML** | **как выглядит** (кнопки, текст, цвета, расположение) | внешность 👗 |
| `MainPage.xaml.cs` | **C#** | **что делает** (что происходит при нажатии) | характер 🧠 |

- **XAML** (читается «зэ́мл») — это язык **разметки**, похожий на HTML. Им мы *рисуем* экран: «здесь текст, тут кнопка, а тут картинка».
- **C#** — уже знакомый язык. Здесь мы пишем **логику**: «когда нажали кнопку — увеличить число».

Файл `.xaml.cs` называют **code-behind** («код позади») — он «стоит за спиной» у внешнего вида и оживляет его.

> 💡 **Лайфхак для понимания:** XAML — это *скелет и внешность* экрана, а C# — *мозг*. Кнопку рисуем в XAML, а что она делает — пишем в C#. Эти два файла всегда работают в паре.

✍️ **Мини-задание 1 (устно).** Ответь своими словами:
1. Что такое MAUI и в чём его суперсила?
2. Какой файл отвечает за внешность экрана, а какой — за поведение?
3. На какой язык из блока 1 похож `.xaml.cs`?

---

## ⚙️ Часть 2. Настройка среды (обязательно, делаем вместе)

Прежде чем писать приложение, нужно **подготовить программу-среду**, в которой мы будем работать. Это делается **один раз**.

> ⚠️ Настройка — самая «занудная», но самая важная часть первого урока. Если среда настроена правильно, дальше всё пойдёт легко. Делаем спокойно, шаг за шагом.

### 🪟 На Windows — Visual Studio (так работают ученики)

1. Скачай и установи **Visual Studio 2022 Community** (бесплатная): [visualstudio.microsoft.com](https://visualstudio.microsoft.com/).
2. В **установщике** (Visual Studio Installer) поставь галочку на рабочей нагрузке (workload):
   **«Разработка мобильных приложений на .NET»** (в оригинале — *.NET Multi-platform App UI development*).
   Это и есть MAUI. Без этой галочки шаблона MAUI не будет.
3. Если Visual Studio уже стоит, но галочки нет: открой **Visual Studio Installer → Изменить (Modify) → отметь этот workload → Изменить**.
4. Проверь, что установлен .NET: открой командную строку и набери:
   ```bash
   dotnet --version
   ```
   Должно показать номер вида `10.0.xxx`.

<details>
<summary>🍎 <b>На Mac в VS Code (так работает преподаватель)</b></summary>

На Mac нет Visual Studio (её закрыли) — работаем в **VS Code** через терминал.

1. Установи **VS Code**: [code.visualstudio.com](https://code.visualstudio.com/).
2. Внутри VS Code поставь расширение **C# Dev Kit** (вкладка Extensions).
3. Установи **.NET 10 SDK**: [dotnet.microsoft.com/download](https://dotnet.microsoft.com/download).
4. Установи **MAUI workload** (одна команда в терминале):
   ```bash
   sudo dotnet workload install maui --source https://api.nuget.org/v3/index.json
   ```
5. Для сборки под iPhone/Mac нужен **Xcode** (среда Apple). Установи инструменты:
   ```bash
   xcode-select --install
   ```
   (и сам Xcode — из App Store, если будешь запускать на симуляторе iPhone).
6. Проверь: `dotnet --version` → `10.0.xxx`.

> 📌 Полная шпаргалка команд для Mac/VS Code — в справочнике [`../справочники/среда-VisualStudio-и-VSCode.md`](../справочники/среда-VisualStudio-и-VSCode.md).
</details>

✍️ **Мини-задание 2.** Набери в терминале / командной строке `dotnet --version` и убедись, что видишь номер, начинающийся с `10.`. Если видишь — среда готова. Если пишет «command not found» — не установлен .NET SDK.

---

## 🆕 Часть 3. Создаём первый проект

### 🪟 На Windows (Visual Studio)

1. Открой Visual Studio → **Создание проекта** (Create a new project).
2. В поиске набери **MAUI** → выбери шаблон **.NET MAUI App** → **Далее**.
3. Имя проекта: **`CounterApp`** → выбери папку → **Далее**.
4. Framework: **.NET 10.0** → **Создать**.
5. Подожди немного — Visual Studio создаст проект и подтянет нужные файлы.

<details>
<summary>🍎 <b>На Mac в VS Code</b></summary>

Одна команда в терминале создаёт такой же проект:
```bash
dotnet new maui -o CounterApp
cd CounterApp
```
- `dotnet new maui` — создать MAUI-приложение;
- `-o CounterApp` — в папку с именем `CounterApp`.

Затем открой папку в VS Code: `code .`

> ⚠️ Чтобы приложение собиралось именно под Mac, проверь в файле `CounterApp.csproj`, что есть строка с `net10.0-maccatalyst` (обычно она уже там). Подробности — в справочнике среды.
</details>

### 🗂 Из чего состоит проект (разбираем структуру)

Открой список файлов проекта (в Visual Studio — **Обозреватель решений** / Solution Explorer справа). Не пугайся — файлов много, но нам важны всего несколько:

| Что | Зачем | Часто трогаем? |
|-----|-------|----------------|
| 📁 **Platforms/** | особые файлы для Android, iOS, Windows, Mac | нет |
| 📁 **Resources/** | картинки, шрифты, иконки, цвета | иногда |
| **MauiProgram.cs** | «стартовый мотор» — настраивает и запускает приложение | редко |
| **App.xaml / App.xaml.cs** | глобальные настройки (общие цвета, стили) | редко |
| **AppShell.xaml** | «оболочка» — какая страница открывается первой | иногда |
| **MainPage.xaml** | 👈 **внешность** главного экрана | **да, постоянно** |
| **MainPage.xaml.cs** | 👈 **логика** главного экрана | **да, постоянно** |

> 💡 **Не бойся большого количества файлов.** 90% времени мы работаем с **двумя**: `MainPage.xaml` (рисуем) и `MainPage.xaml.cs` (программируем). Остальное создаётся само.

✍️ **Мини-задание 3.** Найди в своём проекте файлы `MainPage.xaml` и `MainPage.xaml.cs`. Открой оба. Не меняй пока ничего — просто посмотри, что внутри разный текст: в одном — теги в угловых скобках (это XAML), в другом — знакомый C#.

---

## ▶️ Часть 4. Первый запуск — «Hello, World!»

Запустим проект **как есть**, ещё ничего не меняя, — чтобы убедиться, что среда работает.

### 🪟 На Windows

1. Вверху, рядом с зелёной кнопкой ▶, в списке платформ выбери **Windows Machine** (запуск на компьютере — самый быстрый способ проверить).
2. Нажми **▶ (Start)** или **F5**.
3. Подожди (первый запуск — до пары минут). Появится **окно приложения**: картинка робота, надпись *Hello, World!* и кнопка **Click me**.
4. Нажми на кнопку **Click me** несколько раз — увидишь, как меняется её текст (`Clicked 1 time`, `Clicked 2 times`…).

🎉 Поздравляю — это твоё **первое приложение с окном**!

<details>
<summary>🍎 <b>На Mac в VS Code</b></summary>

Запуск на самом Mac (окно-приложение macOS):
```bash
dotnet build -t:Run -f net10.0-maccatalyst
```
- `-t:Run` — собрать **и сразу запустить**;
- `-f net10.0-maccatalyst` — под какую платформу (маковое окно).

Если сборка «капризничает» — почисти и пересобери:
```bash
rm -rf bin obj
dotnet restore
```
</details>

> 🐞 **Если не запускается:** проверь, что выбран правильный workload/платформа (Часть 2), и что интернет был при создании проекта (подтягиваются пакеты). На Mac помогает `rm -rf bin obj` + `dotnet restore`. Больше решений — в [лайфхак.md](лайфхак.md).

✍️ **Мини-задание 4.** Запусти приложение и нажми кнопку **Click me** пять раз. Какой текст стал на кнопке? Закрой окно — это остановит приложение.

---

## 🔎 Часть 5. Разбираем главный экран (`MainPage.xaml`)

Теперь поймём, **как** экран получился таким. Открой `MainPage.xaml`. Разберём его **по кусочкам** — не весь сразу.

### Кусок 1. «Обёртка» страницы

```xml
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CounterApp.MainPage">
    <!-- сюда мы кладём всё, что должно быть на экране -->
</ContentPage>
```

- **`ContentPage`** — это **один экран** приложения. Всё, что мы хотим показать, живёт **внутри** него.
- Строчки `xmlns=...` — служебные (говорят XAML, какие слова он понимает). Их **не трогаем**.
- `x:Class="CounterApp.MainPage"` — связывает этот XAML-файл с его «мозгом» `MainPage.xaml.cs`. Тоже не трогаем.

> 💡 XAML построен на **тегах в угловых скобках**, как HTML: у каждого элемента есть открывающий `<Тег ...>` и либо закрывающий `</Тег>`, либо самозакрытие `/>`. Внутри `<...>` пишем **свойства** вида `Имя="значение"`.

### Кусок 2. Контейнер — «стопка» элементов

```xml
<VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">
    <!-- элементы здесь встанут друг под другом -->
</VerticalStackLayout>
```

- **`VerticalStackLayout`** — контейнер, который ставит элементы **вертикально, друг под другом** (как стопка тарелок). Про контейнеры подробно — в уроке 2.
- `Padding="30"` — **внутренний отступ** от краёв (чтобы не липло к рамке).
- `Spacing="20"` — **расстояние между** элементами внутри стопки.

### Кусок 3. Текст — `Label`

```xml
<Label Text="Hello, World!" FontSize="32" />
```

- **`Label`** — это **текст на экране** (аналог `Console.WriteLine`, только красивый).
- `Text="..."` — что написано;
- `FontSize="32"` — размер шрифта.

### Кусок 4. Кнопка — `Button`

```xml
<Button Text="Click me" Clicked="OnCounterClicked" />
```

- **`Button`** — кнопка, на которую можно нажать.
- `Text="Click me"` — надпись на кнопке;
- `Clicked="OnCounterClicked"` — **самое важное**: имя метода в C#, который выполнится **при нажатии**. То есть «нажали кнопку → запусти метод `OnCounterClicked`».

> 💡 **Связь XAML ↔ C#:** свойство `Clicked` в XAML — это мостик к методу в файле `.xaml.cs`. В XAML мы говорим *«какой метод звать»*, а *сам метод* пишем в C#.

✍️ **Мини-задание 5.** В `MainPage.xaml` найди `Label` с текстом `Hello, World!` и поменяй текст на свой, например `Text="Моё первое приложение"`. Запусти — надпись изменилась. Это твой первый шаг в XAML! 🎨

---

## 🔢 Часть 6. Собираем своё приложение «Счётчик»

Теперь сделаем **свой** экран с нуля: большое число и три кнопки — **+1**, **−1**, **Сброс**. Идём маленькими шагами.

### Шаг 1. Очистим экран под себя

В `MainPage.xaml` **удали** всё, что было внутри `<ContentPage>...</ContentPage>`, и оставь пустой контейнер:

```xml
<VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">

</VerticalStackLayout>
```

- `VerticalOptions="Center"` — прижмёт нашу стопку **по центру** экрана по вертикали.

### Шаг 2. Заголовок

Внутрь стопки добавь первый `Label` — подпись сверху:

```xml
<Label Text="Мой счётчик"
       FontSize="32"
       HorizontalOptions="Center" />
```

- `HorizontalOptions="Center"` — поставит текст **по центру** по горизонтали.

Запусти — вверху появилась надпись «Мой счётчик». ✅

### Шаг 3. Большое число (с именем!)

Теперь — сам счётчик. Это `Label`, но с **именем**, чтобы к нему можно было обратиться из C#:

```xml
<Label x:Name="CountLabel"
       Text="0"
       FontSize="80"
       HorizontalOptions="Center" />
```

- **`x:Name="CountLabel"`** — 🔑 **ключевой момент!** Мы дали метке **имя** `CountLabel`. Теперь в C# мы сможем сказать `CountLabel.Text = ...` и менять это число.
- `Text="0"` — сначала показываем ноль;
- `FontSize="80"` — очень крупно, чтобы число было видно издалека.

> 💡 **Правило:** элемент, который ты хочешь **менять из кода**, должен иметь `x:Name`. Без имени C# «не видит» его. Имена — по-английски, как переменные (`CountLabel`, а не `метка`).

### Шаг 4. Три кнопки

Под числом добавь три кнопки. У каждой — свой метод в `Clicked`:

```xml
<Button Text="+1" Clicked="OnPlusClicked" />
<Button Text="-1" Clicked="OnMinusClicked" />
<Button Text="Сброс" Clicked="OnResetClicked" />
```

Мы **пообещали**, что при нажатии сработают методы `OnPlusClicked`, `OnMinusClicked`, `OnResetClicked`. Их пока нет — сейчас напишем. Если запустить прямо сейчас, будет ошибка «метод не найден» — это нормально, идём в C#.

> ✅ **Весь `MainPage.xaml` целиком** — в [материалы/MainPage.xaml](материалы/MainPage.xaml). Сверься, если что-то не сходится.

### Шаг 5. Переходим в «мозг» — `MainPage.xaml.cs`

Открой `MainPage.xaml.cs`. Внутри класса `MainPage` уже есть конструктор. Первым делом заведём **переменную-счётчик** — она будет помнить текущее число:

```csharp
public partial class MainPage : ContentPage
{
    int count = 0;               // 👈 наше число, живёт всё время работы приложения

    public MainPage()
    {
        InitializeComponent();   // эта строка «оживляет» XAML — не трогаем
    }

    // сюда добавим методы кнопок ⬇️
}
```

- `int count = 0;` — обычная переменная `int`, как в блоке 1. Она объявлена **в классе** (а не внутри метода), поэтому **не обнуляется** между нажатиями — помнит значение.
- `InitializeComponent();` — связывает C# с нашим XAML (создаёт `CountLabel` и кнопки из разметки). Без неё код не увидит элементы.

### Шаг 6. Кнопка «+1»

Добавь метод (внутри класса, ниже конструктора):

```csharp
private void OnPlusClicked(object? sender, EventArgs e)
{
    count++;                          // увеличили число на 1
    CountLabel.Text = count.ToString();   // показали его в метке на экране
}
```

Разберём построчно:
- `private void OnPlusClicked(object? sender, EventArgs e)` — **обработчик события**. Имя `OnPlusClicked` **совпадает** с тем, что мы написали в XAML (`Clicked="OnPlusClicked"`). Скобки `(object? sender, EventArgs e)` — обязательная «подпись» любого обработчика кнопки (что именно нажали и подробности события). Пока просто пишем их всегда так.
- `count++;` — увеличить `count` на 1 (знакомо из блока 1).
- `CountLabel.Text = count.ToString();` — вот зачем нужно было имя! Берём метку `CountLabel` и меняем её `Text`. `count` — число, а `Text` хочет строку, поэтому переводим числом→строкой через `count.ToString()`.

Запусти. Нажми **+1** — число растёт! 🎉

### Шаг 7. Кнопки «−1» и «Сброс»

По образцу добавь ещё два метода:

```csharp
private void OnMinusClicked(object? sender, EventArgs e)
{
    count--;                          // уменьшили на 1
    CountLabel.Text = count.ToString();
}

private void OnResetClicked(object? sender, EventArgs e)
{
    count = 0;                        // обнулили
    CountLabel.Text = count.ToString();
}
```

> ✅ **Весь `MainPage.xaml.cs` целиком** — в [материалы/MainPage.xaml.cs](материалы/MainPage.xaml.cs).

### Шаг 8. Проверяем всё

Запусти и проверь:
- **+1** — число растёт (0 → 1 → 2 …);
- **−1** — уменьшается (может уйти в минус: 0 → −1 — это нормально);
- **Сброс** — снова 0.

**Должно получиться (на экране):**
```
        Мой счётчик

            3

          [ +1 ]
          [ -1 ]
        [ Сброс ]
```

🏆 Готово — ты сделал своё первое **интерактивное** приложение!

✍️ **Мини-задание 6.** Проверь все три кнопки. Затем в XAML поменяй заголовок на «Счётчик кликов» и цвет числа: добавь метке `CountLabel` свойство `TextColor="Purple"`. Запусти — число стало фиолетовым.

---

## 🧠 Как всё связано (соберём картинку)

```
XAML (внешность)                 C# code-behind (мозг)
─────────────────                ──────────────────────
<Button Clicked="OnPlusClicked"> ──►  private void OnPlusClicked(...)
<Label  x:Name="CountLabel">     ◄──  CountLabel.Text = count.ToString();
```

1. Человек **нажимает кнопку** → MAUI вызывает метод из `Clicked`.
2. Метод в C# **меняет данные** (`count`) и **обновляет экран** через `CountLabel.Text`.
3. Человек **видит новое число**.

Это главный цикл любого приложения: **действие пользователя → код → изменение экрана**.

---

## 🎯 Итоги урока

- **MAUI** — приложения с окном на C#, «напиши раз — запусти везде».
- Экран = **XAML** (внешность) + **C# code-behind** (логика).
- Базовые элементы: **`Label`** (текст), **`Button`** (кнопка), контейнер **`VerticalStackLayout`**.
- **`x:Name`** даёт элементу имя, чтобы менять его из кода.
- **`Clicked="Метод"`** связывает кнопку с методом-обработчиком `(object? sender, EventArgs e)`.
- Меняем экран так: `Элемент.Свойство = значение;` (например `CountLabel.Text = ...`).

➡️ Дальше — [практика.md](практика.md) (доработай счётчик), затем [викторина.html](викторина.html).
🆘 Пропустил или собираешь дома — [самоучитель.md](самоучитель.md) (пересборка с нуля по шагам).
🧰 Что-то не запускается — [лайфхак.md](лайфхак.md). Быстро вспомнить — [итого.md](итого.md).

> **В следующем уроке** разберём **макеты** (`VerticalStackLayout` / `HorizontalStackLayout`) подробно — научимся красиво расставлять элементы: в ряд, в колонку и вложенно.
