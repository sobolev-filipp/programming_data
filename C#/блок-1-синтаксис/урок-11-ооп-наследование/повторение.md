# 🔁 Разминка (5 минут) · Урок 11

> Повторяем Урок 10 (классы) — сегодня уберём из них повторение через общего «родителя».

## 🎤 Блиц-вопросы (устно, 2 мин)

1. Что такое класс и объект? (чертёж и то, что по нему сделано) — сегодня свяжем классы в «семью».
2. В Уроке 10 классы часто выходили похожими? (да, почти копия) — сегодня уберём копипасту на примере банка.
3. Что делает свойство с `private`-хранилищем? (прячет поле, даёт доступ через `get/set`) — это и есть инкапсуляция.
4. Как в Godot ты писал в начале скрипта? (`extends Node2D`) — это наследование, сегодня в C#.

## ⌨️ Быстрый разогрев (2 мин)

Набери и запусти — наследник берёт метод у родителя:
```csharp
SavingsAccount s = new SavingsAccount();
s.Owner = "Аня";
s.Deposit(500);
Console.WriteLine($"{s.Owner}: {s.Balance}");

class Account
{
    public string Owner;
    public int Balance;
    public void Deposit(int sum) { Balance += sum; }
}
class SavingsAccount : Account { }
```

- Класс `SavingsAccount` **пустой**, но `Deposit()` работает — он **унаследован** от `Account` (двоеточие `SavingsAccount : Account`). Мы написали `Deposit()` один раз в родителе, а пользуются им все виды счетов. Сегодня научимся ещё и **менять** унаследованное поведение (вклад, карта, зарплатный — по-разному).

## 🌉 Что нового будет сегодня

- **Наследование** `class Child : Parent`, конструктор родителя через `: base(...)`.
- **`virtual`/`override`** — наследник меняет метод; `base.Method()` — версия родителя.
- **Полиморфизм** — `List<Родитель>` из разных наследников.
- **Инкапсуляция** — `private`/`protected`/`public`, прячем данные (баланс).
- Проект — «Банк: счета клиентов» без копипасты.

> ⚙️ Учителю: мотивация прямая — показать копипасту двух счетов (вклад/карта: одинаковые владелец, баланс, `Deposit`) → «вынесем общее в родителя `Account`». Пример намеренно бытовой (не игры) — по просьбе автора чередуем сферы. Мостики: Python `class X(Base)` / `super()`, Godot `extends`. Три кита ООП назвать явно: инкапсуляция (была в ур.10 через свойства), наследование и полиморфизм (сегодня). Урок 12 — финальный проект блока, соберёт всё.
