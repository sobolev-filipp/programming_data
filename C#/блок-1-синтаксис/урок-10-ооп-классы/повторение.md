# 🔁 Разминка (5 минут) · Урок 10

> Повторяем прошлые уроки — сегодня соберём данные и поведение героя в один **свой тип** (класс).

## 🎤 Блиц-вопросы (устно, 2 мин)

1. Как в Уроке 8 мы хранили «героя» одной штукой? (словарь `Dictionary`) — сегодня сделаем это красивее, классом.
2. Что делал метод из Урока 7? (свою команду с параметрами и `return`) — сегодня метод «переедет» внутрь объекта.
3. Что такое `Math.Max(3, 5)` — через что вызывается? (через тип `Math`, без создания объекта) — это `static`, разберём сегодня.
4. Как в Python описывали свой тип? (`class ...:` и `__init__`) — в C# очень похоже.

## ⌨️ Быстрый разогрев (2 мин)

Набери и запусти — это уже класс и объект:
```csharp
Cat cat = new Cat();
cat.Name = "Барсик";
Console.WriteLine("Кот: " + cat.Name);

class Cat
{
    public string Name;
}
```

- Мы описали свой тип `Cat` (чертёж) и создали объект `cat` по нему. `cat.Name` — поле этого объекта. Сегодня научим объекты хранить много данных, действовать и рождаться с готовыми значениями.

## 🌉 Что нового будет сегодня

- **Класс** (чертёж) и **объект** (`new`); **поля** — данные, **методы** — поведение.
- **Конструктор** — создать объект сразу с данными; **свойства** `get/set` — умные поля.
- **`static`** (общее для класса) и **перегрузка** (одно имя — разные параметры).
- Проект — «Арена: Герой против Монстра» из двух классов.

> ⚙️ Учителю: мотивация — показать «россыпь переменных» для двух героев (легко перепутать) → собрать в класс. Мостик из Python (`class`/`__init__`) и Godot (ноды-объекты) сильный. `static` привязать к уже знакомым `Math.Max`/`Console.WriteLine`. Перегрузку показать как то, чего **не умели** локальные функции из Урока 7. Наследование — СЛЕДУЮЩИЙ урок (11), сегодня без него.
