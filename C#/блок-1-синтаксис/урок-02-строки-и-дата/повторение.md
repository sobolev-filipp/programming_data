# 🔁 Разминка (5 минут) · Урок 2

> Повторяем Урок 1 (вывод, типы, ввод, арифметика) — на нём строится работа со строками.

## 🎤 Блиц-вопросы (устно, 2 мин)

1. Чем вывести текст и как вставить в него переменную? (`Console.WriteLine`, `$"...{x}..."`)
2. Что возвращает `Console.ReadLine()`? (строку) Как превратить в число? (`Convert.ToInt32`)
3. Чему равно `10 / 3` для целых и почему? (3 — деление нацело)
4. В каких кавычках `string`, а в каких `char`? (двойные / одинарные)

## ⌨️ Быстрый разогрев (2 мин)

Набери и запусти — вспоминаем ввод и `$"..."`:
```csharp
Console.Write("Как тебя зовут? ");
string name = Console.ReadLine();
Console.WriteLine($"Привет, {name}!");
```

- Работает — отлично. Сегодня научим программу **обрабатывать** это имя: делать заглавным, считать буквы, брать куски.

## 🌉 Что нового будет сегодня

- **Методы строк** через точку: `имя.ToUpper()`, `имя.Length`, `имя.Substring(0,3)` (в Python было `s.upper()`, `len(s)`, `s[0:3]`).
- **Символ по индексу** `имя[0]` (счёт с 0 — как в Python).
- **Поиск** в строке: `Contains`, `IndexOf`.

> ⚙️ Учителю: проверь, что все помнят `$"..."` и ввод. Кто быстро — пусть выведет имя дважды: как есть и через `ToUpper()`.
