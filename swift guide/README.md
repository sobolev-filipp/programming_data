<h1>Справочник SWIFT</h1>

<h2>Оглавление</h2>
<ol>
  <li><a href="#create_app">Создание первого IOS приложения</a></li>
  <li><a href="#let_const">Переменные и константы</a></li>
  <li><a href="#type_date">Типы данных в Swift</li>
  <li><a href="#operator">Операторы в Swift</a></li>
  <li><a href="#controls">Контроль потока выполнения (ветвления)</a></li>
  <li><a href="#functions">Функции в Swift</a></li>
  <li><a href="#collections">Коллекции в Swift</a></li>
  <li><a href="#dictionary">Словари (Dictionary)</a></li>
  <li><a href="#cycle">Циклы в Swift</a></li>
  <li><a href="#optional">Работа с опционалами и специальными символами</a></li>
  <li><a href="#generation">Генерация</a></li>
  <li><a href="#interpolation">Строковая интерполяция</a></li>
  <li><a href="#guard">guard оператор</a></li>
  <li><a href="#picker">UIPickerView</a></li>
</ol>

<br/>
<h2 id="create_app">Создание первого IOS приложения</h2>
<img width="300" height="300" src="https://terminalroot.com/assets/img/programming/swift.jpg" />

1.	Запустите Xcode.
2.	Выберите пункт <b>“Create a new Xcode project”</b> (создание нового проекта).
3.	В открывшемся окне выберите категорию <b>“Application”</b> слева и шаблон <b>“Application”</b> справа.
4.	Нажмите кнопку <b>“Next”</b>.
5.	Заполните параметры проекта:
  + <b>Product Name:</b> придумайте короткое и запоминающееся название (например FirstApp).
  + <b>Team:</b> оставьте пустым, если у вас нет учётной записи разработчика Apple.
  + <b>Organization Identifier:</b> введите обратный домен (например, com.example).
  + <b>Interface:</b> выберите Storyboard.
  + <b>Language:</b> выберите Swift.
  + <b>Use Core Data</b> / <b>Include Unit Tests </b>/ <b>Include UI Tests</b>: отключите эти опции.
7.	Нажмите <b>“Next”</b> и выберите папку для сохранения проекта. Нажмите “Create”.

<h2 id="let_const">Переменные и константы</h2>
<p>Переменные и константы служат для хранения данных в программе.</p>

<h3>Константы (const)</h3>
<p>Константы объявляются с помощью ключевого слова let и не могут быть изменены после присвоения значения.</p>

<p>Пример:</p>

```swift
let pi = 3.14
pi = 3.15 // Ошибка! Нельзя изменить значение константы.
```

<h2>Переменные (var)</h2>

```swift
var age = 12
age += 1 // Теперь возраст равен 13
```

<h2 id="type_date">Типы данных в Swift</h2>
<p>Swift поддерживает широкий спектр встроенных типов данных:</p>

<ul>
  <li><b>Int</b>: целые числа</li>
  <li><b>Double</b> или <b>Float</b>: дробные числа</li>
  <li><b>Bool</b>: логические значения</li>
  <li><b>String</b>: строки</li>
  <li><b>Array</b>: массивы</li>
  <li><b>Dictionary</b>: словари</li>
  <li><b>Set</b>: множества</li>
</ul>

<p>Примеры объявлений:</p>

```swift
//let имя_переменной: тип_данных = значение
let integerValue: Int = 10
let doubleValue: Double = 3.14
let booleanValue: Bool = true
let stringValue: String = "Привет"
```

<h2 id="operator">Операторы в Swift</h2>
<p>Операторы выполняют стандартные математические и логические операции.</p>

<h3>Арифметические операторы:</h3>
<ul>
  <li><b>+</b> - сложение</li>
  <li><b>-</b> - вычитание</li>
  <li><b>*</b> - умножение</li>
  <li><b>/</b> - деление</li>
  <li><b>%</b> - остаток от  деления</li>
</ul>

<p>Пример:</p>

```swift
let sum = 5 + 3 // Результат: 8
let remainder = 10 % 3 // Остаток: 1
```

<h3>Операторы сравнения:</h3>
<ul>
  <li><b>==</b> - равно</li>
  <li><b>!=</b> - не равно</li>
  <li><b><</b> - меньше</li>
  <li><b>></b> - больше</li>
  <li><b><=</b> - меньше или равно</li>
  <li><b>>=</b> - больше или равно</li>
</ul>

<p>Пример:</p>

```swift
let isEqual = 5 == 5 // Истина
let notEqual = 5 != 3 // Истина
```

<h3>Логические операторы:</h3>
<ul>
  <li><b>&&</b> - логическое <b></b>И</b></li>
  <li><b>||</b> - логическое <b>ИЛИ</b></li>
  <li><b>!</b> - отрицание</li>
</ul>

<p>Пример:</p>

```swift
let result = true && false // Ложь
let anotherResult = !false || true // Истина
```

<h2 id="controls">Контроль потока выполнения (ветвления)</h2>
<p>Управляющие конструкции позволяют изменять порядок выполнения программы в зависимости от условий.</p>

<h3>Конструкция if-else:</h3>
<p>Выполняет блок кода, если условие истинно.</p>
<p>Пример:</p>

```swift
let temperature = 25

if temperature >= 30 {
    print("Жарко")
} else if temperature >= 20 {
    print("Тепло")
} else {
    print("Прохладно")
}
```

<h3>Конструкция switch-case:</h3>
<p>Альтернативный способ ветвления, позволяющий проверять множество возможных значений.</p>
<p>Пример:</p>

```swift
let day = "суббота"

switch day {
case "понедельник", "вторник", "среда", "четверг", "пятница":
    print("Будний день")
case "суббота", "воскресенье":
    print("Выходной")
default:
    print("Неправильный день")
}
```

<h2 id="functions">Функции в Swift</h2>
<p>Функции позволяют группировать фрагменты кода и многократно использовать их.</p>

<h3>Простая функция:</h3>

```swift
func greet(name: String) {
    print("Здравствуйте, $name)")
}

greet(name: "Иван") // Вывод: "Здравствуйте, Иван"
```

<h3>Функция с возвратом значения:</h3>

```swift
func addNumbers(a: Int, b: Int) -> Int {
    return a + b
}

let result = addNumbers(a: 5, b: 3) // Результат: 8
```

<h2 id="collections">Коллекции в Swiftd</h2>
<p>Коллекции позволяют хранить группы данных.</p>

<h3>Массивы (Array):</h3>
<p>Хранят упорядоченные последовательности элементов одного типа.</p>

<p>Пример:</p>

```swift
var numbers = [1, 2, 3, 4, 5]
numbers.append(6) // Добавляет элемент в конец массива
```

<h4>Основные операции</h4>
<h5>Создание массива</h5>

```swift
let array = [1, 2, 3]
```

<p>или пустой массив определенного типа:</p>

```swift
var emptyArray: [Int] = []
```

<h5>Добавление элементов</h5>
<ul>
  <li><b>append</b> - добавляет элемент в конец массива:</li>

```swift
array.append(4)
```
  
  <li><b>insert(at)</b> - вставляет элемент в указанную позицию:</li>

```swift
array.insert(0, аи: 0)
```

</ul>

<h5>Удаление элементов</h5>
<ul>
  <li><b>removeLast()</b> - удаляет последний элемент:</li>

```swift
array.removeLast()
```
  
  <li><b>removeFirst()</b> - удаляет первый элемент:</li>

```swift
array.removeFirst()
```
  
  <li><b>remove(at)</b> - удаляет элемент по индексу:</li>

```swift
array.remove(at: 1)
```
  
</ul>

<h5>Доступ к элементам</h5>
<ul>
  <li><b>first</b>,<b>last</b> - доступ к первому и последнему элементам соответственно:</li>

```swift
if let firstElement = array.first {
    print(firstElement)
}
```
  
  <li><b>count</b> - возвращает количество элементов в массиве:</li>

```swift
print(array.count)
```
  
</ul>

<h5>Обход элементов</h5>
<ul>
  <li>Через цикл <b>for</b>:</li>

```swift
for element in array {
    print(element)
}
```
  
  <li>Использование индексов:</li>

```swift
for index in array.indices {
    print("Index: $index), Value: $array[index])")
}
```
  
</ul>

<h5>Изменение порядка элементов</h5>
<ul>
  <li><b>reverse()</b> - переворачивает порядок элементов:</li>

```swift
let reversedArray = array.reversed()
```
  
  <li><b>sort(by:)</b> - сортирует элементы по заданному критерию:</li>

```swift
let sortedArray = array.sorted { $0 < $1 }
```
  
</ul>

<h5>Поиск элемента</h5>
<ul>
  <li><b>contains()</b> - проверяет наличие элемента:</li>

```swift
if array.contains(3) {
    print("Есть!")
}
```
  
  <li><b>filter()</b> - фильтрует элементы по условию:</li>

```swift
let filteredArray = array.filter { $0 > 2 }
```
  
</ul>

<h2 id="dictionary">Словари (Dictionary):</h2>
<p>Хранят пары ключ-значение.</p>
<p>Пример:</p>

```swift
var studentGrades = ["Иван": 5, "Марина": 4]
studentGrades["Сергей"] = 3 // Добавляет новую оценку
```

<h2 id="cycle">Циклы в Swift</h2>
<p>Циклы предназначены для многократного выполнения блоков кода.</p>

<h3>Цикл for-in:</h3>
<p>Проходит по диапазону чисел или элементов массива.</p>
<br/>
<p>Пример:</p>

```swift
for number in 1...5 {
    print(number)
}
```

<br/>
<h3>Цикл while:</h3>
<p>Продолжает выполнение, пока выполняется указанное условие.</p>
<br/>
<p>Пример:</p>

```swift
var counter = 0

while counter < 5 {
    print(counter)
    counter += 1
}
```

<br/>
<h3>Цикл repeat-while:</h3>
<p>Сначала выполняет тело цикла, затем проверяет условие.</p>
<br/>
<p>Пример:</p>

```swift
var count = 0

repeat {
    print(count)
    count += 1
} while count < 5
```

<br/>

<h4>Специальные операторы в Swift</h4>
<br/>
<h5>Диапазоны (1...3, 1..<3)</h5>
<p>Диапазоны позволяют указывать интервалы чисел:</p>
<ul>
  <li><b>1...3</b> - закрытый диапазон (включает обе границы: 1, 2, 3)</li>
  <li><b>1..<3</b> - полуоткрытый диапазон (включает начало, но не конец: 1, 2)</li>
</ul>

<br/>
<p>Пример:</p>

```swift
for i in 1...3 {
    print(i) // выведет 1, 2, 3
}

for j in 1..<3 {
    print(j) // выведет 1, 2
}
```

<br/>

<h5>Оператор объединения с нулевым значением (??)</h5>
<p>Этот оператор используется для замены nil (отсутствующее значение) на другое значение по умолчанию:</p>
<br/>
<p>Пример:</p>

```swift
let optionalValue: String? = nil
let safeValue = optionalValue ?? "Значение по умолчанию"

print(safeValue) // выведет "Значение по умолчанию"
```

<br/>

<h2 id="optional">Работа с опционалами и специальными символами (?, !)</h2>
<p>В Swift многие значения могут быть опциональными, то есть потенциально отсутствующими. Для работы с такими значениями используются специальные символы:</p>
<ul>
  <li><b>?</b> -обозначает опционал (возможно nil)</li>
  <li><b>!</b> - принудительное развертывание опционала (предполагается, что значение не nil)</li>
</ul>

<br/>
<p>Пример:</p>

```swift
let array: [String]? = ["один", "два", "три"]
let element = array?[0] // Опциональный доступ к элементу

let forcedElement = array![0] // Принудительное развертывание (может вызвать ошибку, если array == nil)
```

<br/>

<h2 id="generation">Генерация</h2>
<p><b></b>Генерация</b> — это когда компьютер сам создаёт что-то: числа, слова, картинки и т. д. Например, в игре компьютер может сам выбрать, куда поставить врага, или в приложении — сгенерировать случайный вопрос для викторины.</p>
<br/>

<h3>Генерация чисел</h3>
<br/>

```swift
let randomNumber = Int.random(in: 1...10)
print("Случайное число: \(randomNumber)")
```

<br/>
<p>Где:</p>
<ul>
  <li><b>Int.random(in: 1...10)</b> - команда, которая выбирает случайное целое число от 1 до 10</li>
  <li><b>1...10</b> - диапазон от 1 до 10 включительно</li>
  <li><b>let randomNumber</b> - мы сохраняем выбранное число в переменную, чтобы потом использовать</li>
</ul>
<br/>

<h3>Генерация элементов из коллекции</h3>
<p><b>Коллекция</b> — это «контейнер» для хранения данных.</p>
<p>Самый простой пример — <b>массив</b> (список элементов).</p>

<br/>
<p>Например, у нас есть список животных:</p>

```swift
let animals = ["кошка", "собака", "слон", "тигр"]
```

<br/>
<p>Чтобы выбрать случайное животное, нужно:</p>
<ol>
  <li>Сгенерировать случайный <b>индекс</b> (номер элемента в списке)</li>
  <li>Взять элемент по этому индексу</li>
</ol>
<br/>

```swift
// 1. Генерируем случайный индекс (от 0 до количества элементов - 1)
let randomIndex = Int.random(in: 0..<animals.count)

// 2. Выбираем элемент по этому индексу
let randomAnimal = animals[randomIndex]

print("Случайное животное: \(randomAnimal)")
```

<br/>

<p>В Swift есть удобный метод <mark>randomElement()</mark>, который позволяет выбрать случайный элемент из коллекции (например, массива) <b>без необходимости вручную генерировать индекс</b>. Это упрощает код и снижает риск ошибок.</p>

<br/>
<h4>randomElement</h4>
<p>Порядок работы:</p>
<ol>
  <li>Вызов метода:</li>
  <br/>

```swift
let randomAnimal = animals.randomElement()
```
  
  <br/>
  <li>Метод возвращает <b>опциональное значение</b> (Optional), потому что коллекция может быть пустой — тогда результат будет <mark>nil</mark></li>
  <li>Чтобы безопасно использовать результат, проверяем его:</li>
  <br/>

```swift
if let chosenAnimal = animals.randomElement() {
    print("Случайное животное: \(chosenAnimal)")
} else {
    print("В списке нет животных!")
}
```
  
  <br/>
</ol>

<h5>Почему возвращается Optional?</h5>
<p>Представьте, что у вас есть мешок с конфетами. Если мешок <b>пустой</b>, вы не сможете достать конфету -вот почему метод возвращает <mark>nil</mark> (ничего), если коллекция пуста.</p>

<br/>
<p>Допустим, у нас есть список фруктов:</p>

```swift
let fruits = ["яблоко", "банан", "груша", "апельсин"]
```

<p>Код с <b>randomElement()</b>:</p>

```swift
if let randomFruit = fruits.randomElement() {
    print("Сегодня ешь \(randomFruit)!")
} else {
    print("Фруктов нет 😕")
}
```

<br/>
<h2 id="interpolation">Строковая интерполяция</h2>
<p>В Swift для динамической вставки переменных в строку используется строковая интерполяция — она выглядит как <b><mark>\(variableName)</mark></b>.</p>
<br/>
<p>Пример:</p>

```swift
var story: String = "Great story!"
var total:String = "It's \(story)"
```

<br/>

<h2 id="guard">guard оператор</h2>
<p><b>guard оператор</b> — это особая конструкция в Swift, применяемая для проверки опциональных значений. </p>
<p>Она позволяет проверить, содержит ли опциональная переменная какое-либо значение, и если да — временно раскрыть это значение для дальнейшей работы с ним. Если же опциональная переменная равна <b>nil</b>, то выполняется блок кода, расположенный после ключевого слова <b>else</b>, и дальнейшее выполнение функции прерывается.</p>
<br/>

```swift
guard let variableName = optionalVariable else {
    // Блок кода, который выполняется, если опциональная переменная равна nil
    return // Или любое другое действие для выхода из функции
}

// Дальнейшее выполнение кода, если опциональная переменная раскрыта успешно
```

<br/>
<h2 name="picker">Что такое UIPickerView и зачем он нужен?</h2>
<p><b>UIPickerView</b> — это компонент интерфейса, который позволяет пользователю выбирать значения из списка. Он удобен для выбора из большого количества вариантов и часто используется в приложениях для iOS.</p>

<br/>
<h3>Обязательные методы для работы с UIPickerView:</h3>
<ol>
    <li><b>Количество компонентов (<mark>numberOfComponents</mark>):</b></li>
<p>Этот метод определяет, сколько столбцов будет в UIPickerView. Чаще всего используется один столбец.</p>

```swift
func numberOfComponents(in pickerView: UIPickerView) -> Int {
    return 1
}
```

<li><b>Количество строк в компоненте (<mark>numberOfRowsInComponent</mark>):</b></li>
<p>Этот метод определяет, сколько строк будет в каждом столбце.</p>

```swift
func pickerView(_ pickerView: UIPickerView, numberOfRowsInComponent component: Int) -> Int {
    return data.count
}
```

<li><b>Название строки (<mark>titleForRow</mark>):</b></li>
<p>Этот метод устанавливает текст для каждой строки в <b>UIPickerView.</b></p>

```swift
func pickerView(_ pickerView: UIPickerView, titleForRow row: Int, forComponent component: Int) -> String? {
    return data[row]
}
```

<li><b>Действие при выборе строки (<mark>didSelectRow</mark>):</b></li>
<p>Этот метод вызывается, когда пользователь выбирает строку в UIPickerView.</p>

```swift
func pickerView(_ pickerView: UIPickerView, didSelectRow row: Int, inComponent component: Int) {
    selectedItem = data[row]
}
```

</ol>

